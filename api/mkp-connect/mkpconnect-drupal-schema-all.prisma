generator mkp_connect_drupal_client {
  provider = "prisma-client"
  output   = "_lib/generated/drupal"
}

datasource mkp_connect_drupal_db {
  provider = "mysql"
  url      = env("MKPCONNECT_DRUPAL_DATABASE_URL")
}

model User_Taxonomy_Access {
  id   Int
  uid  Int    @default(0)
  term String @default("") @mkp_connect_drupal_db.VarChar(60)

  @@id([uid, term])
  @@index([uid], map: "uid")
  @@index([uid, term], map: "uid_term")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model accesslog {
  aid       Int     @id @default(autoincrement())
  sid       String  @default("") @mkp_connect_drupal_db.VarChar(128)
  title     String? @mkp_connect_drupal_db.VarChar(255)
  path      String? @mkp_connect_drupal_db.VarChar(255)
  url       String? @mkp_connect_drupal_db.Text
  hostname  String? @mkp_connect_drupal_db.VarChar(128)
  uid       Int?    @default(0) @mkp_connect_drupal_db.UnsignedInt
  timer     Int     @default(0) @mkp_connect_drupal_db.UnsignedInt
  timestamp Int     @default(0) @mkp_connect_drupal_db.UnsignedInt

  @@index([timestamp], map: "accesslog_timestamp")
  @@index([uid], map: "uid")
}

model accesslog_detail {
  aid       Int     @id @default(autoincrement())
  sid       String  @default("") @mkp_connect_drupal_db.VarChar(64)
  title     String? @mkp_connect_drupal_db.VarChar(255)
  path      String? @mkp_connect_drupal_db.VarChar(255)
  url       String? @mkp_connect_drupal_db.Text
  hostname  String? @mkp_connect_drupal_db.VarChar(128)
  uid       Int?    @default(0) @mkp_connect_drupal_db.UnsignedInt
  timer     Int     @default(0) @mkp_connect_drupal_db.UnsignedInt
  timestamp Int     @default(0) @mkp_connect_drupal_db.UnsignedInt

  @@index([timestamp], map: "accesslog_timestamp")
  @@index([uid], map: "uid")
}

model accesslog_summary {
  aid   Int     @id @default(autoincrement())
  uid   Int?    @default(0) @mkp_connect_drupal_db.UnsignedInt
  title String? @mkp_connect_drupal_db.VarChar(255)
  path  String? @mkp_connect_drupal_db.VarChar(255)
  count BigInt
}

model accesslog_summary_default {
  aid   Int     @id @default(0)
  uid   Int?    @default(0) @mkp_connect_drupal_db.UnsignedInt
  title String? @mkp_connect_drupal_db.VarChar(255)
  path  String? @mkp_connect_drupal_db.VarChar(255)
  count BigInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model actions {
  aid        String @id @default("0") @mkp_connect_drupal_db.VarChar(255)
  type       String @default("") @mkp_connect_drupal_db.VarChar(32)
  callback   String @default("") @mkp_connect_drupal_db.VarChar(255)
  parameters Bytes
  label      String @default("0") @mkp_connect_drupal_db.VarChar(255)
}

model allowed_values_board {
  ID       Int     @id @default(autoincrement())
  title    String? @mkp_connect_drupal_db.VarChar(80)
  category Int?
}

model allowed_values_center {
  ID       Int     @id @default(autoincrement())
  title    String? @mkp_connect_drupal_db.VarChar(80)
  region   String? @mkp_connect_drupal_db.VarChar(80)
  category Int?
}

model allowed_values_community {
  ID    Int     @id @default(autoincrement())
  title String? @mkp_connect_drupal_db.VarChar(80)
}

model allowed_values_owner {
  id       Int     @id @default(autoincrement())
  title    String? @mkp_connect_drupal_db.VarChar(80)
  category Int?
  nid      Int?
}

model allowed_values_pos_categories {
  ID    Int     @id @default(autoincrement())
  title String? @mkp_connect_drupal_db.VarChar(80)
}

model allowed_values_region {
  ID    Int     @id @default(autoincrement())
  title String? @mkp_connect_drupal_db.VarChar(80)
  nid   Int?
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model allowed_values_trainings {
  label String  @mkp_connect_drupal_db.VarChar(255)
  value String  @mkp_connect_drupal_db.VarChar(512)
  name  String? @mkp_connect_drupal_db.VarChar(255)

  @@ignore
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model authmap {
  aid      Int    @id @default(autoincrement()) @mkp_connect_drupal_db.UnsignedInt
  uid      Int    @default(0)
  authname String @unique(map: "authname") @default("") @mkp_connect_drupal_db.VarChar(128)
  module   String @default("") @mkp_connect_drupal_db.VarChar(128)

  @@index([uid, module], map: "uid_module")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model batch {
  bid       Int    @id @mkp_connect_drupal_db.UnsignedInt
  token     String @mkp_connect_drupal_db.VarChar(64)
  timestamp Int
  batch     Bytes?

  @@index([token], map: "token")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model block {
  bid        Int    @id @default(autoincrement())
  module     String @default("") @mkp_connect_drupal_db.VarChar(64)
  delta      String @default("0") @mkp_connect_drupal_db.VarChar(32)
  theme      String @default("") @mkp_connect_drupal_db.VarChar(64)
  status     Int    @default(0) @mkp_connect_drupal_db.TinyInt
  weight     Int    @default(0)
  region     String @default("") @mkp_connect_drupal_db.VarChar(64)
  custom     Int    @default(0) @mkp_connect_drupal_db.TinyInt
  visibility Int    @default(0) @mkp_connect_drupal_db.TinyInt
  pages      String @mkp_connect_drupal_db.Text
  title      String @default("") @mkp_connect_drupal_db.VarChar(255)
  cache      Int    @default(1) @mkp_connect_drupal_db.TinyInt

  @@unique([theme, module, delta], map: "tmd")
  @@index([theme, status, region, weight, module], map: "list")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model block_custom {
  bid    Int     @id @default(autoincrement()) @mkp_connect_drupal_db.UnsignedInt
  body   String? @mkp_connect_drupal_db.LongText
  info   String  @unique(map: "info") @default("") @mkp_connect_drupal_db.VarChar(128)
  format String? @mkp_connect_drupal_db.VarChar(255)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model block_node_type {
  module String @mkp_connect_drupal_db.VarChar(64)
  delta  String @mkp_connect_drupal_db.VarChar(32)
  type   String @mkp_connect_drupal_db.VarChar(32)

  @@id([module, delta, type])
  @@index([type], map: "type")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model block_role {
  module String @mkp_connect_drupal_db.VarChar(64)
  delta  String @mkp_connect_drupal_db.VarChar(32)
  rid    Int    @mkp_connect_drupal_db.UnsignedInt

  @@id([module, delta, rid])
  @@index([rid], map: "rid")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model blocked_ips {
  iid Int    @id @default(autoincrement()) @mkp_connect_drupal_db.UnsignedInt
  ip  String @default("") @mkp_connect_drupal_db.VarChar(40)

  @@index([ip], map: "blocked_ip")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model book1 {
  uid String? @mkp_connect_drupal_db.VarChar(255)
  rid String? @mkp_connect_drupal_db.VarChar(255)

  @@ignore
}

model book2 {
  nid String @id @mkp_connect_drupal_db.VarChar(255)
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model book22 {
  nid     String? @mkp_connect_drupal_db.VarChar(255)
  created String? @mkp_connect_drupal_db.VarChar(255)
  changed String? @mkp_connect_drupal_db.VarChar(255)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model book23 {
  nid     String? @mkp_connect_drupal_db.VarChar(255)
  created String? @mkp_connect_drupal_db.VarChar(255)
  changed String? @mkp_connect_drupal_db.VarChar(255)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model book24 {
  nid     String? @mkp_connect_drupal_db.VarChar(255)
  created String? @mkp_connect_drupal_db.VarChar(255)
  changed String? @mkp_connect_drupal_db.VarChar(255)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model book3 {
  uid     String? @mkp_connect_drupal_db.VarChar(255)
  picture String? @mkp_connect_drupal_db.VarChar(255)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model book4 {
  uid     String? @mkp_connect_drupal_db.VarChar(255)
  picture String? @mkp_connect_drupal_db.VarChar(255)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model book41 {
  SID                String? @mkp_connect_drupal_db.VarChar(255)
  submitted_datetime String? @mkp_connect_drupal_db.VarChar(255)
  IP_Address         String? @map("IP Address") @mkp_connect_drupal_db.VarChar(255)
  UID                String? @mkp_connect_drupal_db.VarChar(255)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model book43 {
  SID   String? @mkp_connect_drupal_db.VarChar(255)
  Index String? @mkp_connect_drupal_db.VarChar(255)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model book98 {
  entity_id               String? @mkp_connect_drupal_db.VarChar(255)
  field_join_status_value String? @mkp_connect_drupal_db.VarChar(255)

  @@ignore
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model cache {
  cid        String @id @default("") @mkp_connect_drupal_db.VarChar(255)
  data       Bytes?
  expire     Int    @default(0)
  created    Int    @default(0)
  serialized Int    @default(0) @mkp_connect_drupal_db.SmallInt

  @@index([expire], map: "expire")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model cache_admin_menu {
  cid        String @id @default("") @mkp_connect_drupal_db.VarChar(255)
  data       Bytes?
  expire     Int    @default(0)
  created    Int    @default(0)
  serialized Int    @default(0) @mkp_connect_drupal_db.SmallInt

  @@index([expire], map: "expire")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model cache_block {
  cid        String @id @default("") @mkp_connect_drupal_db.VarChar(255)
  data       Bytes?
  expire     Int    @default(0)
  created    Int    @default(0)
  serialized Int    @default(0) @mkp_connect_drupal_db.SmallInt

  @@index([expire], map: "expire")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model cache_bootstrap {
  cid        String @id @default("") @mkp_connect_drupal_db.VarChar(255)
  data       Bytes?
  expire     Int    @default(0)
  created    Int    @default(0)
  serialized Int    @default(0) @mkp_connect_drupal_db.SmallInt

  @@index([expire], map: "expire")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model cache_entity_og_membership {
  cid        String @id @default("") @mkp_connect_drupal_db.VarChar(255)
  data       Bytes?
  expire     Int    @default(0)
  created    Int    @default(0)
  serialized Int    @default(0) @mkp_connect_drupal_db.SmallInt

  @@index([expire], map: "expire")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model cache_entity_og_membership_type {
  cid        String @id @default("") @mkp_connect_drupal_db.VarChar(255)
  data       Bytes?
  expire     Int    @default(0)
  created    Int    @default(0)
  serialized Int    @default(0) @mkp_connect_drupal_db.SmallInt

  @@index([expire], map: "expire")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model cache_feeds_http {
  cid        String @id @default("") @mkp_connect_drupal_db.VarChar(255)
  data       Bytes?
  expire     Int    @default(0)
  created    Int    @default(0)
  serialized Int    @default(0) @mkp_connect_drupal_db.SmallInt

  @@index([expire], map: "expire")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model cache_field {
  cid        String @id @default("") @mkp_connect_drupal_db.VarChar(255)
  data       Bytes?
  expire     Int    @default(0)
  created    Int    @default(0)
  serialized Int    @default(0) @mkp_connect_drupal_db.SmallInt

  @@index([expire], map: "expire")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model cache_filter {
  cid        String @id @default("") @mkp_connect_drupal_db.VarChar(255)
  data       Bytes?
  expire     Int    @default(0)
  created    Int    @default(0)
  serialized Int    @default(0) @mkp_connect_drupal_db.SmallInt

  @@index([expire], map: "expire")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model cache_form {
  cid        String @id @default("") @mkp_connect_drupal_db.VarChar(255)
  data       Bytes?
  expire     Int    @default(0)
  created    Int    @default(0)
  serialized Int    @default(0) @mkp_connect_drupal_db.SmallInt

  @@index([expire], map: "expire")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model cache_geocoder {
  cid        String @id @default("") @mkp_connect_drupal_db.VarChar(255)
  data       Bytes?
  expire     Int    @default(0)
  created    Int    @default(0)
  serialized Int    @default(0) @mkp_connect_drupal_db.SmallInt

  @@index([expire], map: "expire")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model cache_image {
  cid        String @id @default("") @mkp_connect_drupal_db.VarChar(255)
  data       Bytes?
  expire     Int    @default(0)
  created    Int    @default(0)
  serialized Int    @default(0) @mkp_connect_drupal_db.SmallInt

  @@index([expire], map: "expire")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model cache_libraries {
  cid        String @id @default("") @mkp_connect_drupal_db.VarChar(255)
  data       Bytes?
  expire     Int    @default(0)
  created    Int    @default(0)
  serialized Int    @default(0) @mkp_connect_drupal_db.SmallInt

  @@index([expire], map: "expire")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model cache_location {
  cid        String  @id @default("") @mkp_connect_drupal_db.VarChar(255)
  data       Bytes?
  expire     Int     @default(0)
  created    Int     @default(0)
  headers    String? @mkp_connect_drupal_db.Text
  serialized Int     @default(0) @mkp_connect_drupal_db.SmallInt

  @@index([expire], map: "expire")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model cache_menu {
  cid        String @id @default("") @mkp_connect_drupal_db.VarChar(255)
  data       Bytes?
  expire     Int    @default(0)
  created    Int    @default(0)
  serialized Int    @default(0) @mkp_connect_drupal_db.SmallInt

  @@index([expire], map: "expire")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model cache_page {
  cid        String @id @default("") @mkp_connect_drupal_db.VarChar(255)
  data       Bytes?
  expire     Int    @default(0)
  created    Int    @default(0)
  serialized Int    @default(0) @mkp_connect_drupal_db.SmallInt

  @@index([expire], map: "expire")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model cache_path {
  cid        String @id @default("") @mkp_connect_drupal_db.VarChar(255)
  data       Bytes?
  expire     Int    @default(0)
  created    Int    @default(0)
  serialized Int    @default(0) @mkp_connect_drupal_db.SmallInt

  @@index([expire], map: "expire")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model cache_rules {
  cid        String @id @default("") @mkp_connect_drupal_db.VarChar(255)
  data       Bytes?
  expire     Int    @default(0)
  created    Int    @default(0)
  serialized Int    @default(0) @mkp_connect_drupal_db.SmallInt

  @@index([expire], map: "expire")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model cache_token {
  cid        String @id @default("") @mkp_connect_drupal_db.VarChar(255)
  data       Bytes?
  expire     Int    @default(0)
  created    Int    @default(0)
  serialized Int    @default(0) @mkp_connect_drupal_db.SmallInt

  @@index([expire], map: "expire")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model cache_update {
  cid        String @id @default("") @mkp_connect_drupal_db.VarChar(255)
  data       Bytes?
  expire     Int    @default(0)
  created    Int    @default(0)
  serialized Int    @default(0) @mkp_connect_drupal_db.SmallInt

  @@index([expire], map: "expire")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model cache_views {
  cid        String @id @default("") @mkp_connect_drupal_db.VarChar(255)
  data       Bytes?
  expire     Int    @default(0)
  created    Int    @default(0)
  serialized Int    @default(0) @mkp_connect_drupal_db.SmallInt

  @@index([expire], map: "expire")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model cache_views_data {
  cid        String @id @default("") @mkp_connect_drupal_db.VarChar(255)
  data       Bytes?
  expire     Int    @default(0)
  created    Int    @default(0)
  serialized Int    @default(1) @mkp_connect_drupal_db.SmallInt

  @@index([expire], map: "expire")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model cck_field_settings {
  field_name     String  @mkp_connect_drupal_db.VarChar(32)
  entity_type    String  @default("") @mkp_connect_drupal_db.VarChar(32)
  bundle         String? @mkp_connect_drupal_db.VarChar(32)
  language       String? @mkp_connect_drupal_db.VarChar(32)
  setting_type   String  @mkp_connect_drupal_db.VarChar(32)
  setting        String  @mkp_connect_drupal_db.VarChar(128)
  setting_option String? @mkp_connect_drupal_db.MediumText

  @@ignore
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model civicrm_action_mapping {
  id                  Int     @id @default(autoincrement()) @mkp_connect_drupal_db.UnsignedInt
  entity              String? @mkp_connect_drupal_db.VarChar(64)
  entity_value        String? @mkp_connect_drupal_db.VarChar(64)
  entity_value_label  String? @mkp_connect_drupal_db.VarChar(64)
  entity_status       String? @mkp_connect_drupal_db.VarChar(64)
  entity_status_label String? @mkp_connect_drupal_db.VarChar(64)
  entity_date_start   String? @mkp_connect_drupal_db.VarChar(64)
  entity_date_end     String? @mkp_connect_drupal_db.VarChar(64)
  entity_recipient    String? @mkp_connect_drupal_db.VarChar(64)
}

model civicrm_member_roles_rules {
  rule_id      Int    @id @default(autoincrement())
  rid          Int    @mkp_connect_drupal_db.UnsignedInt
  type_id      Int    @mkp_connect_drupal_db.UnsignedInt
  status_codes String @mkp_connect_drupal_db.Text
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model ckeditor_input_format {
  name   String @default("") @mkp_connect_drupal_db.VarChar(128)
  format String @default("") @mkp_connect_drupal_db.VarChar(128)

  @@id([name, format])
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model ckeditor_settings {
  name     String  @id @default("") @mkp_connect_drupal_db.VarChar(128)
  settings String? @mkp_connect_drupal_db.Text
}

model content_access {
  nid      Int     @id @default(0) @mkp_connect_drupal_db.UnsignedInt
  settings String? @mkp_connect_drupal_db.MediumText
}

model content_type_positions {
  vid                         Int     @id @default(0) @mkp_connect_drupal_db.UnsignedInt
  nid                         Int     @default(0) @mkp_connect_drupal_db.UnsignedInt
  field_pos_role_value        String? @mkp_connect_drupal_db.VarChar(80)
  field_pos_order_value       Int?
  field_pos_uid_uid           Int?    @mkp_connect_drupal_db.UnsignedInt
  field_pos_center_nid        Int?    @mkp_connect_drupal_db.UnsignedInt
  field_pos_center_name_value String? @mkp_connect_drupal_db.LongText
  field_pos_category_value    String? @mkp_connect_drupal_db.LongText
  field_pos_entity_type_value String? @mkp_connect_drupal_db.LongText
  field_maincontact_value     String? @mkp_connect_drupal_db.LongText
  field_pos_center_id_value   String? @mkp_connect_drupal_db.LongText
  field_term_enddate_value    String? @mkp_connect_drupal_db.VarChar(20)
  field_public_contact_value  String? @mkp_connect_drupal_db.LongText
  field_term_startdate_value  String? @mkp_connect_drupal_db.VarChar(20)

  @@index([field_pos_center_nid], map: "field_pos_center_nid")
  @@index([field_pos_uid_uid], map: "field_pos_uid_uid")
  @@index([nid], map: "nid")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model context {
  name           String  @id @default("") @mkp_connect_drupal_db.VarChar(255)
  description    String  @default("") @mkp_connect_drupal_db.VarChar(255)
  tag            String  @default("") @mkp_connect_drupal_db.VarChar(255)
  conditions     String? @mkp_connect_drupal_db.Text
  reactions      String? @mkp_connect_drupal_db.Text
  condition_mode Int?    @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model ctools_css_cache {
  cid      String  @id @mkp_connect_drupal_db.VarChar(128)
  filename String? @mkp_connect_drupal_db.VarChar(255)
  css      String? @mkp_connect_drupal_db.LongText
  filter   Int?    @mkp_connect_drupal_db.TinyInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model ctools_object_cache {
  sid     String @mkp_connect_drupal_db.VarChar(64)
  name    String @mkp_connect_drupal_db.VarChar(128)
  obj     String @mkp_connect_drupal_db.VarChar(128)
  updated Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  data    Bytes?

  @@id([sid, obj, name])
  @@index([updated], map: "updated")
}

model custom_og_invite {
  iid   Int    @id @default(autoincrement()) @mkp_connect_drupal_db.UnsignedInt
  gid   Int
  email String @mkp_connect_drupal_db.VarChar(255)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model data_tables {
  title        String  @default("") @mkp_connect_drupal_db.VarChar(255)
  name         String  @id @default("") @mkp_connect_drupal_db.VarChar(128)
  table_schema String? @mkp_connect_drupal_db.Text
  meta         String? @mkp_connect_drupal_db.Text
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model date_format_locale {
  format   String @mkp_connect_drupal_db.VarChar(100)
  type     String @mkp_connect_drupal_db.VarChar(64)
  language String @mkp_connect_drupal_db.VarChar(12)

  @@id([type, language])
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model date_format_type {
  type   String @id @mkp_connect_drupal_db.VarChar(64)
  title  String @mkp_connect_drupal_db.VarChar(255)
  locked Int    @default(0) @mkp_connect_drupal_db.TinyInt

  @@index([title], map: "title")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model date_formats {
  dfid   Int    @id @default(autoincrement()) @mkp_connect_drupal_db.UnsignedInt
  format String @mkp_connect_drupal_db.VarChar(100)
  type   String @mkp_connect_drupal_db.VarChar(64)
  locked Int    @default(0) @mkp_connect_drupal_db.TinyInt

  @@unique([format, type], map: "formats")
}

model diagnostics {
  id        Int      @id @default(autoincrement())
  session   String?  @mkp_connect_drupal_db.VarChar(255)
  batchid   Int?
  type      String?  @mkp_connect_drupal_db.VarChar(20)
  source    String?  @mkp_connect_drupal_db.VarChar(255)
  nid       Int?
  timestamp DateTime @mkp_connect_drupal_db.DateTime(0)
  message   String   @mkp_connect_drupal_db.VarChar(4000)
  report    String?  @default("0") @mkp_connect_drupal_db.VarChar(30)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model dr7mkp_node {
  nid       Int    @id @default(autoincrement()) @mkp_connect_drupal_db.UnsignedInt
  vid       Int?   @unique(map: "vid") @mkp_connect_drupal_db.UnsignedInt
  type      String @default("") @mkp_connect_drupal_db.VarChar(32)
  language  String @default("") @mkp_connect_drupal_db.VarChar(12)
  title     String @default("") @mkp_connect_drupal_db.VarChar(255)
  uid       Int    @default(0)
  status    Int    @default(1)
  created   Int    @default(0)
  changed   Int    @default(0)
  comment   Int    @default(0)
  promote   Int    @default(0)
  sticky    Int    @default(0)
  tnid      Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  translate Int    @default(0)

  @@index([language], map: "language")
  @@index([changed], map: "node_changed")
  @@index([created], map: "node_created")
  @@index([promote, status, sticky, created], map: "node_frontpage")
  @@index([status, type, nid], map: "node_status_type")
  @@index([title, type(length: 4)], map: "node_title_type")
  @@index([type(length: 4)], map: "node_type")
  @@index([tnid], map: "tnid")
  @@index([translate], map: "translate")
  @@index([uid], map: "uid")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model event_participant {
  id            Int     @id @default(autoincrement())
  uid           Int     @default(0) @mkp_connect_drupal_db.UnsignedInt
  sort_name     String? @mkp_connect_drupal_db.VarChar(128)
  title         String? @mkp_connect_drupal_db.VarChar(255)
  event_id      Int?
  event_type_id Int?    @default(0) @mkp_connect_drupal_db.UnsignedInt
  role_id       String? @mkp_connect_drupal_db.VarChar(128)
  nodeid_25     Int?
  start_date    String? @mkp_connect_drupal_db.Text
  latest        Int?

  @@index([uid, nodeid_25], map: "uid_nodeid_25")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model facebook_tracking_pixel_base_codes {
  base_code_id     Int     @id @default(autoincrement())
  base_code_name   String? @default("") @mkp_connect_drupal_db.VarChar(255)
  base_code_fbid   String  @mkp_connect_drupal_db.VarChar(200)
  base_code_global Int?
  weight           Int     @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model facebook_tracking_pixel_events_path {
  event_id           Int     @id @default(autoincrement())
  event_uid          String? @mkp_connect_drupal_db.VarChar(200)
  event_name         String? @mkp_connect_drupal_db.VarChar(200)
  event_path         String? @default("") @mkp_connect_drupal_db.VarChar(255)
  event_path_system  String? @default("") @mkp_connect_drupal_db.VarChar(255)
  event_type         String? @default("") @mkp_connect_drupal_db.VarChar(1000)
  event_js_file      String? @default("") @mkp_connect_drupal_db.VarChar(255)
  event_base_code_id Int?
  event_enable       Int?    @default(1)
  weight             Int     @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model feed_import_hashes {
  id                Int    @id @default(autoincrement()) @mkp_connect_drupal_db.UnsignedInt
  feed_machine_name String @mkp_connect_drupal_db.VarChar(64)
  feed_group        String @mkp_connect_drupal_db.VarChar(32)
  entity            String @mkp_connect_drupal_db.VarChar(64)
  entity_id         Int    @mkp_connect_drupal_db.UnsignedInt
  hash              String @mkp_connect_drupal_db.VarChar(32)
  expire            Int?   @default(0) @mkp_connect_drupal_db.UnsignedInt

  @@index([feed_group, entity, hash], map: "hash_index")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model feed_import_settings {
  id                Int    @id @default(autoincrement()) @mkp_connect_drupal_db.UnsignedInt
  name              String @mkp_connect_drupal_db.VarChar(64)
  machine_name      String @unique(map: "machine_name") @mkp_connect_drupal_db.VarChar(64)
  entity            String @mkp_connect_drupal_db.VarChar(64)
  cron_import       Int    @mkp_connect_drupal_db.UnsignedTinyInt
  settings          Bytes
  last_run          Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  last_run_duration Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  last_run_items    Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model feeds_importer {
  id     String @id @default("") @mkp_connect_drupal_db.VarChar(128)
  config Bytes?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model feeds_item {
  entity_type String @default("") @mkp_connect_drupal_db.VarChar(32)
  entity_id   Int    @mkp_connect_drupal_db.UnsignedInt
  id          String @default("") @mkp_connect_drupal_db.VarChar(128)
  feed_nid    Int    @mkp_connect_drupal_db.UnsignedInt
  imported    Int    @default(0)
  url         String @mkp_connect_drupal_db.Text
  guid        String @mkp_connect_drupal_db.Text
  hash        String @default("") @mkp_connect_drupal_db.VarChar(32)

  @@id([entity_type, entity_id])
  @@index([feed_nid], map: "feed_nid")
  @@index([entity_type, guid(length: 128)], map: "global_lookup_guid")
  @@index([entity_type, url(length: 128)], map: "global_lookup_url")
  @@index([id], map: "id")
  @@index([imported], map: "imported")
  @@index([entity_type, id, feed_nid, guid(length: 128)], map: "lookup_guid")
  @@index([entity_type, id, feed_nid, url(length: 128)], map: "lookup_url")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model feeds_log {
  flid         Int    @id @default(autoincrement())
  id           String @default("") @mkp_connect_drupal_db.VarChar(128)
  feed_nid     Int    @mkp_connect_drupal_db.UnsignedInt
  log_time     Int    @default(0)
  request_time Int    @default(0)
  type         String @default("") @mkp_connect_drupal_db.VarChar(64)
  message      String @mkp_connect_drupal_db.LongText
  variables    Bytes
  severity     Int    @default(0) @mkp_connect_drupal_db.UnsignedTinyInt

  @@index([id], map: "id")
  @@index([id, feed_nid], map: "id_feed_nid")
  @@index([log_time], map: "log_time")
  @@index([request_time], map: "request_time")
  @@index([type], map: "type")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model feeds_push_subscriptions {
  domain        String  @default("") @mkp_connect_drupal_db.VarChar(128)
  subscriber_id Int     @default(0) @mkp_connect_drupal_db.UnsignedInt
  timestamp     Int     @default(0)
  hub           String  @mkp_connect_drupal_db.Text
  topic         String  @mkp_connect_drupal_db.Text
  secret        String  @default("") @mkp_connect_drupal_db.VarChar(128)
  status        String  @default("") @mkp_connect_drupal_db.VarChar(64)
  post_fields   String? @mkp_connect_drupal_db.Text

  @@id([domain, subscriber_id])
  @@index([timestamp], map: "timestamp")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model feeds_source {
  id             String @default("") @mkp_connect_drupal_db.VarChar(128)
  feed_nid       Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  config         Bytes?
  source         String @mkp_connect_drupal_db.Text
  state          Bytes?
  fetcher_result Bytes?
  imported       Int    @default(0) @mkp_connect_drupal_db.UnsignedInt

  @@id([id, feed_nid])
  @@index([feed_nid], map: "feed_nid")
  @@index([id], map: "id")
  @@index([id, source(length: 128)], map: "id_source")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_config {
  id             Int    @id @default(autoincrement())
  field_name     String @mkp_connect_drupal_db.VarChar(32)
  type           String @mkp_connect_drupal_db.VarChar(128)
  module         String @default("") @mkp_connect_drupal_db.VarChar(128)
  active         Int    @default(0) @mkp_connect_drupal_db.TinyInt
  storage_type   String @mkp_connect_drupal_db.VarChar(128)
  storage_module String @default("") @mkp_connect_drupal_db.VarChar(128)
  storage_active Int    @default(0) @mkp_connect_drupal_db.TinyInt
  locked         Int    @default(0) @mkp_connect_drupal_db.TinyInt
  data           Bytes
  cardinality    Int    @default(0) @mkp_connect_drupal_db.TinyInt
  translatable   Int    @default(0) @mkp_connect_drupal_db.TinyInt
  deleted        Int    @default(0) @mkp_connect_drupal_db.TinyInt

  @@index([active], map: "active")
  @@index([deleted], map: "deleted")
  @@index([field_name], map: "field_name")
  @@index([module], map: "module")
  @@index([storage_active], map: "storage_active")
  @@index([storage_module], map: "storage_module")
  @@index([storage_type], map: "storage_type")
  @@index([type], map: "type")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_config_instance {
  id          Int    @id @default(autoincrement())
  field_id    Int
  field_name  String @default("") @mkp_connect_drupal_db.VarChar(32)
  entity_type String @default("") @mkp_connect_drupal_db.VarChar(32)
  bundle      String @default("") @mkp_connect_drupal_db.VarChar(128)
  data        Bytes
  deleted     Int    @default(0) @mkp_connect_drupal_db.TinyInt

  @@index([deleted], map: "deleted")
  @@index([field_name, entity_type, bundle], map: "field_name_bundle")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_body {
  entity_type  String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle       String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted      Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id    Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id  Int?    @mkp_connect_drupal_db.UnsignedInt
  language     String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta        Int     @mkp_connect_drupal_db.UnsignedInt
  body_value   String? @mkp_connect_drupal_db.LongText
  body_summary String? @mkp_connect_drupal_db.LongText
  body_format  String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([body_format], map: "body_format")
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_body_copy1 {
  entity_type  String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle       String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted      Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id    Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id  Int?    @mkp_connect_drupal_db.UnsignedInt
  language     String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta        Int     @mkp_connect_drupal_db.UnsignedInt
  body_value   String? @mkp_connect_drupal_db.LongText
  body_summary String? @mkp_connect_drupal_db.LongText
  body_format  String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([body_format], map: "body_format")
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_comment_body {
  entity_type         String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle              String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted             Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id           Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id         Int?    @mkp_connect_drupal_db.UnsignedInt
  language            String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta               Int     @mkp_connect_drupal_db.UnsignedInt
  comment_body_value  String? @mkp_connect_drupal_db.LongText
  comment_body_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([comment_body_format], map: "comment_body_format")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_about_entity {
  entity_type               String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                    String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                   Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                 Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id               Int?    @mkp_connect_drupal_db.UnsignedInt
  language                  String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                     Int     @mkp_connect_drupal_db.UnsignedInt
  field_about_entity_value  String? @mkp_connect_drupal_db.LongText
  field_about_entity_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_about_entity_format], map: "field_about_entity_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_address {
  entity_type          String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle               String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted              Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id            Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id          Int?    @mkp_connect_drupal_db.UnsignedInt
  language             String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                Int     @mkp_connect_drupal_db.UnsignedInt
  field_address_value  String? @mkp_connect_drupal_db.VarChar(120)
  field_address_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_address_format], map: "field_address_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_admin {
  entity_type        String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle             String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted            Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id          Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id        Int?    @mkp_connect_drupal_db.UnsignedInt
  language           String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta              Int     @mkp_connect_drupal_db.UnsignedInt
  field_admin_value  String? @mkp_connect_drupal_db.VarChar(60)
  field_admin_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_admin_format], map: "field_admin_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_admin_email {
  entity_type              String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                   String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                  Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id              Int?    @mkp_connect_drupal_db.UnsignedInt
  language                 String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                    Int     @mkp_connect_drupal_db.UnsignedInt
  field_admin_email_value  String? @mkp_connect_drupal_db.VarChar(60)
  field_admin_email_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_admin_email_format], map: "field_admin_email_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_admin_email1 {
  entity_type               String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                    String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                   Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                 Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id               Int?    @mkp_connect_drupal_db.UnsignedInt
  language                  String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                     Int     @mkp_connect_drupal_db.UnsignedInt
  field_admin_email1_value  String? @mkp_connect_drupal_db.VarChar(60)
  field_admin_email1_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_admin_email1_format], map: "field_admin_email1_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_admin_phone {
  entity_type              String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                   String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                  Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id              Int?    @mkp_connect_drupal_db.UnsignedInt
  language                 String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                    Int     @mkp_connect_drupal_db.UnsignedInt
  field_admin_phone_value  String? @mkp_connect_drupal_db.VarChar(40)
  field_admin_phone_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_admin_phone_format], map: "field_admin_phone_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_admin_uid {
  entity_type           String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted               Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id             Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id           Int?   @mkp_connect_drupal_db.UnsignedInt
  language              String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                 Int    @mkp_connect_drupal_db.UnsignedInt
  field_admin_uid_value Int?

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_announce_group {
  entity_type                String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                     String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                    Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                  Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id                Int?   @mkp_connect_drupal_db.UnsignedInt
  language                   String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                      Int    @mkp_connect_drupal_db.UnsignedInt
  field_announce_group_value Int?

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_approved {
  entity_type          String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle               String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted              Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id            Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id          Int?   @mkp_connect_drupal_db.UnsignedInt
  language             String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                Int    @mkp_connect_drupal_db.UnsignedInt
  field_approved_value Int?

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_approved_value], map: "field_approved_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_approved_by {
  entity_type              String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                   String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                  Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id              Int?    @mkp_connect_drupal_db.UnsignedInt
  language                 String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                    Int     @mkp_connect_drupal_db.UnsignedInt
  field_approved_by_value  String? @mkp_connect_drupal_db.VarChar(40)
  field_approved_by_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_approved_by_format], map: "field_approved_by_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_area {
  entity_type      String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle           String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted          Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id        Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id      Int?    @mkp_connect_drupal_db.UnsignedInt
  language         String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta            Int     @mkp_connect_drupal_db.UnsignedInt
  field_area_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_area_value], map: "field_area_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_area_copy1 {
  entity_type      String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle           String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted          Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id        Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id      Int?    @mkp_connect_drupal_db.UnsignedInt
  language         String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta            Int     @mkp_connect_drupal_db.UnsignedInt
  field_area_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_area_value], map: "field_area_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_area_link {
  entity_type            String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                 String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id              Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id            Int?    @mkp_connect_drupal_db.UnsignedInt
  language               String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                  Int     @mkp_connect_drupal_db.UnsignedInt
  field_area_link_value  String? @mkp_connect_drupal_db.VarChar(60)
  field_area_link_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_area_link_format], map: "field_area_link_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_area_nid {
  entity_type          String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle               String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted              Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id            Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id          Int?   @mkp_connect_drupal_db.UnsignedInt
  language             String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                Int    @mkp_connect_drupal_db.UnsignedInt
  field_area_nid_value Int?

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_area_nid_copy1 {
  entity_type          String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle               String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted              Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id            Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id          Int?   @mkp_connect_drupal_db.UnsignedInt
  language             String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                Int    @mkp_connect_drupal_db.UnsignedInt
  field_area_nid_value Int?

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_areacommunity_entity {
  entity_type                       String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                            String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                           Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                         Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                       Int?    @mkp_connect_drupal_db.UnsignedInt
  language                          String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                             Int     @mkp_connect_drupal_db.UnsignedInt
  field_areacommunity_entity_value  String? @mkp_connect_drupal_db.VarChar(80)
  field_areacommunity_entity_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_areacommunity_entity_format], map: "field_areacommunity_entity_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_areatype {
  entity_type        String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle             String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted            Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id          Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id        Int?   @mkp_connect_drupal_db.UnsignedInt
  language           String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta              Int    @mkp_connect_drupal_db.UnsignedInt
  field_areatype_tid Int?   @mkp_connect_drupal_db.UnsignedInt

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_areatype_tid], map: "field_areatype_tid")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_audience {
  entity_type          String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle               String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted              Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id            Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id          Int?    @mkp_connect_drupal_db.UnsignedInt
  language             String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                Int     @mkp_connect_drupal_db.UnsignedInt
  field_audience_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_audience_value], map: "field_audience_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_broadcast {
  entity_type           String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted               Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id             Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id           Int?   @mkp_connect_drupal_db.UnsignedInt
  language              String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                 Int    @mkp_connect_drupal_db.UnsignedInt
  field_broadcast_value Int?

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_broadcast_value], map: "field_broadcast_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_build_complete {
  entity_type                String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                     String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                    Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                  Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id                Int?   @mkp_connect_drupal_db.UnsignedInt
  language                   String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                      Int    @mkp_connect_drupal_db.UnsignedInt
  field_build_complete_value Int?

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_build_complete_value], map: "field_build_complete_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_caaid {
  entity_type       String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle            String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted           Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id         Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id       Int?   @mkp_connect_drupal_db.UnsignedInt
  language          String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta             Int    @mkp_connect_drupal_db.UnsignedInt
  field_caaid_value Int?

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_calendar_widget {
  entity_type                  String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                       String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                      Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                    Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                  Int?    @mkp_connect_drupal_db.UnsignedInt
  language                     String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                        Int     @mkp_connect_drupal_db.UnsignedInt
  field_calendar_widget_value  String? @mkp_connect_drupal_db.VarChar(180)
  field_calendar_widget_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_calendar_widget_format], map: "field_calendar_widget_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_category {
  entity_type          String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle               String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted              Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id            Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id          Int?    @mkp_connect_drupal_db.UnsignedInt
  language             String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                Int     @mkp_connect_drupal_db.UnsignedInt
  field_category_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_category_value], map: "field_category_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_center {
  entity_type         String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle              String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted             Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id           Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id         Int?    @mkp_connect_drupal_db.UnsignedInt
  language            String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta               Int     @mkp_connect_drupal_db.UnsignedInt
  field_center_value  String? @mkp_connect_drupal_db.VarChar(40)
  field_center_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_center_format], map: "field_center_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_chartered {
  entity_type           String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted               Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id             Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id           Int?   @mkp_connect_drupal_db.UnsignedInt
  language              String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                 Int    @mkp_connect_drupal_db.UnsignedInt
  field_chartered_value Int?

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_chartered_value], map: "field_chartered_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_city {
  entity_type       String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle            String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted           Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id         Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id       Int?    @mkp_connect_drupal_db.UnsignedInt
  language          String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta             Int     @mkp_connect_drupal_db.UnsignedInt
  field_city_value  String? @mkp_connect_drupal_db.VarChar(80)
  field_city_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_city_format], map: "field_city_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_civi_part_event {
  entity_type                 String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                      String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                     Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                   Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id                 Int?   @mkp_connect_drupal_db.UnsignedInt
  language                    String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                       Int    @mkp_connect_drupal_db.UnsignedInt
  field_civi_part_event_value Int?

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_civi_staff_event {
  entity_type                  String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                       String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                      Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                    Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id                  Int?   @mkp_connect_drupal_db.UnsignedInt
  language                     String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                        Int    @mkp_connect_drupal_db.UnsignedInt
  field_civi_staff_event_value Int?

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_civicrm_event {
  entity_type               String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                    String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                   Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                 Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id               Int?   @mkp_connect_drupal_db.UnsignedInt
  language                  String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                     Int    @mkp_connect_drupal_db.UnsignedInt
  field_civicrm_event_value Int?

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_civicrm_event_2 {
  entity_type                 String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                      String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                     Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                   Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id                 Int?   @mkp_connect_drupal_db.UnsignedInt
  language                    String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                       Int    @mkp_connect_drupal_db.UnsignedInt
  field_civicrm_event_2_value Int?

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_class {
  entity_type        String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle             String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted            Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id          Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id        Int?    @mkp_connect_drupal_db.UnsignedInt
  language           String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta              Int     @mkp_connect_drupal_db.UnsignedInt
  field_class_value  String? @mkp_connect_drupal_db.VarChar(60)
  field_class_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_class_format], map: "field_class_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_committee {
  entity_type           String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted               Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id             Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id           Int?    @mkp_connect_drupal_db.UnsignedInt
  language              String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                 Int     @mkp_connect_drupal_db.UnsignedInt
  field_committee_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_committee_value], map: "field_committee_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_committee_nid {
  entity_type               String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                    String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                   Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                 Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id               Int?   @mkp_connect_drupal_db.UnsignedInt
  language                  String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                     Int    @mkp_connect_drupal_db.UnsignedInt
  field_committee_nid_value Int?

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_community {
  entity_type           String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted               Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id             Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id           Int?    @mkp_connect_drupal_db.UnsignedInt
  language              String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                 Int     @mkp_connect_drupal_db.UnsignedInt
  field_community_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_community_value], map: "field_community_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_community_link {
  entity_type                 String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                      String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                     Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                   Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                 Int?    @mkp_connect_drupal_db.UnsignedInt
  language                    String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                       Int     @mkp_connect_drupal_db.UnsignedInt
  field_community_link_value  String? @mkp_connect_drupal_db.VarChar(60)
  field_community_link_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_community_link_format], map: "field_community_link_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_community_nid {
  entity_type               String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                    String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                   Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                 Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id               Int?   @mkp_connect_drupal_db.UnsignedInt
  language                  String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                     Int    @mkp_connect_drupal_db.UnsignedInt
  field_community_nid_value Int?

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_community_nid_copy1 {
  entity_type               String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                    String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                   Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                 Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id               Int?   @mkp_connect_drupal_db.UnsignedInt
  language                  String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                     Int    @mkp_connect_drupal_db.UnsignedInt
  field_community_nid_value Int?

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_contact {
  entity_type       String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle            String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted           Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id         Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id       Int?   @mkp_connect_drupal_db.UnsignedInt
  language          String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta             Int    @mkp_connect_drupal_db.UnsignedInt
  field_contact_uid Int?   @mkp_connect_drupal_db.UnsignedInt

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_contact_uid], map: "field_contact_uid")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_contact_email {
  entity_type                String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                     String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                    Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                  Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                Int?    @mkp_connect_drupal_db.UnsignedInt
  language                   String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                      Int     @mkp_connect_drupal_db.UnsignedInt
  field_contact_email_value  String? @mkp_connect_drupal_db.VarChar(80)
  field_contact_email_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_contact_email_format], map: "field_contact_email_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_contact_name {
  entity_type               String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                    String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                   Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                 Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id               Int?    @mkp_connect_drupal_db.UnsignedInt
  language                  String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                     Int     @mkp_connect_drupal_db.UnsignedInt
  field_contact_name_value  String? @mkp_connect_drupal_db.VarChar(120)
  field_contact_name_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_contact_name_format], map: "field_contact_name_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_contact_phone {
  entity_type                String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                     String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                    Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                  Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                Int?    @mkp_connect_drupal_db.UnsignedInt
  language                   String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                      Int     @mkp_connect_drupal_db.UnsignedInt
  field_contact_phone_value  String? @mkp_connect_drupal_db.VarChar(40)
  field_contact_phone_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_contact_phone_format], map: "field_contact_phone_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_content_type {
  entity_type              String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                   String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                  Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id              Int?    @mkp_connect_drupal_db.UnsignedInt
  language                 String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                    Int     @mkp_connect_drupal_db.UnsignedInt
  field_content_type_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_content_type_value], map: "field_content_type_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_country {
  entity_type          String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle               String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted              Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id            Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id          Int?    @mkp_connect_drupal_db.UnsignedInt
  language             String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                Int     @mkp_connect_drupal_db.UnsignedInt
  field_country_value  String? @mkp_connect_drupal_db.VarChar(60)
  field_country_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_country_format], map: "field_country_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_csz {
  entity_type      String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle           String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted          Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id        Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id      Int?    @mkp_connect_drupal_db.UnsignedInt
  language         String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta            Int     @mkp_connect_drupal_db.UnsignedInt
  field_csz_value  String? @mkp_connect_drupal_db.VarChar(255)
  field_csz_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_csz_format], map: "field_csz_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_data_system {
  entity_type             String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                  String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                 Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id               Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id             Int?    @mkp_connect_drupal_db.UnsignedInt
  language                String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                   Int     @mkp_connect_drupal_db.UnsignedInt
  field_data_system_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_data_system_value], map: "field_data_system_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_description {
  entity_type              String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                   String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                  Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id              Int?    @mkp_connect_drupal_db.UnsignedInt
  language                 String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                    Int     @mkp_connect_drupal_db.UnsignedInt
  field_description_value  String? @mkp_connect_drupal_db.VarChar(100)
  field_description_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_description_format], map: "field_description_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_do_not_list {
  entity_type             String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                  String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                 Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id               Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id             Int?    @mkp_connect_drupal_db.UnsignedInt
  language                String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                   Int     @mkp_connect_drupal_db.UnsignedInt
  field_do_not_list_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_do_not_list_value], map: "field_do_not_list_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_docudate {
  entity_type          String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle               String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted              Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id            Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id          Int?   @mkp_connect_drupal_db.UnsignedInt
  language             String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                Int    @mkp_connect_drupal_db.UnsignedInt
  field_docudate_value Int?

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_drupal_mos_node {
  entity_type                 String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                      String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                     Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                   Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id                 Int?   @mkp_connect_drupal_db.UnsignedInt
  language                    String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                       Int    @mkp_connect_drupal_db.UnsignedInt
  field_drupal_mos_node_value Int?

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_drupal_nb_node {
  entity_type                String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                     String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                    Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                  Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id                Int?   @mkp_connect_drupal_db.UnsignedInt
  language                   String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                      Int    @mkp_connect_drupal_db.UnsignedInt
  field_drupal_nb_node_value Int?

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_drupal_staff_node {
  entity_type                   String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                        String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                       Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                     Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id                   Int?   @mkp_connect_drupal_db.UnsignedInt
  language                      String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                         Int    @mkp_connect_drupal_db.UnsignedInt
  field_drupal_staff_node_value Int?

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_email_date {
  entity_type            String    @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                 String    @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                Int       @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id              Int       @mkp_connect_drupal_db.UnsignedInt
  revision_id            Int?      @mkp_connect_drupal_db.UnsignedInt
  language               String    @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                  Int       @mkp_connect_drupal_db.UnsignedInt
  field_email_date_value DateTime? @mkp_connect_drupal_db.DateTime(0)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_email_date_value], map: "field_email_date_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_email_from {
  entity_type             String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                  String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                 Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id               Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id             Int?    @mkp_connect_drupal_db.UnsignedInt
  language                String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                   Int     @mkp_connect_drupal_db.UnsignedInt
  field_email_from_value  String? @mkp_connect_drupal_db.VarChar(240)
  field_email_from_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_email_from_format], map: "field_email_from_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_email_prefix {
  entity_type               String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                    String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                   Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                 Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id               Int?    @mkp_connect_drupal_db.UnsignedInt
  language                  String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                     Int     @mkp_connect_drupal_db.UnsignedInt
  field_email_prefix_value  String? @mkp_connect_drupal_db.VarChar(40)
  field_email_prefix_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_email_prefix_format], map: "field_email_prefix_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_end_date {
  entity_type          String    @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle               String    @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted              Int       @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id            Int       @mkp_connect_drupal_db.UnsignedInt
  revision_id          Int?      @mkp_connect_drupal_db.UnsignedInt
  language             String    @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                Int       @mkp_connect_drupal_db.UnsignedInt
  field_end_date_value DateTime? @mkp_connect_drupal_db.DateTime(0)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_end_date_value], map: "field_end_date_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_entity {
  entity_type        String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle             String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted            Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id          Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id        Int?   @mkp_connect_drupal_db.UnsignedInt
  language           String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta              Int    @mkp_connect_drupal_db.UnsignedInt
  field_entity_value Int?

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_entity_type {
  entity_type             String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                  String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                 Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id               Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id             Int?    @mkp_connect_drupal_db.UnsignedInt
  language                String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                   Int     @mkp_connect_drupal_db.UnsignedInt
  field_entity_type_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_entity_type_value], map: "field_entity_type_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_event_complete {
  entity_type                String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                     String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                    Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                  Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id                Int?   @mkp_connect_drupal_db.UnsignedInt
  language                   String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                      Int    @mkp_connect_drupal_db.UnsignedInt
  field_event_complete_value Int?

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_event_complete_value], map: "field_event_complete_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_event_image {
  entity_type              String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                   String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                  Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id              Int?    @mkp_connect_drupal_db.UnsignedInt
  language                 String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                    Int     @mkp_connect_drupal_db.UnsignedInt
  field_event_image_fid    Int?    @mkp_connect_drupal_db.UnsignedInt
  field_event_image_alt    String? @mkp_connect_drupal_db.VarChar(512)
  field_event_image_title  String? @mkp_connect_drupal_db.VarChar(1024)
  field_event_image_width  Int?    @mkp_connect_drupal_db.UnsignedInt
  field_event_image_height Int?    @mkp_connect_drupal_db.UnsignedInt

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_event_image_fid], map: "field_event_image_fid")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_event_subtype {
  entity_type               String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                    String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                   Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                 Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id               Int?    @mkp_connect_drupal_db.UnsignedInt
  language                  String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                     Int     @mkp_connect_drupal_db.UnsignedInt
  field_event_subtype_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_event_subtype_value], map: "field_event_subtype_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_event_type {
  entity_type            String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                 String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id              Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id            Int?    @mkp_connect_drupal_db.UnsignedInt
  language               String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                  Int     @mkp_connect_drupal_db.UnsignedInt
  field_event_type_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_event_type_value], map: "field_event_type_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_extra_information {
  entity_type                    String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                         String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                        Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                      Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                    Int?    @mkp_connect_drupal_db.UnsignedInt
  language                       String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                          Int     @mkp_connect_drupal_db.UnsignedInt
  field_extra_information_value  String? @mkp_connect_drupal_db.LongText
  field_extra_information_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_extra_information_format], map: "field_extra_information_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_facility {
  entity_type           String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted               Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id             Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id           Int?    @mkp_connect_drupal_db.UnsignedInt
  language              String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                 Int     @mkp_connect_drupal_db.UnsignedInt
  field_facility_value  String? @mkp_connect_drupal_db.VarChar(80)
  field_facility_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_facility_format], map: "field_facility_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_feed_item_description {
  entity_type                         String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                              String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                             Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                           Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                         Int?    @mkp_connect_drupal_db.UnsignedInt
  language                            String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                               Int     @mkp_connect_drupal_db.UnsignedInt
  field_feed_item_description_value   String? @mkp_connect_drupal_db.LongText
  field_feed_item_description_summary String? @mkp_connect_drupal_db.LongText
  field_feed_item_description_format  String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_feed_item_description_format], map: "field_feed_item_description_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_file_attachment {
  entity_type                       String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                            String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                           Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                         Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                       Int?    @mkp_connect_drupal_db.UnsignedInt
  language                          String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                             Int     @mkp_connect_drupal_db.UnsignedInt
  field_file_attachment_fid         Int?    @mkp_connect_drupal_db.UnsignedInt
  field_file_attachment_display     Int     @default(1) @mkp_connect_drupal_db.UnsignedTinyInt
  field_file_attachment_description String? @mkp_connect_drupal_db.Text

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_file_attachment_fid], map: "field_file_attachment_fid")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_file_attachment_copy1 {
  entity_type                       String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                            String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                           Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                         Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                       Int?    @mkp_connect_drupal_db.UnsignedInt
  language                          String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                             Int     @mkp_connect_drupal_db.UnsignedInt
  field_file_attachment_fid         Int?    @mkp_connect_drupal_db.UnsignedInt
  field_file_attachment_display     Int     @default(1) @mkp_connect_drupal_db.UnsignedTinyInt
  field_file_attachment_description String? @mkp_connect_drupal_db.Text

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_file_attachment_fid], map: "field_file_attachment_fid")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_focus {
  entity_type       String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle            String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted           Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id         Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id       Int?    @mkp_connect_drupal_db.UnsignedInt
  language          String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta             Int     @mkp_connect_drupal_db.UnsignedInt
  field_focus_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_focus_value], map: "field_focus_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_format {
  entity_type        String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle             String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted            Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id          Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id        Int?    @mkp_connect_drupal_db.UnsignedInt
  language           String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta              Int     @mkp_connect_drupal_db.UnsignedInt
  field_format_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_format_value], map: "field_format_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_frequency {
  entity_type           String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted               Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id             Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id           Int?    @mkp_connect_drupal_db.UnsignedInt
  language              String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                 Int     @mkp_connect_drupal_db.UnsignedInt
  field_frequency_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_frequency_value], map: "field_frequency_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_gateway {
  entity_type         String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle              String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted             Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id           Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id         Int?    @mkp_connect_drupal_db.UnsignedInt
  language            String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta               Int     @mkp_connect_drupal_db.UnsignedInt
  field_gateway_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_gateway_value], map: "field_gateway_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_gateway_copy {
  entity_type         String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle              String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted             Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id           Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id         Int?    @mkp_connect_drupal_db.UnsignedInt
  language            String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta               Int     @mkp_connect_drupal_db.UnsignedInt
  field_gateway_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_gateway_value], map: "field_gateway_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_geo1 {
  entity_type         String   @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle              String   @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted             Int      @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id           Int      @mkp_connect_drupal_db.UnsignedInt
  revision_id         Int?     @mkp_connect_drupal_db.UnsignedInt
  language            String   @default("") @mkp_connect_drupal_db.VarChar(32)
  delta               Int      @mkp_connect_drupal_db.UnsignedInt
  field_geo1_geom     Bytes?
  field_geo1_geo_type String?  @default("") @mkp_connect_drupal_db.VarChar(64)
  field_geo1_lat      Decimal? @mkp_connect_drupal_db.Decimal(18, 12)
  field_geo1_lon      Decimal? @mkp_connect_drupal_db.Decimal(18, 12)
  field_geo1_left     Decimal? @mkp_connect_drupal_db.Decimal(18, 12)
  field_geo1_top      Decimal? @mkp_connect_drupal_db.Decimal(18, 12)
  field_geo1_right    Decimal? @mkp_connect_drupal_db.Decimal(18, 12)
  field_geo1_bottom   Decimal? @mkp_connect_drupal_db.Decimal(18, 12)
  field_geo1_geohash  String?  @mkp_connect_drupal_db.VarChar(16)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_geo1_top, field_geo1_bottom, field_geo1_left, field_geo1_right], map: "field_geo1_bbox")
  @@index([field_geo1_bottom], map: "field_geo1_bottom")
  @@index([field_geo1_lat, field_geo1_lon], map: "field_geo1_centroid")
  @@index([field_geo1_geohash], map: "field_geo1_geohash")
  @@index([field_geo1_lat], map: "field_geo1_lat")
  @@index([field_geo1_left], map: "field_geo1_left")
  @@index([field_geo1_lon], map: "field_geo1_lon")
  @@index([field_geo1_right], map: "field_geo1_right")
  @@index([field_geo1_top], map: "field_geo1_top")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_geolocate {
  entity_type              String   @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                   String   @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                  Int      @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                Int      @mkp_connect_drupal_db.UnsignedInt
  revision_id              Int?     @mkp_connect_drupal_db.UnsignedInt
  language                 String   @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                    Int      @mkp_connect_drupal_db.UnsignedInt
  field_geolocate_geom     Bytes?
  field_geolocate_geo_type String?  @default("") @mkp_connect_drupal_db.VarChar(64)
  field_geolocate_lat      Decimal? @mkp_connect_drupal_db.Decimal(18, 12)
  field_geolocate_lon      Decimal? @mkp_connect_drupal_db.Decimal(18, 12)
  field_geolocate_left     Decimal? @mkp_connect_drupal_db.Decimal(18, 12)
  field_geolocate_top      Decimal? @mkp_connect_drupal_db.Decimal(18, 12)
  field_geolocate_right    Decimal? @mkp_connect_drupal_db.Decimal(18, 12)
  field_geolocate_bottom   Decimal? @mkp_connect_drupal_db.Decimal(18, 12)
  field_geolocate_geohash  String?  @mkp_connect_drupal_db.VarChar(16)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_geolocate_top, field_geolocate_bottom, field_geolocate_left, field_geolocate_right], map: "field_geolocate_bbox")
  @@index([field_geolocate_bottom], map: "field_geolocate_bottom")
  @@index([field_geolocate_lat, field_geolocate_lon], map: "field_geolocate_centroid")
  @@index([field_geolocate_geohash], map: "field_geolocate_geohash")
  @@index([field_geolocate_lat], map: "field_geolocate_lat")
  @@index([field_geolocate_left], map: "field_geolocate_left")
  @@index([field_geolocate_lon], map: "field_geolocate_lon")
  @@index([field_geolocate_right], map: "field_geolocate_right")
  @@index([field_geolocate_top], map: "field_geolocate_top")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_google_drive {
  entity_type               String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                    String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                   Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                 Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id               Int?    @mkp_connect_drupal_db.UnsignedInt
  language                  String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                     Int     @mkp_connect_drupal_db.UnsignedInt
  field_google_drive_value  String? @mkp_connect_drupal_db.VarChar(80)
  field_google_drive_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_google_drive_format], map: "field_google_drive_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_google_group {
  entity_type              String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                   String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                  Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id              Int?   @mkp_connect_drupal_db.UnsignedInt
  language                 String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                    Int    @mkp_connect_drupal_db.UnsignedInt
  field_google_group_value Int?

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_google_group_value], map: "field_google_group_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_group_email {
  entity_type              String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                   String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                  Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id              Int?    @mkp_connect_drupal_db.UnsignedInt
  language                 String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                    Int     @mkp_connect_drupal_db.UnsignedInt
  field_group_email_value  String? @mkp_connect_drupal_db.VarChar(60)
  field_group_email_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_group_email_format], map: "field_group_email_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_group_id {
  entity_type          String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle               String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted              Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id            Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id          Int?   @mkp_connect_drupal_db.UnsignedInt
  language             String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                Int    @mkp_connect_drupal_db.UnsignedInt
  field_group_id_value Int?

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_group_photo {
  entity_type              String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                   String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                  Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id              Int?    @mkp_connect_drupal_db.UnsignedInt
  language                 String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                    Int     @mkp_connect_drupal_db.UnsignedInt
  field_group_photo_fid    Int?    @mkp_connect_drupal_db.UnsignedInt
  field_group_photo_alt    String? @mkp_connect_drupal_db.VarChar(512)
  field_group_photo_title  String? @mkp_connect_drupal_db.VarChar(1024)
  field_group_photo_width  Int?    @mkp_connect_drupal_db.UnsignedInt
  field_group_photo_height Int?    @mkp_connect_drupal_db.UnsignedInt

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_group_photo_fid], map: "field_group_photo_fid")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_group_type {
  entity_type            String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                 String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id              Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id            Int?    @mkp_connect_drupal_db.UnsignedInt
  language               String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                  Int     @mkp_connect_drupal_db.UnsignedInt
  field_group_type_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_group_type_value], map: "field_group_type_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_group_type_2 {
  entity_type              String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                   String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                  Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id              Int?    @mkp_connect_drupal_db.UnsignedInt
  language                 String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                    Int     @mkp_connect_drupal_db.UnsignedInt
  field_group_type_2_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_group_type_2_value], map: "field_group_type_2_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_history {
  entity_type          String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle               String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted              Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id            Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id          Int?    @mkp_connect_drupal_db.UnsignedInt
  language             String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                Int     @mkp_connect_drupal_db.UnsignedInt
  field_history_value  String? @mkp_connect_drupal_db.LongText
  field_history_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_history_format], map: "field_history_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_ig_member {
  entity_type           String   @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                String   @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted               Int      @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id             Int      @mkp_connect_drupal_db.UnsignedInt
  revision_id           Int?     @mkp_connect_drupal_db.UnsignedInt
  language              String   @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                 Int      @mkp_connect_drupal_db.UnsignedInt
  field_ig_member_value Decimal? @mkp_connect_drupal_db.Decimal(10, 2)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_igp_prequisite {
  entity_type                String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                     String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                    Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                  Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                Int?    @mkp_connect_drupal_db.UnsignedInt
  language                   String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                      Int     @mkp_connect_drupal_db.UnsignedInt
  field_igp_prequisite_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_igp_prequisite_value], map: "field_igp_prequisite_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_igroup_class {
  entity_type              String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                   String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                  Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id              Int?    @mkp_connect_drupal_db.UnsignedInt
  language                 String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                    Int     @mkp_connect_drupal_db.UnsignedInt
  field_igroup_class_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_igroup_class_value], map: "field_igroup_class_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_igroup_initiated_visit {
  entity_type                        String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                             String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                            Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                          Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                        Int?    @mkp_connect_drupal_db.UnsignedInt
  language                           String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                              Int     @mkp_connect_drupal_db.UnsignedInt
  field_igroup_initiated_visit_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_igroup_initiated_visit_value], map: "field_igroup_initiated_visit_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_igroup_new_members {
  entity_type                    String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                         String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                        Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                      Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                    Int?    @mkp_connect_drupal_db.UnsignedInt
  language                       String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                          Int     @mkp_connect_drupal_db.UnsignedInt
  field_igroup_new_members_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_igroup_new_members_value], map: "field_igroup_new_members_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_igroup_status {
  entity_type               String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                    String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                   Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                 Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id               Int?    @mkp_connect_drupal_db.UnsignedInt
  language                  String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                     Int     @mkp_connect_drupal_db.UnsignedInt
  field_igroup_status_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_igroup_status_value], map: "field_igroup_status_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_igroup_type {
  entity_type             String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                  String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                 Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id               Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id             Int?    @mkp_connect_drupal_db.UnsignedInt
  language                String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                   Int     @mkp_connect_drupal_db.UnsignedInt
  field_igroup_type_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_igroup_type_value], map: "field_igroup_type_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_igroup_uninitiated_visit {
  entity_type                          String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                               String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                              Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                            Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                          Int?    @mkp_connect_drupal_db.UnsignedInt
  language                             String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                                Int     @mkp_connect_drupal_db.UnsignedInt
  field_igroup_uninitiated_visit_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_igroup_uninitiated_visit_value], map: "field_igroup_uninitiated_visit_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_image {
  entity_type        String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle             String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted            Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id          Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id        Int?    @mkp_connect_drupal_db.UnsignedInt
  language           String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta              Int     @mkp_connect_drupal_db.UnsignedInt
  field_image_fid    Int?    @mkp_connect_drupal_db.UnsignedInt
  field_image_alt    String? @mkp_connect_drupal_db.VarChar(512)
  field_image_title  String? @mkp_connect_drupal_db.VarChar(1024)
  field_image_width  Int?    @mkp_connect_drupal_db.UnsignedInt
  field_image_height Int?    @mkp_connect_drupal_db.UnsignedInt

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_image_fid], map: "field_image_fid")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_image_location {
  entity_type                 String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                      String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                     Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                   Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                 Int?    @mkp_connect_drupal_db.UnsignedInt
  language                    String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                       Int     @mkp_connect_drupal_db.UnsignedInt
  field_image_location_value  String? @mkp_connect_drupal_db.VarChar(240)
  field_image_location_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_image_location_format], map: "field_image_location_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_image_location_copy {
  entity_type                 String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                      String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                     Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                   Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                 Int?    @mkp_connect_drupal_db.UnsignedInt
  language                    String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                       Int     @mkp_connect_drupal_db.UnsignedInt
  field_image_location_value  String? @mkp_connect_drupal_db.VarChar(240)
  field_image_location_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_image_location_format], map: "field_image_location_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_img {
  entity_type      String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle           String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted          Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id        Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id      Int?    @mkp_connect_drupal_db.UnsignedInt
  language         String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta            Int     @mkp_connect_drupal_db.UnsignedInt
  field_img_fid    Int?    @mkp_connect_drupal_db.UnsignedInt
  field_img_alt    String? @mkp_connect_drupal_db.VarChar(512)
  field_img_title  String? @mkp_connect_drupal_db.VarChar(1024)
  field_img_width  Int?    @mkp_connect_drupal_db.UnsignedInt
  field_img_height Int?    @mkp_connect_drupal_db.UnsignedInt

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_img_fid], map: "field_img_fid")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_invitation_email_address {
  entity_type                           String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                                String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                               Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                             Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                           Int?    @mkp_connect_drupal_db.UnsignedInt
  language                              String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                                 Int     @mkp_connect_drupal_db.UnsignedInt
  field_invitation_email_address_value  String? @mkp_connect_drupal_db.VarChar(255)
  field_invitation_email_address_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_invitation_email_address_format], map: "field_invitation_email_address_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_invitation_email_body {
  entity_type                        String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                             String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                            Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                          Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                        Int?    @mkp_connect_drupal_db.UnsignedInt
  language                           String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                              Int     @mkp_connect_drupal_db.UnsignedInt
  field_invitation_email_body_value  String? @mkp_connect_drupal_db.LongText
  field_invitation_email_body_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_invitation_email_body_format], map: "field_invitation_email_body_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_invitation_email_subject {
  entity_type                           String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                                String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                               Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                             Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                           Int?    @mkp_connect_drupal_db.UnsignedInt
  language                              String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                                 Int     @mkp_connect_drupal_db.UnsignedInt
  field_invitation_email_subject_value  String? @mkp_connect_drupal_db.VarChar(255)
  field_invitation_email_subject_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_invitation_email_subject_format], map: "field_invitation_email_subject_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_join_status {
  entity_type             String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                  String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                 Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id               Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id             Int?    @mkp_connect_drupal_db.UnsignedInt
  language                String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                   Int     @mkp_connect_drupal_db.UnsignedInt
  field_join_status_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_join_status_value], map: "field_join_status_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_language {
  entity_type          String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle               String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted              Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id            Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id          Int?    @mkp_connect_drupal_db.UnsignedInt
  language             String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                Int     @mkp_connect_drupal_db.UnsignedInt
  field_language_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_language_value], map: "field_language_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_latitude {
  entity_type          String   @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle               String   @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted              Int      @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id            Int      @mkp_connect_drupal_db.UnsignedInt
  revision_id          Int?     @mkp_connect_drupal_db.UnsignedInt
  language             String   @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                Int      @mkp_connect_drupal_db.UnsignedInt
  field_latitude_value Decimal? @mkp_connect_drupal_db.Decimal(10, 6)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_leader {
  entity_type         String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle              String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted             Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id           Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id         Int?    @mkp_connect_drupal_db.UnsignedInt
  language            String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta               Int     @mkp_connect_drupal_db.UnsignedInt
  field_leader_value  String? @mkp_connect_drupal_db.VarChar(120)
  field_leader_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_leader_format], map: "field_leader_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_legacy_nid {
  entity_type             String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                  String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                 Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id               Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id             Int?    @mkp_connect_drupal_db.UnsignedInt
  language                String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                   Int     @mkp_connect_drupal_db.UnsignedInt
  field_legacy_nid_value  String? @mkp_connect_drupal_db.VarChar(255)
  field_legacy_nid_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_legacy_nid_format], map: "field_legacy_nid_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_library_destination {
  entity_type                     String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                          String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                         Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                       Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                     Int?    @mkp_connect_drupal_db.UnsignedInt
  language                        String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                           Int     @mkp_connect_drupal_db.UnsignedInt
  field_library_destination_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_library_destination_value], map: "field_library_destination_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_link_1_link {
  entity_type              String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                   String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                  Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id              Int?    @mkp_connect_drupal_db.UnsignedInt
  language                 String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                    Int     @mkp_connect_drupal_db.UnsignedInt
  field_link_1_link_value  String? @mkp_connect_drupal_db.VarChar(100)
  field_link_1_link_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_link_1_link_format], map: "field_link_1_link_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_link_1_title {
  entity_type               String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                    String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                   Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                 Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id               Int?    @mkp_connect_drupal_db.UnsignedInt
  language                  String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                     Int     @mkp_connect_drupal_db.UnsignedInt
  field_link_1_title_value  String? @mkp_connect_drupal_db.VarChar(100)
  field_link_1_title_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_link_1_title_format], map: "field_link_1_title_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_link_2_link {
  entity_type              String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                   String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                  Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id              Int?    @mkp_connect_drupal_db.UnsignedInt
  language                 String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                    Int     @mkp_connect_drupal_db.UnsignedInt
  field_link_2_link_value  String? @mkp_connect_drupal_db.VarChar(100)
  field_link_2_link_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_link_2_link_format], map: "field_link_2_link_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_link_2_title {
  entity_type               String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                    String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                   Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                 Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id               Int?    @mkp_connect_drupal_db.UnsignedInt
  language                  String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                     Int     @mkp_connect_drupal_db.UnsignedInt
  field_link_2_title_value  String? @mkp_connect_drupal_db.VarChar(100)
  field_link_2_title_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_link_2_title_format], map: "field_link_2_title_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_link_3_link {
  entity_type              String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                   String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                  Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id              Int?    @mkp_connect_drupal_db.UnsignedInt
  language                 String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                    Int     @mkp_connect_drupal_db.UnsignedInt
  field_link_3_link_value  String? @mkp_connect_drupal_db.VarChar(100)
  field_link_3_link_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_link_3_link_format], map: "field_link_3_link_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_link_3_title {
  entity_type               String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                    String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                   Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                 Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id               Int?    @mkp_connect_drupal_db.UnsignedInt
  language                  String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                     Int     @mkp_connect_drupal_db.UnsignedInt
  field_link_3_title_value  String? @mkp_connect_drupal_db.VarChar(100)
  field_link_3_title_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_link_3_title_format], map: "field_link_3_title_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_loc_lat_lon {
  entity_type              String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                   String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                  Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id              Int?    @mkp_connect_drupal_db.UnsignedInt
  language                 String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                    Int     @mkp_connect_drupal_db.UnsignedInt
  field_loc_lat_lon_value  String? @mkp_connect_drupal_db.VarChar(100)
  field_loc_lat_lon_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_loc_lat_lon_format], map: "field_loc_lat_lon_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_location {
  entity_type           String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted               Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id             Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id           Int?    @mkp_connect_drupal_db.UnsignedInt
  language              String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                 Int     @mkp_connect_drupal_db.UnsignedInt
  field_location_value  String? @mkp_connect_drupal_db.VarChar(100)
  field_location_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_location_format], map: "field_location_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_longitude {
  entity_type           String   @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                String   @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted               Int      @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id             Int      @mkp_connect_drupal_db.UnsignedInt
  revision_id           Int?     @mkp_connect_drupal_db.UnsignedInt
  language              String   @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                 Int      @mkp_connect_drupal_db.UnsignedInt
  field_longitude_value Decimal? @mkp_connect_drupal_db.Decimal(10, 6)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_main_contact {
  entity_type              String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                   String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                  Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id              Int?   @mkp_connect_drupal_db.UnsignedInt
  language                 String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                    Int    @mkp_connect_drupal_db.UnsignedInt
  field_main_contact_value Int?

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_main_contact_value], map: "field_main_contact_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_max_participants {
  entity_type                  String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                       String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                      Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                    Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id                  Int?   @mkp_connect_drupal_db.UnsignedInt
  language                     String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                        Int    @mkp_connect_drupal_db.UnsignedInt
  field_max_participants_value Int?

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_meeting_night {
  entity_type               String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                    String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                   Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                 Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id               Int?    @mkp_connect_drupal_db.UnsignedInt
  language                  String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                     Int     @mkp_connect_drupal_db.UnsignedInt
  field_meeting_night_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_meeting_night_value], map: "field_meeting_night_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_meeting_time {
  entity_type               String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                    String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                   Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                 Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id               Int?    @mkp_connect_drupal_db.UnsignedInt
  language                  String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                     Int     @mkp_connect_drupal_db.UnsignedInt
  field_meeting_time_value  String? @mkp_connect_drupal_db.VarChar(120)
  field_meeting_time_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_meeting_time_format], map: "field_meeting_time_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_message_id {
  entity_type             String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                  String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                 Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id               Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id             Int?    @mkp_connect_drupal_db.UnsignedInt
  language                String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                   Int     @mkp_connect_drupal_db.UnsignedInt
  field_message_id_value  String? @mkp_connect_drupal_db.VarChar(320)
  field_message_id_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_message_id_format], map: "field_message_id_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_migrate_example_country {
  entity_type                          String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                               String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                              Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                            Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                          Int?    @mkp_connect_drupal_db.UnsignedInt
  language                             String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                                Int     @mkp_connect_drupal_db.UnsignedInt
  field_migrate_example_country_value  String? @mkp_connect_drupal_db.VarChar(255)
  field_migrate_example_country_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_migrate_example_country_format], map: "field_migrate_example_country_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_migrate_example_favbeers {
  entity_type                        String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                             String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                            Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                          Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id                        Int?   @mkp_connect_drupal_db.UnsignedInt
  language                           String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                              Int    @mkp_connect_drupal_db.UnsignedInt
  field_migrate_example_favbeers_nid Int?   @mkp_connect_drupal_db.UnsignedInt

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_migrate_example_favbeers_nid], map: "field_migrate_example_favbeers_nid")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_migrate_example_gender {
  entity_type                        String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                             String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                            Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                          Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id                        Int?   @mkp_connect_drupal_db.UnsignedInt
  language                           String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                              Int    @mkp_connect_drupal_db.UnsignedInt
  field_migrate_example_gender_value Int?

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_migrate_example_gender_value], map: "field_migrate_example_gender_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_migrate_example_image {
  entity_type                        String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                             String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                            Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                          Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                        Int?    @mkp_connect_drupal_db.UnsignedInt
  language                           String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                              Int     @mkp_connect_drupal_db.UnsignedInt
  field_migrate_example_image_fid    Int?    @mkp_connect_drupal_db.UnsignedInt
  field_migrate_example_image_alt    String? @mkp_connect_drupal_db.VarChar(512)
  field_migrate_example_image_title  String? @mkp_connect_drupal_db.VarChar(1024)
  field_migrate_example_image_width  Int?    @mkp_connect_drupal_db.UnsignedInt
  field_migrate_example_image_height Int?    @mkp_connect_drupal_db.UnsignedInt

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_migrate_example_image_fid], map: "field_migrate_example_image_fid")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_migrate_example_top_vintag {
  entity_type                            String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                                 String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                                Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                              Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id                            Int?   @mkp_connect_drupal_db.UnsignedInt
  language                               String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                                  Int    @mkp_connect_drupal_db.UnsignedInt
  field_migrate_example_top_vintag_value Int?

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_migrate_example_wine_ratin {
  entity_type                            String   @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                                 String   @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                                Int      @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                              Int      @mkp_connect_drupal_db.UnsignedInt
  revision_id                            Int?     @mkp_connect_drupal_db.UnsignedInt
  language                               String   @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                                  Int      @mkp_connect_drupal_db.UnsignedInt
  field_migrate_example_wine_ratin_value Decimal? @mkp_connect_drupal_db.Decimal(10, 2)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_mixed_gender {
  entity_type              String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                   String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                  Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id              Int?   @mkp_connect_drupal_db.UnsignedInt
  language                 String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                    Int    @mkp_connect_drupal_db.UnsignedInt
  field_mixed_gender_value Int?

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_mixed_gender_value], map: "field_mixed_gender_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_mkp_area {
  entity_type           String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted               Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id             Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id           Int?    @mkp_connect_drupal_db.UnsignedInt
  language              String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                 Int     @mkp_connect_drupal_db.UnsignedInt
  field_mkp_area_value  String? @mkp_connect_drupal_db.VarChar(60)
  field_mkp_area_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_mkp_area_format], map: "field_mkp_area_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_mkp_community {
  entity_type                String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                     String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                    Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                  Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                Int?    @mkp_connect_drupal_db.UnsignedInt
  language                   String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                      Int     @mkp_connect_drupal_db.UnsignedInt
  field_mkp_community_value  String? @mkp_connect_drupal_db.VarChar(60)
  field_mkp_community_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_mkp_community_format], map: "field_mkp_community_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_mkp_region {
  entity_type             String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                  String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                 Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id               Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id             Int?    @mkp_connect_drupal_db.UnsignedInt
  language                String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                   Int     @mkp_connect_drupal_db.UnsignedInt
  field_mkp_region_value  String? @mkp_connect_drupal_db.VarChar(60)
  field_mkp_region_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_mkp_region_format], map: "field_mkp_region_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_mkpi {
  entity_type      String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle           String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted          Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id        Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id      Int?   @mkp_connect_drupal_db.UnsignedInt
  language         String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta            Int    @mkp_connect_drupal_db.UnsignedInt
  field_mkpi_value Int?

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_mkpi_value], map: "field_mkpi_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_mkpi_build {
  entity_type             String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                  String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                 Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id               Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id             Int?    @mkp_connect_drupal_db.UnsignedInt
  language                String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                   Int     @mkp_connect_drupal_db.UnsignedInt
  field_mkpi_build_value  String? @mkp_connect_drupal_db.VarChar(2)
  field_mkpi_build_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_mkpi_build_format], map: "field_mkpi_build_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_moderation {
  entity_type            String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                 String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id              Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id            Int?    @mkp_connect_drupal_db.UnsignedInt
  language               String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                  Int     @mkp_connect_drupal_db.UnsignedInt
  field_moderation_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_moderation_value], map: "field_moderation_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_mos_fee {
  entity_type         String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle              String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted             Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id           Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id         Int?   @mkp_connect_drupal_db.UnsignedInt
  language            String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta               Int    @mkp_connect_drupal_db.UnsignedInt
  field_mos_fee_value Int?

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_multipay {
  entity_type           String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted               Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id             Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id           Int?    @mkp_connect_drupal_db.UnsignedInt
  language              String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                 Int     @mkp_connect_drupal_db.UnsignedInt
  field_multipay_value  String? @mkp_connect_drupal_db.VarChar(2)
  field_multipay_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_multipay_format], map: "field_multipay_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_nb_civi_event {
  entity_type                String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                     String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                    Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                  Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                Int?    @mkp_connect_drupal_db.UnsignedInt
  language                   String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                      Int     @mkp_connect_drupal_db.UnsignedInt
  field_nb_civi_event_value  String? @mkp_connect_drupal_db.VarChar(255)
  field_nb_civi_event_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_nb_civi_event_format], map: "field_nb_civi_event_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_nb_info_link {
  entity_type              String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                   String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                  Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id              Int?   @mkp_connect_drupal_db.UnsignedInt
  language                 String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                    Int    @mkp_connect_drupal_db.UnsignedInt
  field_nb_info_link_value Int?

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_news_image {
  entity_type             String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                  String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                 Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id               Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id             Int?    @mkp_connect_drupal_db.UnsignedInt
  language                String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                   Int     @mkp_connect_drupal_db.UnsignedInt
  field_news_image_fid    Int?    @mkp_connect_drupal_db.UnsignedInt
  field_news_image_alt    String? @mkp_connect_drupal_db.VarChar(512)
  field_news_image_title  String? @mkp_connect_drupal_db.VarChar(1024)
  field_news_image_width  Int?    @mkp_connect_drupal_db.UnsignedInt
  field_news_image_height Int?    @mkp_connect_drupal_db.UnsignedInt

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_news_image_fid], map: "field_news_image_fid")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_nudity {
  entity_type        String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle             String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted            Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id          Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id        Int?    @mkp_connect_drupal_db.UnsignedInt
  language           String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta              Int     @mkp_connect_drupal_db.UnsignedInt
  field_nudity_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_nudity_value], map: "field_nudity_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_number_of_members {
  entity_type                   String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                        String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                       Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                     Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id                   Int?   @mkp_connect_drupal_db.UnsignedInt
  language                      String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                         Int    @mkp_connect_drupal_db.UnsignedInt
  field_number_of_members_value Int?

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_number_of_users {
  entity_type                 String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                      String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                     Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                   Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id                 Int?   @mkp_connect_drupal_db.UnsignedInt
  language                    String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                       Int    @mkp_connect_drupal_db.UnsignedInt
  field_number_of_users_value Int?

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_nwta_end_date {
  entity_type               String    @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                    String    @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                   Int       @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                 Int       @mkp_connect_drupal_db.UnsignedInt
  revision_id               Int?      @mkp_connect_drupal_db.UnsignedInt
  language                  String    @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                     Int       @mkp_connect_drupal_db.UnsignedInt
  field_nwta_end_date_value DateTime? @mkp_connect_drupal_db.DateTime(0)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_nwta_end_date_value], map: "field_nwta_end_date_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_open_group {
  entity_type            String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                 String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id              Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id            Int?    @mkp_connect_drupal_db.UnsignedInt
  language               String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                  Int     @mkp_connect_drupal_db.UnsignedInt
  field_open_group_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_open_group_value], map: "field_open_group_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_order {
  entity_type       String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle            String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted           Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id         Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id       Int?   @mkp_connect_drupal_db.UnsignedInt
  language          String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta             Int    @mkp_connect_drupal_db.UnsignedInt
  field_order_value Int?

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_owner {
  entity_type       String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle            String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted           Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id         Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id       Int?    @mkp_connect_drupal_db.UnsignedInt
  language          String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta             Int     @mkp_connect_drupal_db.UnsignedInt
  field_owner_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_owner_value], map: "field_owner_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_owner_1 {
  entity_type         String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle              String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted             Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id           Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id         Int?    @mkp_connect_drupal_db.UnsignedInt
  language            String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta               Int     @mkp_connect_drupal_db.UnsignedInt
  field_owner_1_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_owner_1_value], map: "field_owner_1_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_owner_nid {
  entity_type           String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted               Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id             Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id           Int?   @mkp_connect_drupal_db.UnsignedInt
  language              String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                 Int    @mkp_connect_drupal_db.UnsignedInt
  field_owner_nid_value Int?

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_owner_nid_copy1 {
  entity_type           String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted               Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id             Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id           Int?   @mkp_connect_drupal_db.UnsignedInt
  language              String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                 Int    @mkp_connect_drupal_db.UnsignedInt
  field_owner_nid_value Int?

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_part_fee {
  entity_type          String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle               String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted              Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id            Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id          Int?   @mkp_connect_drupal_db.UnsignedInt
  language             String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                Int    @mkp_connect_drupal_db.UnsignedInt
  field_part_fee_value Int?

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_part_reg_end_date {
  entity_type                   String    @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                        String    @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                       Int       @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                     Int       @mkp_connect_drupal_db.UnsignedInt
  revision_id                   Int?      @mkp_connect_drupal_db.UnsignedInt
  language                      String    @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                         Int       @mkp_connect_drupal_db.UnsignedInt
  field_part_reg_end_date_value DateTime? @mkp_connect_drupal_db.DateTime(0)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_part_reg_end_date_value], map: "field_part_reg_end_date_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_part_reg_start_date {
  entity_type                     String    @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                          String    @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                         Int       @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                       Int       @mkp_connect_drupal_db.UnsignedInt
  revision_id                     Int?      @mkp_connect_drupal_db.UnsignedInt
  language                        String    @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                           Int       @mkp_connect_drupal_db.UnsignedInt
  field_part_reg_start_date_value DateTime? @mkp_connect_drupal_db.DateTime(0)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_part_reg_start_date_value], map: "field_part_reg_start_date_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_part_registration_link {
  entity_type                         String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                              String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                             Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                           Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                         Int?    @mkp_connect_drupal_db.UnsignedInt
  language                            String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                               Int     @mkp_connect_drupal_db.UnsignedInt
  field_part_registration_link_value  String? @mkp_connect_drupal_db.VarChar(120)
  field_part_registration_link_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_part_registration_link_format], map: "field_part_registration_link_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_part_start_date {
  entity_type                 String    @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                      String    @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                     Int       @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                   Int       @mkp_connect_drupal_db.UnsignedInt
  revision_id                 Int?      @mkp_connect_drupal_db.UnsignedInt
  language                    String    @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                       Int       @mkp_connect_drupal_db.UnsignedInt
  field_part_start_date_value DateTime? @mkp_connect_drupal_db.DateTime(0)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_part_start_date_value], map: "field_part_start_date_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_participant_registrar {
  entity_type                        String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                             String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                            Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                          Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                        Int?    @mkp_connect_drupal_db.UnsignedInt
  language                           String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                              Int     @mkp_connect_drupal_db.UnsignedInt
  field_participant_registrar_value  String? @mkp_connect_drupal_db.VarChar(60)
  field_participant_registrar_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_participant_registrar_format], map: "field_participant_registrar_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_participant_registrar_emai {
  entity_type                             String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                                  String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                                 Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                               Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                             Int?    @mkp_connect_drupal_db.UnsignedInt
  language                                String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                                   Int     @mkp_connect_drupal_db.UnsignedInt
  field_participant_registrar_emai_value  String? @mkp_connect_drupal_db.VarChar(60)
  field_participant_registrar_emai_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_participant_registrar_emai_format], map: "field_participant_registrar_emai_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_participant_registrar_phon {
  entity_type                             String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                                  String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                                 Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                               Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                             Int?    @mkp_connect_drupal_db.UnsignedInt
  language                                String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                                   Int     @mkp_connect_drupal_db.UnsignedInt
  field_participant_registrar_phon_value  String? @mkp_connect_drupal_db.VarChar(40)
  field_participant_registrar_phon_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_participant_registrar_phon_format], map: "field_participant_registrar_phon_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_percentage_of_members {
  entity_type                       String   @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                            String   @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                           Int      @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                         Int      @mkp_connect_drupal_db.UnsignedInt
  revision_id                       Int?     @mkp_connect_drupal_db.UnsignedInt
  language                          String   @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                             Int      @mkp_connect_drupal_db.UnsignedInt
  field_percentage_of_members_value Decimal? @mkp_connect_drupal_db.Decimal(10, 2)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_postal_code {
  entity_type              String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                   String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                  Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id              Int?    @mkp_connect_drupal_db.UnsignedInt
  language                 String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                    Int     @mkp_connect_drupal_db.UnsignedInt
  field_postal_code_value  String? @mkp_connect_drupal_db.VarChar(20)
  field_postal_code_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_postal_code_format], map: "field_postal_code_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_prerequisite {
  entity_type              String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                   String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                  Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id              Int?    @mkp_connect_drupal_db.UnsignedInt
  language                 String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                    Int     @mkp_connect_drupal_db.UnsignedInt
  field_prerequisite_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_prerequisite_value], map: "field_prerequisite_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_price {
  entity_type       String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle            String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted           Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id         Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id       Int?   @mkp_connect_drupal_db.UnsignedInt
  language          String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta             Int    @mkp_connect_drupal_db.UnsignedInt
  field_price_value Int?

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_private {
  entity_type         String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle              String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted             Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id           Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id         Int?   @mkp_connect_drupal_db.UnsignedInt
  language            String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta               Int    @mkp_connect_drupal_db.UnsignedInt
  field_private_value Int?

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_private_value], map: "field_private_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_private_group {
  entity_type               String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                    String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                   Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                 Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id               Int?   @mkp_connect_drupal_db.UnsignedInt
  language                  String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                     Int    @mkp_connect_drupal_db.UnsignedInt
  field_private_group_value Int?

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_private_group_value], map: "field_private_group_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_process_type {
  entity_type              String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                   String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                  Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id              Int?    @mkp_connect_drupal_db.UnsignedInt
  language                 String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                    Int     @mkp_connect_drupal_db.UnsignedInt
  field_process_type_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_process_type_value], map: "field_process_type_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_public_contact {
  entity_type                String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                     String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                    Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                  Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id                Int?   @mkp_connect_drupal_db.UnsignedInt
  language                   String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                      Int    @mkp_connect_drupal_db.UnsignedInt
  field_public_contact_value Int?

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_public_contact_value], map: "field_public_contact_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_public_display {
  entity_type                String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                     String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                    Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                  Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id                Int?   @mkp_connect_drupal_db.UnsignedInt
  language                   String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                      Int    @mkp_connect_drupal_db.UnsignedInt
  field_public_display_value Int?

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_public_display_value], map: "field_public_display_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_readiness {
  entity_type            String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                 String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id              Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id            Int?    @mkp_connect_drupal_db.UnsignedInt
  language               String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                  Int     @mkp_connect_drupal_db.UnsignedInt
  field_readiness_value  String? @mkp_connect_drupal_db.VarChar(2)
  field_readiness_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_readiness_format], map: "field_readiness_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_reg_login {
  entity_type            String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                 String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id              Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id            Int?    @mkp_connect_drupal_db.UnsignedInt
  language               String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                  Int     @mkp_connect_drupal_db.UnsignedInt
  field_reg_login_value  String? @mkp_connect_drupal_db.VarChar(2)
  field_reg_login_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_reg_login_format], map: "field_reg_login_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_region {
  entity_type        String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle             String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted            Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id          Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id        Int?    @mkp_connect_drupal_db.UnsignedInt
  language           String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta              Int     @mkp_connect_drupal_db.UnsignedInt
  field_region_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_region_value], map: "field_region_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_region_link {
  entity_type              String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                   String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                  Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id              Int?    @mkp_connect_drupal_db.UnsignedInt
  language                 String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                    Int     @mkp_connect_drupal_db.UnsignedInt
  field_region_link_value  String? @mkp_connect_drupal_db.VarChar(60)
  field_region_link_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_region_link_format], map: "field_region_link_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_region_nid {
  entity_type            String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                 String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id              Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id            Int?   @mkp_connect_drupal_db.UnsignedInt
  language               String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                  Int    @mkp_connect_drupal_db.UnsignedInt
  field_region_nid_value Int?

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_registration_end_date {
  entity_type                       String    @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                            String    @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                           Int       @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                         Int       @mkp_connect_drupal_db.UnsignedInt
  revision_id                       Int?      @mkp_connect_drupal_db.UnsignedInt
  language                          String    @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                             Int       @mkp_connect_drupal_db.UnsignedInt
  field_registration_end_date_value DateTime? @mkp_connect_drupal_db.DateTime(0)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_registration_end_date_value], map: "field_registration_end_date_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_registration_link {
  entity_type                    String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                         String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                        Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                      Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                    Int?    @mkp_connect_drupal_db.UnsignedInt
  language                       String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                          Int     @mkp_connect_drupal_db.UnsignedInt
  field_registration_link_value  String? @mkp_connect_drupal_db.VarChar(120)
  field_registration_link_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_registration_link_format], map: "field_registration_link_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_registration_start_date {
  entity_type                         String    @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                              String    @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                             Int       @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                           Int       @mkp_connect_drupal_db.UnsignedInt
  revision_id                         Int?      @mkp_connect_drupal_db.UnsignedInt
  language                            String    @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                               Int       @mkp_connect_drupal_db.UnsignedInt
  field_registration_start_date_value DateTime? @mkp_connect_drupal_db.DateTime(0)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_registration_start_date_value], map: "field_registration_start_date_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_related_event_1 {
  entity_type                 String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                      String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                     Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                   Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id                 Int?   @mkp_connect_drupal_db.UnsignedInt
  language                    String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                       Int    @mkp_connect_drupal_db.UnsignedInt
  field_related_event_1_value Int?

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_related_event_2 {
  entity_type                 String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                      String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                     Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                   Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id                 Int?   @mkp_connect_drupal_db.UnsignedInt
  language                    String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                       Int    @mkp_connect_drupal_db.UnsignedInt
  field_related_event_2_value Int?

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_reply_to {
  entity_type          String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle               String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted              Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id            Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id          Int?    @mkp_connect_drupal_db.UnsignedInt
  language             String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                Int     @mkp_connect_drupal_db.UnsignedInt
  field_reply_to_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_reply_to_value], map: "field_reply_to_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_responsible {
  entity_type              String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                   String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                  Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id              Int?    @mkp_connect_drupal_db.UnsignedInt
  language                 String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                    Int     @mkp_connect_drupal_db.UnsignedInt
  field_responsible_value  String? @mkp_connect_drupal_db.VarChar(120)
  field_responsible_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_responsible_format], map: "field_responsible_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_role {
  entity_type       String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle            String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted           Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id         Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id       Int?    @mkp_connect_drupal_db.UnsignedInt
  language          String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta             Int     @mkp_connect_drupal_db.UnsignedInt
  field_role_value  String? @mkp_connect_drupal_db.VarChar(60)
  field_role_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_role_format], map: "field_role_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_specialized {
  entity_type              String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                   String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                  Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id              Int?    @mkp_connect_drupal_db.UnsignedInt
  language                 String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                    Int     @mkp_connect_drupal_db.UnsignedInt
  field_specialized_value  String? @mkp_connect_drupal_db.VarChar(60)
  field_specialized_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_specialized_format], map: "field_specialized_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_sponsored {
  entity_type           String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted               Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id             Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id           Int?    @mkp_connect_drupal_db.UnsignedInt
  language              String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                 Int     @mkp_connect_drupal_db.UnsignedInt
  field_sponsored_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_sponsored_value], map: "field_sponsored_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_sponsored_entity {
  entity_type                  String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                       String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                      Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                    Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                  Int?    @mkp_connect_drupal_db.UnsignedInt
  language                     String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                        Int     @mkp_connect_drupal_db.UnsignedInt
  field_sponsored_entity_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_sponsored_entity_value], map: "field_sponsored_entity_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_sstype {
  entity_type        String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle             String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted            Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id          Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id        Int?    @mkp_connect_drupal_db.UnsignedInt
  language           String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta              Int     @mkp_connect_drupal_db.UnsignedInt
  field_sstype_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_sstype_value], map: "field_sstype_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_staff_coordinator {
  entity_type                    String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                         String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                        Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                      Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                    Int?    @mkp_connect_drupal_db.UnsignedInt
  language                       String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                          Int     @mkp_connect_drupal_db.UnsignedInt
  field_staff_coordinator_value  String? @mkp_connect_drupal_db.VarChar(60)
  field_staff_coordinator_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_staff_coordinator_format], map: "field_staff_coordinator_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_staff_coordinator_email {
  entity_type                          String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                               String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                              Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                            Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                          Int?    @mkp_connect_drupal_db.UnsignedInt
  language                             String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                                Int     @mkp_connect_drupal_db.UnsignedInt
  field_staff_coordinator_email_value  String? @mkp_connect_drupal_db.VarChar(60)
  field_staff_coordinator_email_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_staff_coordinator_email_format], map: "field_staff_coordinator_email_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_staff_coordinator_phone {
  entity_type                          String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                               String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                              Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                            Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                          Int?    @mkp_connect_drupal_db.UnsignedInt
  language                             String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                                Int     @mkp_connect_drupal_db.UnsignedInt
  field_staff_coordinator_phone_value  String? @mkp_connect_drupal_db.VarChar(40)
  field_staff_coordinator_phone_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_staff_coordinator_phone_format], map: "field_staff_coordinator_phone_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_staff_fee {
  entity_type           String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted               Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id             Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id           Int?   @mkp_connect_drupal_db.UnsignedInt
  language              String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                 Int    @mkp_connect_drupal_db.UnsignedInt
  field_staff_fee_value Int?

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_staff_group_nid {
  entity_type                 String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                      String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                     Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                   Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id                 Int?   @mkp_connect_drupal_db.UnsignedInt
  language                    String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                       Int    @mkp_connect_drupal_db.UnsignedInt
  field_staff_group_nid_value Int?

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_staff_reg_end_date {
  entity_type                    String    @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                         String    @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                        Int       @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                      Int       @mkp_connect_drupal_db.UnsignedInt
  revision_id                    Int?      @mkp_connect_drupal_db.UnsignedInt
  language                       String    @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                          Int       @mkp_connect_drupal_db.UnsignedInt
  field_staff_reg_end_date_value DateTime? @mkp_connect_drupal_db.DateTime(0)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_staff_reg_end_date_value], map: "field_staff_reg_end_date_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_staff_reg_start_date {
  entity_type                      String    @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                           String    @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                          Int       @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                        Int       @mkp_connect_drupal_db.UnsignedInt
  revision_id                      Int?      @mkp_connect_drupal_db.UnsignedInt
  language                         String    @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                            Int       @mkp_connect_drupal_db.UnsignedInt
  field_staff_reg_start_date_value DateTime? @mkp_connect_drupal_db.DateTime(0)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_staff_reg_start_date_value], map: "field_staff_reg_start_date_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_staff_registration_link {
  entity_type                          String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                               String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                              Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                            Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                          Int?    @mkp_connect_drupal_db.UnsignedInt
  language                             String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                                Int     @mkp_connect_drupal_db.UnsignedInt
  field_staff_registration_link_value  String? @mkp_connect_drupal_db.VarChar(255)
  field_staff_registration_link_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_staff_registration_link_format], map: "field_staff_registration_link_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_staff_start_date {
  entity_type                  String    @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                       String    @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                      Int       @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                    Int       @mkp_connect_drupal_db.UnsignedInt
  revision_id                  Int?      @mkp_connect_drupal_db.UnsignedInt
  language                     String    @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                        Int       @mkp_connect_drupal_db.UnsignedInt
  field_staff_start_date_value DateTime? @mkp_connect_drupal_db.DateTime(0)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_staff_start_date_value], map: "field_staff_start_date_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_start_date {
  entity_type            String    @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                 String    @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                Int       @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id              Int       @mkp_connect_drupal_db.UnsignedInt
  revision_id            Int?      @mkp_connect_drupal_db.UnsignedInt
  language               String    @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                  Int       @mkp_connect_drupal_db.UnsignedInt
  field_start_date_value DateTime? @mkp_connect_drupal_db.DateTime(0)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_start_date_value], map: "field_start_date_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_state_province {
  entity_type                 String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                      String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                     Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                   Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                 Int?    @mkp_connect_drupal_db.UnsignedInt
  language                    String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                       Int     @mkp_connect_drupal_db.UnsignedInt
  field_state_province_value  String? @mkp_connect_drupal_db.VarChar(80)
  field_state_province_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_state_province_format], map: "field_state_province_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_tags {
  entity_type    String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle         String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted        Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id      Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id    Int?   @mkp_connect_drupal_db.UnsignedInt
  language       String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta          Int    @mkp_connect_drupal_db.UnsignedInt
  field_tags_tid Int?   @mkp_connect_drupal_db.UnsignedInt

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_tags_tid], map: "field_tags_tid")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_talk_group {
  entity_type            String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                 String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id              Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id            Int?   @mkp_connect_drupal_db.UnsignedInt
  language               String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                  Int    @mkp_connect_drupal_db.UnsignedInt
  field_talk_group_value Int?

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_talk_group_old {
  entity_type            String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                 String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id              Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id            Int?   @mkp_connect_drupal_db.UnsignedInt
  language               String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                  Int    @mkp_connect_drupal_db.UnsignedInt
  field_talk_group_value Int?

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_term_end_date {
  entity_type               String    @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                    String    @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                   Int       @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                 Int       @mkp_connect_drupal_db.UnsignedInt
  revision_id               Int?      @mkp_connect_drupal_db.UnsignedInt
  language                  String    @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                     Int       @mkp_connect_drupal_db.UnsignedInt
  field_term_end_date_value DateTime? @mkp_connect_drupal_db.DateTime(0)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_term_end_date_value], map: "field_term_end_date_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_term_start_date {
  entity_type                 String    @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                      String    @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                     Int       @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                   Int       @mkp_connect_drupal_db.UnsignedInt
  revision_id                 Int?      @mkp_connect_drupal_db.UnsignedInt
  language                    String    @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                       Int       @mkp_connect_drupal_db.UnsignedInt
  field_term_start_date_value DateTime? @mkp_connect_drupal_db.DateTime(0)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_term_start_date_value], map: "field_term_start_date_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_tinyurl {
  entity_type          String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle               String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted              Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id            Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id          Int?    @mkp_connect_drupal_db.UnsignedInt
  language             String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                Int     @mkp_connect_drupal_db.UnsignedInt
  field_tinyurl_value  String? @mkp_connect_drupal_db.VarChar(100)
  field_tinyurl_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_tinyurl_format], map: "field_tinyurl_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_topic {
  entity_type       String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle            String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted           Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id         Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id       Int?    @mkp_connect_drupal_db.UnsignedInt
  language          String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta             Int     @mkp_connect_drupal_db.UnsignedInt
  field_topic_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_topic_value], map: "field_topic_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_training_type {
  entity_type                String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                     String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                    Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                  Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                Int?    @mkp_connect_drupal_db.UnsignedInt
  language                   String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                      Int     @mkp_connect_drupal_db.UnsignedInt
  field_training_type_value  String? @mkp_connect_drupal_db.VarChar(40)
  field_training_type_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_training_type_format], map: "field_training_type_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_type {
  entity_type      String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle           String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted          Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id        Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id      Int?    @mkp_connect_drupal_db.UnsignedInt
  language         String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta            Int     @mkp_connect_drupal_db.UnsignedInt
  field_type_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_type_value], map: "field_type_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_upload_csv {
  entity_type                  String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                       String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                      Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                    Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                  Int?    @mkp_connect_drupal_db.UnsignedInt
  language                     String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                        Int     @mkp_connect_drupal_db.UnsignedInt
  field_upload_csv_fid         Int?    @mkp_connect_drupal_db.UnsignedInt
  field_upload_csv_display     Int     @default(1) @mkp_connect_drupal_db.UnsignedTinyInt
  field_upload_csv_description String? @mkp_connect_drupal_db.Text

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_upload_csv_fid], map: "field_upload_csv_fid")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_vacc_rule {
  entity_type            String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                 String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id              Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id            Int?    @mkp_connect_drupal_db.UnsignedInt
  language               String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                  Int     @mkp_connect_drupal_db.UnsignedInt
  field_vacc_rule_value  String? @mkp_connect_drupal_db.VarChar(10)
  field_vacc_rule_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_vacc_rule_format], map: "field_vacc_rule_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_venue {
  entity_type       String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle            String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted           Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id         Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id       Int?    @mkp_connect_drupal_db.UnsignedInt
  language          String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta             Int     @mkp_connect_drupal_db.UnsignedInt
  field_venue_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_venue_value], map: "field_venue_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_vgroup_abbreviation {
  entity_type                      String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                           String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                          Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                        Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                      Int?    @mkp_connect_drupal_db.UnsignedInt
  language                         String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                            Int     @mkp_connect_drupal_db.UnsignedInt
  field_vgroup_abbreviation_value  String? @mkp_connect_drupal_db.VarChar(20)
  field_vgroup_abbreviation_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_vgroup_abbreviation_format], map: "field_vgroup_abbreviation_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_wait_list {
  entity_type            String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                 String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id              Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id            Int?    @mkp_connect_drupal_db.UnsignedInt
  language               String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                  Int     @mkp_connect_drupal_db.UnsignedInt
  field_wait_list_value  String? @mkp_connect_drupal_db.VarChar(2)
  field_wait_list_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_wait_list_format], map: "field_wait_list_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_waitlist {
  entity_type          String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle               String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted              Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id            Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id          Int?   @mkp_connect_drupal_db.UnsignedInt
  language             String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                Int    @mkp_connect_drupal_db.UnsignedInt
  field_waitlist_value Int?

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_waitlist_value], map: "field_waitlist_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_what {
  entity_type      String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle           String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted          Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id        Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id      Int?    @mkp_connect_drupal_db.UnsignedInt
  language         String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta            Int     @mkp_connect_drupal_db.UnsignedInt
  field_what_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_what_value], map: "field_what_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_who {
  entity_type     String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle          String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted         Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id       Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id     Int?    @mkp_connect_drupal_db.UnsignedInt
  language        String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta           Int     @mkp_connect_drupal_db.UnsignedInt
  field_who_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_who_value], map: "field_who_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_wp_link {
  entity_type          String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle               String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted              Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id            Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id          Int?    @mkp_connect_drupal_db.UnsignedInt
  language             String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                Int     @mkp_connect_drupal_db.UnsignedInt
  field_wp_link_value  String? @mkp_connect_drupal_db.VarChar(80)
  field_wp_link_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_wp_link_format], map: "field_wp_link_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_field_zoom {
  entity_type      String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle           String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted          Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id        Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id      Int?   @mkp_connect_drupal_db.UnsignedInt
  language         String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta            Int    @mkp_connect_drupal_db.UnsignedInt
  field_zoom_value Int?

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_group_access {
  entity_type        String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle             String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted            Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id          Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id        Int?   @mkp_connect_drupal_db.UnsignedInt
  language           String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta              Int    @mkp_connect_drupal_db.UnsignedInt
  group_access_value Int?

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([group_access_value], map: "group_access_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_group_group {
  entity_type       String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle            String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted           Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id         Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id       Int?   @mkp_connect_drupal_db.UnsignedInt
  language          String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta             Int    @mkp_connect_drupal_db.UnsignedInt
  group_group_value Int?

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([group_group_value], map: "group_group_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_migrate_example_beer_styles {
  entity_type                     String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                          String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                         Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                       Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id                     Int?   @mkp_connect_drupal_db.UnsignedInt
  language                        String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                           Int    @mkp_connect_drupal_db.UnsignedInt
  migrate_example_beer_styles_tid Int?   @mkp_connect_drupal_db.UnsignedInt

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([migrate_example_beer_styles_tid], map: "migrate_example_beer_styles_tid")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_migrate_example_wine_best_with {
  entity_type                        String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                             String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                            Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                          Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id                        Int?   @mkp_connect_drupal_db.UnsignedInt
  language                           String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                              Int    @mkp_connect_drupal_db.UnsignedInt
  migrate_example_wine_best_with_tid Int?   @mkp_connect_drupal_db.UnsignedInt

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([migrate_example_wine_best_with_tid], map: "migrate_example_wine_best_with_tid")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_migrate_example_wine_regions {
  entity_type                      String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                           String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                          Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                        Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id                      Int?   @mkp_connect_drupal_db.UnsignedInt
  language                         String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                            Int    @mkp_connect_drupal_db.UnsignedInt
  migrate_example_wine_regions_tid Int?   @mkp_connect_drupal_db.UnsignedInt

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([migrate_example_wine_regions_tid], map: "migrate_example_wine_regions_tid")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_migrate_example_wine_varieties {
  entity_type                        String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                             String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                            Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                          Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id                        Int?   @mkp_connect_drupal_db.UnsignedInt
  language                           String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                              Int    @mkp_connect_drupal_db.UnsignedInt
  migrate_example_wine_varieties_tid Int?   @mkp_connect_drupal_db.UnsignedInt

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([migrate_example_wine_varieties_tid], map: "migrate_example_wine_varieties_tid")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_og_membership_request {
  entity_type                  String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                       String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                      Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                    Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                  Int?    @mkp_connect_drupal_db.UnsignedInt
  language                     String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                        Int     @mkp_connect_drupal_db.UnsignedInt
  og_membership_request_value  String? @mkp_connect_drupal_db.LongText
  og_membership_request_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([og_membership_request_format], map: "og_membership_request_format")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_og_roles_permissions {
  entity_type                String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                     String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                    Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                  Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id                Int?   @mkp_connect_drupal_db.UnsignedInt
  language                   String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                      Int    @mkp_connect_drupal_db.UnsignedInt
  og_roles_permissions_value Int?

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([og_roles_permissions_value], map: "og_roles_permissions_value")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_data_og_user_node {
  entity_type            String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                 String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id              Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id            Int?   @mkp_connect_drupal_db.UnsignedInt
  language               String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                  Int    @mkp_connect_drupal_db.UnsignedInt
  og_user_node_target_id Int    @mkp_connect_drupal_db.UnsignedInt

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([og_user_node_target_id], map: "og_user_node_target_id")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_deleted_data_140 {
  entity_type        String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle             String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted            Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id          Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id        Int?   @mkp_connect_drupal_db.UnsignedInt
  language           String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta              Int    @mkp_connect_drupal_db.UnsignedInt
  field_taxonomy_tid Int?   @mkp_connect_drupal_db.UnsignedInt

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_taxonomy_tid], map: "field_taxonomy_tid")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_deleted_data_93 {
  entity_type              String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                   String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                  Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id              Int?    @mkp_connect_drupal_db.UnsignedInt
  language                 String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                    Int     @mkp_connect_drupal_db.UnsignedInt
  field_mkpcommunity_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_mkpcommunity_value], map: "field_mkpcommunity_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_deleted_revision_140 {
  entity_type        String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle             String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted            Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id          Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id        Int    @mkp_connect_drupal_db.UnsignedInt
  language           String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta              Int    @mkp_connect_drupal_db.UnsignedInt
  field_taxonomy_tid Int?   @mkp_connect_drupal_db.UnsignedInt

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_taxonomy_tid], map: "field_taxonomy_tid")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_deleted_revision_93 {
  entity_type              String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                   String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                  Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id              Int     @mkp_connect_drupal_db.UnsignedInt
  language                 String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                    Int     @mkp_connect_drupal_db.UnsignedInt
  field_mkpcommunity_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_mkpcommunity_value], map: "field_mkpcommunity_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_body {
  entity_type  String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle       String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted      Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id    Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id  Int     @mkp_connect_drupal_db.UnsignedInt
  language     String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta        Int     @mkp_connect_drupal_db.UnsignedInt
  body_value   String? @mkp_connect_drupal_db.LongText
  body_summary String? @mkp_connect_drupal_db.LongText
  body_format  String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([body_format], map: "body_format")
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_comment_body {
  entity_type         String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle              String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted             Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id           Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id         Int     @mkp_connect_drupal_db.UnsignedInt
  language            String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta               Int     @mkp_connect_drupal_db.UnsignedInt
  comment_body_value  String? @mkp_connect_drupal_db.LongText
  comment_body_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([comment_body_format], map: "comment_body_format")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_about_entity {
  entity_type               String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                    String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                   Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                 Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id               Int     @mkp_connect_drupal_db.UnsignedInt
  language                  String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                     Int     @mkp_connect_drupal_db.UnsignedInt
  field_about_entity_value  String? @mkp_connect_drupal_db.LongText
  field_about_entity_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_about_entity_format], map: "field_about_entity_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_address {
  entity_type          String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle               String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted              Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id            Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id          Int     @mkp_connect_drupal_db.UnsignedInt
  language             String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                Int     @mkp_connect_drupal_db.UnsignedInt
  field_address_value  String? @mkp_connect_drupal_db.VarChar(120)
  field_address_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_address_format], map: "field_address_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_admin {
  entity_type        String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle             String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted            Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id          Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id        Int     @mkp_connect_drupal_db.UnsignedInt
  language           String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta              Int     @mkp_connect_drupal_db.UnsignedInt
  field_admin_value  String? @mkp_connect_drupal_db.VarChar(60)
  field_admin_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_admin_format], map: "field_admin_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_admin_email {
  entity_type              String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                   String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                  Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id              Int     @mkp_connect_drupal_db.UnsignedInt
  language                 String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                    Int     @mkp_connect_drupal_db.UnsignedInt
  field_admin_email_value  String? @mkp_connect_drupal_db.VarChar(60)
  field_admin_email_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_admin_email_format], map: "field_admin_email_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_admin_email1 {
  entity_type               String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                    String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                   Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                 Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id               Int     @mkp_connect_drupal_db.UnsignedInt
  language                  String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                     Int     @mkp_connect_drupal_db.UnsignedInt
  field_admin_email1_value  String? @mkp_connect_drupal_db.VarChar(60)
  field_admin_email1_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_admin_email1_format], map: "field_admin_email1_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_admin_phone {
  entity_type              String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                   String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                  Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id              Int     @mkp_connect_drupal_db.UnsignedInt
  language                 String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                    Int     @mkp_connect_drupal_db.UnsignedInt
  field_admin_phone_value  String? @mkp_connect_drupal_db.VarChar(40)
  field_admin_phone_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_admin_phone_format], map: "field_admin_phone_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_admin_uid {
  entity_type           String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted               Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id             Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id           Int    @mkp_connect_drupal_db.UnsignedInt
  language              String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                 Int    @mkp_connect_drupal_db.UnsignedInt
  field_admin_uid_value Int?

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_announce_group {
  entity_type                String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                     String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                    Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                  Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id                Int    @mkp_connect_drupal_db.UnsignedInt
  language                   String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                      Int    @mkp_connect_drupal_db.UnsignedInt
  field_announce_group_value Int?

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_approved {
  entity_type          String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle               String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted              Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id            Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id          Int    @mkp_connect_drupal_db.UnsignedInt
  language             String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                Int    @mkp_connect_drupal_db.UnsignedInt
  field_approved_value Int?

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_approved_value], map: "field_approved_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_approved_by {
  entity_type              String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                   String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                  Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id              Int     @mkp_connect_drupal_db.UnsignedInt
  language                 String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                    Int     @mkp_connect_drupal_db.UnsignedInt
  field_approved_by_value  String? @mkp_connect_drupal_db.VarChar(40)
  field_approved_by_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_approved_by_format], map: "field_approved_by_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_area {
  entity_type      String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle           String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted          Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id        Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id      Int     @mkp_connect_drupal_db.UnsignedInt
  language         String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta            Int     @mkp_connect_drupal_db.UnsignedInt
  field_area_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_area_value], map: "field_area_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_area_link {
  entity_type            String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                 String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id              Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id            Int     @mkp_connect_drupal_db.UnsignedInt
  language               String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                  Int     @mkp_connect_drupal_db.UnsignedInt
  field_area_link_value  String? @mkp_connect_drupal_db.VarChar(60)
  field_area_link_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_area_link_format], map: "field_area_link_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_area_nid {
  entity_type          String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle               String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted              Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id            Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id          Int    @mkp_connect_drupal_db.UnsignedInt
  language             String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                Int    @mkp_connect_drupal_db.UnsignedInt
  field_area_nid_value Int?

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_areacommunity_entity {
  entity_type                       String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                            String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                           Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                         Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                       Int     @mkp_connect_drupal_db.UnsignedInt
  language                          String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                             Int     @mkp_connect_drupal_db.UnsignedInt
  field_areacommunity_entity_value  String? @mkp_connect_drupal_db.VarChar(80)
  field_areacommunity_entity_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_areacommunity_entity_format], map: "field_areacommunity_entity_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_areatype {
  entity_type        String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle             String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted            Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id          Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id        Int    @mkp_connect_drupal_db.UnsignedInt
  language           String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta              Int    @mkp_connect_drupal_db.UnsignedInt
  field_areatype_tid Int?   @mkp_connect_drupal_db.UnsignedInt

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_areatype_tid], map: "field_areatype_tid")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_audience {
  entity_type          String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle               String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted              Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id            Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id          Int     @mkp_connect_drupal_db.UnsignedInt
  language             String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                Int     @mkp_connect_drupal_db.UnsignedInt
  field_audience_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_audience_value], map: "field_audience_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_broadcast {
  entity_type           String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted               Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id             Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id           Int    @mkp_connect_drupal_db.UnsignedInt
  language              String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                 Int    @mkp_connect_drupal_db.UnsignedInt
  field_broadcast_value Int?

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_broadcast_value], map: "field_broadcast_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_build_complete {
  entity_type                String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                     String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                    Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                  Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id                Int    @mkp_connect_drupal_db.UnsignedInt
  language                   String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                      Int    @mkp_connect_drupal_db.UnsignedInt
  field_build_complete_value Int?

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_build_complete_value], map: "field_build_complete_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_caaid {
  entity_type       String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle            String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted           Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id         Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id       Int    @mkp_connect_drupal_db.UnsignedInt
  language          String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta             Int    @mkp_connect_drupal_db.UnsignedInt
  field_caaid_value Int?

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_calendar_widget {
  entity_type                  String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                       String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                      Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                    Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                  Int     @mkp_connect_drupal_db.UnsignedInt
  language                     String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                        Int     @mkp_connect_drupal_db.UnsignedInt
  field_calendar_widget_value  String? @mkp_connect_drupal_db.VarChar(180)
  field_calendar_widget_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_calendar_widget_format], map: "field_calendar_widget_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_category {
  entity_type          String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle               String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted              Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id            Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id          Int     @mkp_connect_drupal_db.UnsignedInt
  language             String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                Int     @mkp_connect_drupal_db.UnsignedInt
  field_category_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_category_value], map: "field_category_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_center {
  entity_type         String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle              String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted             Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id           Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id         Int     @mkp_connect_drupal_db.UnsignedInt
  language            String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta               Int     @mkp_connect_drupal_db.UnsignedInt
  field_center_value  String? @mkp_connect_drupal_db.VarChar(40)
  field_center_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_center_format], map: "field_center_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_chartered {
  entity_type           String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted               Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id             Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id           Int    @mkp_connect_drupal_db.UnsignedInt
  language              String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                 Int    @mkp_connect_drupal_db.UnsignedInt
  field_chartered_value Int?

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_chartered_value], map: "field_chartered_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_city {
  entity_type       String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle            String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted           Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id         Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id       Int     @mkp_connect_drupal_db.UnsignedInt
  language          String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta             Int     @mkp_connect_drupal_db.UnsignedInt
  field_city_value  String? @mkp_connect_drupal_db.VarChar(80)
  field_city_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_city_format], map: "field_city_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_civi_part_event {
  entity_type                 String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                      String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                     Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                   Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id                 Int    @mkp_connect_drupal_db.UnsignedInt
  language                    String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                       Int    @mkp_connect_drupal_db.UnsignedInt
  field_civi_part_event_value Int?

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_civi_staff_event {
  entity_type                  String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                       String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                      Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                    Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id                  Int    @mkp_connect_drupal_db.UnsignedInt
  language                     String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                        Int    @mkp_connect_drupal_db.UnsignedInt
  field_civi_staff_event_value Int?

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_civicrm_event {
  entity_type               String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                    String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                   Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                 Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id               Int    @mkp_connect_drupal_db.UnsignedInt
  language                  String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                     Int    @mkp_connect_drupal_db.UnsignedInt
  field_civicrm_event_value Int?

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_civicrm_event_2 {
  entity_type                 String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                      String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                     Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                   Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id                 Int    @mkp_connect_drupal_db.UnsignedInt
  language                    String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                       Int    @mkp_connect_drupal_db.UnsignedInt
  field_civicrm_event_2_value Int?

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_class {
  entity_type        String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle             String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted            Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id          Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id        Int     @mkp_connect_drupal_db.UnsignedInt
  language           String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta              Int     @mkp_connect_drupal_db.UnsignedInt
  field_class_value  String? @mkp_connect_drupal_db.VarChar(60)
  field_class_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_class_format], map: "field_class_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_committee {
  entity_type           String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted               Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id             Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id           Int     @mkp_connect_drupal_db.UnsignedInt
  language              String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                 Int     @mkp_connect_drupal_db.UnsignedInt
  field_committee_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_committee_value], map: "field_committee_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_committee_nid {
  entity_type               String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                    String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                   Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                 Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id               Int    @mkp_connect_drupal_db.UnsignedInt
  language                  String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                     Int    @mkp_connect_drupal_db.UnsignedInt
  field_committee_nid_value Int?

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_community {
  entity_type           String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted               Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id             Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id           Int     @mkp_connect_drupal_db.UnsignedInt
  language              String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                 Int     @mkp_connect_drupal_db.UnsignedInt
  field_community_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_community_value], map: "field_community_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_community_link {
  entity_type                 String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                      String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                     Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                   Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                 Int     @mkp_connect_drupal_db.UnsignedInt
  language                    String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                       Int     @mkp_connect_drupal_db.UnsignedInt
  field_community_link_value  String? @mkp_connect_drupal_db.VarChar(60)
  field_community_link_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_community_link_format], map: "field_community_link_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_community_nid {
  entity_type               String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                    String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                   Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                 Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id               Int    @mkp_connect_drupal_db.UnsignedInt
  language                  String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                     Int    @mkp_connect_drupal_db.UnsignedInt
  field_community_nid_value Int?

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_contact {
  entity_type       String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle            String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted           Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id         Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id       Int    @mkp_connect_drupal_db.UnsignedInt
  language          String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta             Int    @mkp_connect_drupal_db.UnsignedInt
  field_contact_uid Int?   @mkp_connect_drupal_db.UnsignedInt

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_contact_uid], map: "field_contact_uid")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_contact_email {
  entity_type                String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                     String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                    Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                  Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                Int     @mkp_connect_drupal_db.UnsignedInt
  language                   String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                      Int     @mkp_connect_drupal_db.UnsignedInt
  field_contact_email_value  String? @mkp_connect_drupal_db.VarChar(80)
  field_contact_email_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_contact_email_format], map: "field_contact_email_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_contact_name {
  entity_type               String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                    String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                   Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                 Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id               Int     @mkp_connect_drupal_db.UnsignedInt
  language                  String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                     Int     @mkp_connect_drupal_db.UnsignedInt
  field_contact_name_value  String? @mkp_connect_drupal_db.VarChar(120)
  field_contact_name_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_contact_name_format], map: "field_contact_name_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_contact_phone {
  entity_type                String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                     String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                    Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                  Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                Int     @mkp_connect_drupal_db.UnsignedInt
  language                   String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                      Int     @mkp_connect_drupal_db.UnsignedInt
  field_contact_phone_value  String? @mkp_connect_drupal_db.VarChar(40)
  field_contact_phone_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_contact_phone_format], map: "field_contact_phone_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_content_type {
  entity_type              String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                   String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                  Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id              Int     @mkp_connect_drupal_db.UnsignedInt
  language                 String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                    Int     @mkp_connect_drupal_db.UnsignedInt
  field_content_type_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_content_type_value], map: "field_content_type_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_country {
  entity_type          String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle               String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted              Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id            Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id          Int     @mkp_connect_drupal_db.UnsignedInt
  language             String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                Int     @mkp_connect_drupal_db.UnsignedInt
  field_country_value  String? @mkp_connect_drupal_db.VarChar(60)
  field_country_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_country_format], map: "field_country_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_csz {
  entity_type      String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle           String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted          Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id        Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id      Int     @mkp_connect_drupal_db.UnsignedInt
  language         String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta            Int     @mkp_connect_drupal_db.UnsignedInt
  field_csz_value  String? @mkp_connect_drupal_db.VarChar(255)
  field_csz_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_csz_format], map: "field_csz_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_data_system {
  entity_type             String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                  String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                 Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id               Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id             Int     @mkp_connect_drupal_db.UnsignedInt
  language                String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                   Int     @mkp_connect_drupal_db.UnsignedInt
  field_data_system_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_data_system_value], map: "field_data_system_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_description {
  entity_type              String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                   String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                  Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id              Int     @mkp_connect_drupal_db.UnsignedInt
  language                 String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                    Int     @mkp_connect_drupal_db.UnsignedInt
  field_description_value  String? @mkp_connect_drupal_db.VarChar(100)
  field_description_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_description_format], map: "field_description_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_do_not_list {
  entity_type             String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                  String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                 Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id               Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id             Int     @mkp_connect_drupal_db.UnsignedInt
  language                String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                   Int     @mkp_connect_drupal_db.UnsignedInt
  field_do_not_list_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_do_not_list_value], map: "field_do_not_list_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_docudate {
  entity_type          String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle               String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted              Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id            Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id          Int    @mkp_connect_drupal_db.UnsignedInt
  language             String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                Int    @mkp_connect_drupal_db.UnsignedInt
  field_docudate_value Int?

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_drupal_mos_node {
  entity_type                 String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                      String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                     Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                   Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id                 Int    @mkp_connect_drupal_db.UnsignedInt
  language                    String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                       Int    @mkp_connect_drupal_db.UnsignedInt
  field_drupal_mos_node_value Int?

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_drupal_nb_node {
  entity_type                String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                     String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                    Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                  Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id                Int    @mkp_connect_drupal_db.UnsignedInt
  language                   String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                      Int    @mkp_connect_drupal_db.UnsignedInt
  field_drupal_nb_node_value Int?

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_drupal_staff_node {
  entity_type                   String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                        String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                       Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                     Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id                   Int    @mkp_connect_drupal_db.UnsignedInt
  language                      String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                         Int    @mkp_connect_drupal_db.UnsignedInt
  field_drupal_staff_node_value Int?

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_email_date {
  entity_type            String    @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                 String    @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                Int       @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id              Int       @mkp_connect_drupal_db.UnsignedInt
  revision_id            Int       @mkp_connect_drupal_db.UnsignedInt
  language               String    @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                  Int       @mkp_connect_drupal_db.UnsignedInt
  field_email_date_value DateTime? @mkp_connect_drupal_db.DateTime(0)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_email_date_value], map: "field_email_date_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_email_from {
  entity_type             String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                  String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                 Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id               Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id             Int     @mkp_connect_drupal_db.UnsignedInt
  language                String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                   Int     @mkp_connect_drupal_db.UnsignedInt
  field_email_from_value  String? @mkp_connect_drupal_db.VarChar(240)
  field_email_from_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_email_from_format], map: "field_email_from_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_email_prefix {
  entity_type               String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                    String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                   Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                 Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id               Int     @mkp_connect_drupal_db.UnsignedInt
  language                  String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                     Int     @mkp_connect_drupal_db.UnsignedInt
  field_email_prefix_value  String? @mkp_connect_drupal_db.VarChar(40)
  field_email_prefix_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_email_prefix_format], map: "field_email_prefix_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_end_date {
  entity_type          String    @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle               String    @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted              Int       @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id            Int       @mkp_connect_drupal_db.UnsignedInt
  revision_id          Int       @mkp_connect_drupal_db.UnsignedInt
  language             String    @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                Int       @mkp_connect_drupal_db.UnsignedInt
  field_end_date_value DateTime? @mkp_connect_drupal_db.DateTime(0)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_end_date_value], map: "field_end_date_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_entity {
  entity_type        String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle             String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted            Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id          Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id        Int    @mkp_connect_drupal_db.UnsignedInt
  language           String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta              Int    @mkp_connect_drupal_db.UnsignedInt
  field_entity_value Int?

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_entity_type {
  entity_type             String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                  String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                 Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id               Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id             Int     @mkp_connect_drupal_db.UnsignedInt
  language                String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                   Int     @mkp_connect_drupal_db.UnsignedInt
  field_entity_type_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_entity_type_value], map: "field_entity_type_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_event_complete {
  entity_type                String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                     String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                    Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                  Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id                Int    @mkp_connect_drupal_db.UnsignedInt
  language                   String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                      Int    @mkp_connect_drupal_db.UnsignedInt
  field_event_complete_value Int?

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_event_complete_value], map: "field_event_complete_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_event_image {
  entity_type              String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                   String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                  Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id              Int     @mkp_connect_drupal_db.UnsignedInt
  language                 String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                    Int     @mkp_connect_drupal_db.UnsignedInt
  field_event_image_fid    Int?    @mkp_connect_drupal_db.UnsignedInt
  field_event_image_alt    String? @mkp_connect_drupal_db.VarChar(512)
  field_event_image_title  String? @mkp_connect_drupal_db.VarChar(1024)
  field_event_image_width  Int?    @mkp_connect_drupal_db.UnsignedInt
  field_event_image_height Int?    @mkp_connect_drupal_db.UnsignedInt

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_event_image_fid], map: "field_event_image_fid")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_event_subtype {
  entity_type               String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                    String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                   Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                 Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id               Int     @mkp_connect_drupal_db.UnsignedInt
  language                  String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                     Int     @mkp_connect_drupal_db.UnsignedInt
  field_event_subtype_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_event_subtype_value], map: "field_event_subtype_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_event_type {
  entity_type            String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                 String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id              Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id            Int     @mkp_connect_drupal_db.UnsignedInt
  language               String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                  Int     @mkp_connect_drupal_db.UnsignedInt
  field_event_type_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_event_type_value], map: "field_event_type_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_extra_information {
  entity_type                    String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                         String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                        Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                      Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                    Int     @mkp_connect_drupal_db.UnsignedInt
  language                       String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                          Int     @mkp_connect_drupal_db.UnsignedInt
  field_extra_information_value  String? @mkp_connect_drupal_db.LongText
  field_extra_information_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_extra_information_format], map: "field_extra_information_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_facility {
  entity_type           String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted               Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id             Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id           Int     @mkp_connect_drupal_db.UnsignedInt
  language              String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                 Int     @mkp_connect_drupal_db.UnsignedInt
  field_facility_value  String? @mkp_connect_drupal_db.VarChar(80)
  field_facility_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_facility_format], map: "field_facility_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_feed_item_description {
  entity_type                         String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                              String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                             Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                           Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                         Int     @mkp_connect_drupal_db.UnsignedInt
  language                            String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                               Int     @mkp_connect_drupal_db.UnsignedInt
  field_feed_item_description_value   String? @mkp_connect_drupal_db.LongText
  field_feed_item_description_summary String? @mkp_connect_drupal_db.LongText
  field_feed_item_description_format  String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_feed_item_description_format], map: "field_feed_item_description_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_file_attachment {
  entity_type                       String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                            String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                           Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                         Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                       Int     @mkp_connect_drupal_db.UnsignedInt
  language                          String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                             Int     @mkp_connect_drupal_db.UnsignedInt
  field_file_attachment_fid         Int?    @mkp_connect_drupal_db.UnsignedInt
  field_file_attachment_display     Int     @default(1) @mkp_connect_drupal_db.UnsignedTinyInt
  field_file_attachment_description String? @mkp_connect_drupal_db.Text

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_file_attachment_fid], map: "field_file_attachment_fid")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_focus {
  entity_type       String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle            String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted           Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id         Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id       Int     @mkp_connect_drupal_db.UnsignedInt
  language          String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta             Int     @mkp_connect_drupal_db.UnsignedInt
  field_focus_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_focus_value], map: "field_focus_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_format {
  entity_type        String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle             String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted            Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id          Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id        Int     @mkp_connect_drupal_db.UnsignedInt
  language           String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta              Int     @mkp_connect_drupal_db.UnsignedInt
  field_format_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_format_value], map: "field_format_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_frequency {
  entity_type           String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted               Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id             Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id           Int     @mkp_connect_drupal_db.UnsignedInt
  language              String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                 Int     @mkp_connect_drupal_db.UnsignedInt
  field_frequency_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_frequency_value], map: "field_frequency_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_gateway {
  entity_type         String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle              String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted             Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id           Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id         Int     @mkp_connect_drupal_db.UnsignedInt
  language            String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta               Int     @mkp_connect_drupal_db.UnsignedInt
  field_gateway_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_gateway_value], map: "field_gateway_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_geo1 {
  entity_type         String   @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle              String   @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted             Int      @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id           Int      @mkp_connect_drupal_db.UnsignedInt
  revision_id         Int      @mkp_connect_drupal_db.UnsignedInt
  language            String   @default("") @mkp_connect_drupal_db.VarChar(32)
  delta               Int      @mkp_connect_drupal_db.UnsignedInt
  field_geo1_geom     Bytes?
  field_geo1_geo_type String?  @default("") @mkp_connect_drupal_db.VarChar(64)
  field_geo1_lat      Decimal? @mkp_connect_drupal_db.Decimal(18, 12)
  field_geo1_lon      Decimal? @mkp_connect_drupal_db.Decimal(18, 12)
  field_geo1_left     Decimal? @mkp_connect_drupal_db.Decimal(18, 12)
  field_geo1_top      Decimal? @mkp_connect_drupal_db.Decimal(18, 12)
  field_geo1_right    Decimal? @mkp_connect_drupal_db.Decimal(18, 12)
  field_geo1_bottom   Decimal? @mkp_connect_drupal_db.Decimal(18, 12)
  field_geo1_geohash  String?  @mkp_connect_drupal_db.VarChar(16)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_geo1_top, field_geo1_bottom, field_geo1_left, field_geo1_right], map: "field_geo1_bbox")
  @@index([field_geo1_bottom], map: "field_geo1_bottom")
  @@index([field_geo1_lat, field_geo1_lon], map: "field_geo1_centroid")
  @@index([field_geo1_geohash], map: "field_geo1_geohash")
  @@index([field_geo1_lat], map: "field_geo1_lat")
  @@index([field_geo1_left], map: "field_geo1_left")
  @@index([field_geo1_lon], map: "field_geo1_lon")
  @@index([field_geo1_right], map: "field_geo1_right")
  @@index([field_geo1_top], map: "field_geo1_top")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_geolocate {
  entity_type              String   @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                   String   @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                  Int      @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                Int      @mkp_connect_drupal_db.UnsignedInt
  revision_id              Int      @mkp_connect_drupal_db.UnsignedInt
  language                 String   @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                    Int      @mkp_connect_drupal_db.UnsignedInt
  field_geolocate_geom     Bytes?
  field_geolocate_geo_type String?  @default("") @mkp_connect_drupal_db.VarChar(64)
  field_geolocate_lat      Decimal? @mkp_connect_drupal_db.Decimal(18, 12)
  field_geolocate_lon      Decimal? @mkp_connect_drupal_db.Decimal(18, 12)
  field_geolocate_left     Decimal? @mkp_connect_drupal_db.Decimal(18, 12)
  field_geolocate_top      Decimal? @mkp_connect_drupal_db.Decimal(18, 12)
  field_geolocate_right    Decimal? @mkp_connect_drupal_db.Decimal(18, 12)
  field_geolocate_bottom   Decimal? @mkp_connect_drupal_db.Decimal(18, 12)
  field_geolocate_geohash  String?  @mkp_connect_drupal_db.VarChar(16)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_geolocate_top, field_geolocate_bottom, field_geolocate_left, field_geolocate_right], map: "field_geolocate_bbox")
  @@index([field_geolocate_bottom], map: "field_geolocate_bottom")
  @@index([field_geolocate_lat, field_geolocate_lon], map: "field_geolocate_centroid")
  @@index([field_geolocate_geohash], map: "field_geolocate_geohash")
  @@index([field_geolocate_lat], map: "field_geolocate_lat")
  @@index([field_geolocate_left], map: "field_geolocate_left")
  @@index([field_geolocate_lon], map: "field_geolocate_lon")
  @@index([field_geolocate_right], map: "field_geolocate_right")
  @@index([field_geolocate_top], map: "field_geolocate_top")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_google_drive {
  entity_type               String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                    String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                   Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                 Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id               Int     @mkp_connect_drupal_db.UnsignedInt
  language                  String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                     Int     @mkp_connect_drupal_db.UnsignedInt
  field_google_drive_value  String? @mkp_connect_drupal_db.VarChar(80)
  field_google_drive_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_google_drive_format], map: "field_google_drive_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_google_group {
  entity_type              String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                   String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                  Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id              Int    @mkp_connect_drupal_db.UnsignedInt
  language                 String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                    Int    @mkp_connect_drupal_db.UnsignedInt
  field_google_group_value Int?

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_google_group_value], map: "field_google_group_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_group_email {
  entity_type              String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                   String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                  Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id              Int     @mkp_connect_drupal_db.UnsignedInt
  language                 String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                    Int     @mkp_connect_drupal_db.UnsignedInt
  field_group_email_value  String? @mkp_connect_drupal_db.VarChar(60)
  field_group_email_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_group_email_format], map: "field_group_email_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_group_id {
  entity_type          String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle               String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted              Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id            Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id          Int    @mkp_connect_drupal_db.UnsignedInt
  language             String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                Int    @mkp_connect_drupal_db.UnsignedInt
  field_group_id_value Int?

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_group_photo {
  entity_type              String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                   String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                  Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id              Int     @mkp_connect_drupal_db.UnsignedInt
  language                 String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                    Int     @mkp_connect_drupal_db.UnsignedInt
  field_group_photo_fid    Int?    @mkp_connect_drupal_db.UnsignedInt
  field_group_photo_alt    String? @mkp_connect_drupal_db.VarChar(512)
  field_group_photo_title  String? @mkp_connect_drupal_db.VarChar(1024)
  field_group_photo_width  Int?    @mkp_connect_drupal_db.UnsignedInt
  field_group_photo_height Int?    @mkp_connect_drupal_db.UnsignedInt

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_group_photo_fid], map: "field_group_photo_fid")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_group_type {
  entity_type            String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                 String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id              Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id            Int     @mkp_connect_drupal_db.UnsignedInt
  language               String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                  Int     @mkp_connect_drupal_db.UnsignedInt
  field_group_type_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_group_type_value], map: "field_group_type_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_group_type_2 {
  entity_type              String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                   String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                  Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id              Int     @mkp_connect_drupal_db.UnsignedInt
  language                 String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                    Int     @mkp_connect_drupal_db.UnsignedInt
  field_group_type_2_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_group_type_2_value], map: "field_group_type_2_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_history {
  entity_type          String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle               String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted              Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id            Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id          Int     @mkp_connect_drupal_db.UnsignedInt
  language             String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                Int     @mkp_connect_drupal_db.UnsignedInt
  field_history_value  String? @mkp_connect_drupal_db.LongText
  field_history_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_history_format], map: "field_history_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_ig_member {
  entity_type           String   @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                String   @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted               Int      @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id             Int      @mkp_connect_drupal_db.UnsignedInt
  revision_id           Int      @mkp_connect_drupal_db.UnsignedInt
  language              String   @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                 Int      @mkp_connect_drupal_db.UnsignedInt
  field_ig_member_value Decimal? @mkp_connect_drupal_db.Decimal(10, 2)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_igp_prequisite {
  entity_type                String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                     String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                    Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                  Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                Int     @mkp_connect_drupal_db.UnsignedInt
  language                   String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                      Int     @mkp_connect_drupal_db.UnsignedInt
  field_igp_prequisite_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_igp_prequisite_value], map: "field_igp_prequisite_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_igroup_class {
  entity_type              String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                   String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                  Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id              Int     @mkp_connect_drupal_db.UnsignedInt
  language                 String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                    Int     @mkp_connect_drupal_db.UnsignedInt
  field_igroup_class_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_igroup_class_value], map: "field_igroup_class_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_igroup_initiated_visit {
  entity_type                        String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                             String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                            Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                          Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                        Int     @mkp_connect_drupal_db.UnsignedInt
  language                           String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                              Int     @mkp_connect_drupal_db.UnsignedInt
  field_igroup_initiated_visit_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_igroup_initiated_visit_value], map: "field_igroup_initiated_visit_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_igroup_new_members {
  entity_type                    String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                         String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                        Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                      Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                    Int     @mkp_connect_drupal_db.UnsignedInt
  language                       String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                          Int     @mkp_connect_drupal_db.UnsignedInt
  field_igroup_new_members_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_igroup_new_members_value], map: "field_igroup_new_members_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_igroup_status {
  entity_type               String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                    String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                   Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                 Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id               Int     @mkp_connect_drupal_db.UnsignedInt
  language                  String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                     Int     @mkp_connect_drupal_db.UnsignedInt
  field_igroup_status_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_igroup_status_value], map: "field_igroup_status_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_igroup_type {
  entity_type             String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                  String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                 Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id               Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id             Int     @mkp_connect_drupal_db.UnsignedInt
  language                String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                   Int     @mkp_connect_drupal_db.UnsignedInt
  field_igroup_type_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_igroup_type_value], map: "field_igroup_type_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_igroup_uninitiated_visit {
  entity_type                          String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                               String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                              Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                            Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                          Int     @mkp_connect_drupal_db.UnsignedInt
  language                             String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                                Int     @mkp_connect_drupal_db.UnsignedInt
  field_igroup_uninitiated_visit_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_igroup_uninitiated_visit_value], map: "field_igroup_uninitiated_visit_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_image {
  entity_type        String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle             String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted            Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id          Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id        Int     @mkp_connect_drupal_db.UnsignedInt
  language           String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta              Int     @mkp_connect_drupal_db.UnsignedInt
  field_image_fid    Int?    @mkp_connect_drupal_db.UnsignedInt
  field_image_alt    String? @mkp_connect_drupal_db.VarChar(512)
  field_image_title  String? @mkp_connect_drupal_db.VarChar(1024)
  field_image_width  Int?    @mkp_connect_drupal_db.UnsignedInt
  field_image_height Int?    @mkp_connect_drupal_db.UnsignedInt

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_image_fid], map: "field_image_fid")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_image_location {
  entity_type                 String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                      String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                     Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                   Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                 Int     @mkp_connect_drupal_db.UnsignedInt
  language                    String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                       Int     @mkp_connect_drupal_db.UnsignedInt
  field_image_location_value  String? @mkp_connect_drupal_db.VarChar(240)
  field_image_location_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_image_location_format], map: "field_image_location_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_img {
  entity_type      String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle           String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted          Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id        Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id      Int     @mkp_connect_drupal_db.UnsignedInt
  language         String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta            Int     @mkp_connect_drupal_db.UnsignedInt
  field_img_fid    Int?    @mkp_connect_drupal_db.UnsignedInt
  field_img_alt    String? @mkp_connect_drupal_db.VarChar(512)
  field_img_title  String? @mkp_connect_drupal_db.VarChar(1024)
  field_img_width  Int?    @mkp_connect_drupal_db.UnsignedInt
  field_img_height Int?    @mkp_connect_drupal_db.UnsignedInt

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_img_fid], map: "field_img_fid")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_invitation_email_address {
  entity_type                           String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                                String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                               Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                             Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                           Int     @mkp_connect_drupal_db.UnsignedInt
  language                              String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                                 Int     @mkp_connect_drupal_db.UnsignedInt
  field_invitation_email_address_value  String? @mkp_connect_drupal_db.VarChar(255)
  field_invitation_email_address_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_invitation_email_address_format], map: "field_invitation_email_address_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_invitation_email_body {
  entity_type                        String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                             String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                            Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                          Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                        Int     @mkp_connect_drupal_db.UnsignedInt
  language                           String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                              Int     @mkp_connect_drupal_db.UnsignedInt
  field_invitation_email_body_value  String? @mkp_connect_drupal_db.LongText
  field_invitation_email_body_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_invitation_email_body_format], map: "field_invitation_email_body_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_invitation_email_subject {
  entity_type                           String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                                String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                               Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                             Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                           Int     @mkp_connect_drupal_db.UnsignedInt
  language                              String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                                 Int     @mkp_connect_drupal_db.UnsignedInt
  field_invitation_email_subject_value  String? @mkp_connect_drupal_db.VarChar(255)
  field_invitation_email_subject_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_invitation_email_subject_format], map: "field_invitation_email_subject_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_join_status {
  entity_type             String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                  String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                 Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id               Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id             Int     @mkp_connect_drupal_db.UnsignedInt
  language                String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                   Int     @mkp_connect_drupal_db.UnsignedInt
  field_join_status_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_join_status_value], map: "field_join_status_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_language {
  entity_type          String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle               String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted              Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id            Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id          Int     @mkp_connect_drupal_db.UnsignedInt
  language             String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                Int     @mkp_connect_drupal_db.UnsignedInt
  field_language_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_language_value], map: "field_language_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_latitude {
  entity_type          String   @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle               String   @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted              Int      @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id            Int      @mkp_connect_drupal_db.UnsignedInt
  revision_id          Int      @mkp_connect_drupal_db.UnsignedInt
  language             String   @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                Int      @mkp_connect_drupal_db.UnsignedInt
  field_latitude_value Decimal? @mkp_connect_drupal_db.Decimal(10, 6)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_leader {
  entity_type         String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle              String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted             Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id           Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id         Int     @mkp_connect_drupal_db.UnsignedInt
  language            String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta               Int     @mkp_connect_drupal_db.UnsignedInt
  field_leader_value  String? @mkp_connect_drupal_db.VarChar(120)
  field_leader_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_leader_format], map: "field_leader_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_legacy_nid {
  entity_type             String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                  String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                 Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id               Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id             Int     @mkp_connect_drupal_db.UnsignedInt
  language                String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                   Int     @mkp_connect_drupal_db.UnsignedInt
  field_legacy_nid_value  String? @mkp_connect_drupal_db.VarChar(255)
  field_legacy_nid_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_legacy_nid_format], map: "field_legacy_nid_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_library_destination {
  entity_type                     String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                          String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                         Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                       Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                     Int     @mkp_connect_drupal_db.UnsignedInt
  language                        String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                           Int     @mkp_connect_drupal_db.UnsignedInt
  field_library_destination_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_library_destination_value], map: "field_library_destination_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_link_1_link {
  entity_type              String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                   String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                  Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id              Int     @mkp_connect_drupal_db.UnsignedInt
  language                 String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                    Int     @mkp_connect_drupal_db.UnsignedInt
  field_link_1_link_value  String? @mkp_connect_drupal_db.VarChar(100)
  field_link_1_link_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_link_1_link_format], map: "field_link_1_link_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_link_1_title {
  entity_type               String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                    String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                   Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                 Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id               Int     @mkp_connect_drupal_db.UnsignedInt
  language                  String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                     Int     @mkp_connect_drupal_db.UnsignedInt
  field_link_1_title_value  String? @mkp_connect_drupal_db.VarChar(100)
  field_link_1_title_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_link_1_title_format], map: "field_link_1_title_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_link_2_link {
  entity_type              String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                   String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                  Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id              Int     @mkp_connect_drupal_db.UnsignedInt
  language                 String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                    Int     @mkp_connect_drupal_db.UnsignedInt
  field_link_2_link_value  String? @mkp_connect_drupal_db.VarChar(100)
  field_link_2_link_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_link_2_link_format], map: "field_link_2_link_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_link_2_title {
  entity_type               String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                    String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                   Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                 Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id               Int     @mkp_connect_drupal_db.UnsignedInt
  language                  String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                     Int     @mkp_connect_drupal_db.UnsignedInt
  field_link_2_title_value  String? @mkp_connect_drupal_db.VarChar(100)
  field_link_2_title_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_link_2_title_format], map: "field_link_2_title_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_link_3_link {
  entity_type              String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                   String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                  Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id              Int     @mkp_connect_drupal_db.UnsignedInt
  language                 String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                    Int     @mkp_connect_drupal_db.UnsignedInt
  field_link_3_link_value  String? @mkp_connect_drupal_db.VarChar(100)
  field_link_3_link_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_link_3_link_format], map: "field_link_3_link_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_link_3_title {
  entity_type               String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                    String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                   Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                 Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id               Int     @mkp_connect_drupal_db.UnsignedInt
  language                  String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                     Int     @mkp_connect_drupal_db.UnsignedInt
  field_link_3_title_value  String? @mkp_connect_drupal_db.VarChar(100)
  field_link_3_title_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_link_3_title_format], map: "field_link_3_title_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_loc_lat_lon {
  entity_type              String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                   String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                  Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id              Int     @mkp_connect_drupal_db.UnsignedInt
  language                 String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                    Int     @mkp_connect_drupal_db.UnsignedInt
  field_loc_lat_lon_value  String? @mkp_connect_drupal_db.VarChar(100)
  field_loc_lat_lon_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_loc_lat_lon_format], map: "field_loc_lat_lon_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_location {
  entity_type           String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted               Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id             Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id           Int     @mkp_connect_drupal_db.UnsignedInt
  language              String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                 Int     @mkp_connect_drupal_db.UnsignedInt
  field_location_value  String? @mkp_connect_drupal_db.VarChar(100)
  field_location_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_location_format], map: "field_location_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_longitude {
  entity_type           String   @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                String   @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted               Int      @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id             Int      @mkp_connect_drupal_db.UnsignedInt
  revision_id           Int      @mkp_connect_drupal_db.UnsignedInt
  language              String   @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                 Int      @mkp_connect_drupal_db.UnsignedInt
  field_longitude_value Decimal? @mkp_connect_drupal_db.Decimal(10, 6)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_main_contact {
  entity_type              String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                   String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                  Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id              Int    @mkp_connect_drupal_db.UnsignedInt
  language                 String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                    Int    @mkp_connect_drupal_db.UnsignedInt
  field_main_contact_value Int?

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_main_contact_value], map: "field_main_contact_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_max_participants {
  entity_type                  String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                       String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                      Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                    Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id                  Int    @mkp_connect_drupal_db.UnsignedInt
  language                     String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                        Int    @mkp_connect_drupal_db.UnsignedInt
  field_max_participants_value Int?

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_meeting_night {
  entity_type               String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                    String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                   Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                 Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id               Int     @mkp_connect_drupal_db.UnsignedInt
  language                  String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                     Int     @mkp_connect_drupal_db.UnsignedInt
  field_meeting_night_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_meeting_night_value], map: "field_meeting_night_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_meeting_time {
  entity_type               String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                    String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                   Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                 Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id               Int     @mkp_connect_drupal_db.UnsignedInt
  language                  String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                     Int     @mkp_connect_drupal_db.UnsignedInt
  field_meeting_time_value  String? @mkp_connect_drupal_db.VarChar(120)
  field_meeting_time_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_meeting_time_format], map: "field_meeting_time_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_message_id {
  entity_type             String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                  String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                 Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id               Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id             Int     @mkp_connect_drupal_db.UnsignedInt
  language                String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                   Int     @mkp_connect_drupal_db.UnsignedInt
  field_message_id_value  String? @mkp_connect_drupal_db.VarChar(320)
  field_message_id_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_message_id_format], map: "field_message_id_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_migrate_example_country {
  entity_type                          String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                               String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                              Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                            Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                          Int     @mkp_connect_drupal_db.UnsignedInt
  language                             String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                                Int     @mkp_connect_drupal_db.UnsignedInt
  field_migrate_example_country_value  String? @mkp_connect_drupal_db.VarChar(255)
  field_migrate_example_country_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_migrate_example_country_format], map: "field_migrate_example_country_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_migrate_example_favbeers {
  entity_type                        String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                             String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                            Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                          Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id                        Int    @mkp_connect_drupal_db.UnsignedInt
  language                           String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                              Int    @mkp_connect_drupal_db.UnsignedInt
  field_migrate_example_favbeers_nid Int?   @mkp_connect_drupal_db.UnsignedInt

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_migrate_example_favbeers_nid], map: "field_migrate_example_favbeers_nid")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_migrate_example_gender {
  entity_type                        String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                             String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                            Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                          Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id                        Int    @mkp_connect_drupal_db.UnsignedInt
  language                           String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                              Int    @mkp_connect_drupal_db.UnsignedInt
  field_migrate_example_gender_value Int?

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_migrate_example_gender_value], map: "field_migrate_example_gender_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_migrate_example_image {
  entity_type                        String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                             String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                            Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                          Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                        Int     @mkp_connect_drupal_db.UnsignedInt
  language                           String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                              Int     @mkp_connect_drupal_db.UnsignedInt
  field_migrate_example_image_fid    Int?    @mkp_connect_drupal_db.UnsignedInt
  field_migrate_example_image_alt    String? @mkp_connect_drupal_db.VarChar(512)
  field_migrate_example_image_title  String? @mkp_connect_drupal_db.VarChar(1024)
  field_migrate_example_image_width  Int?    @mkp_connect_drupal_db.UnsignedInt
  field_migrate_example_image_height Int?    @mkp_connect_drupal_db.UnsignedInt

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_migrate_example_image_fid], map: "field_migrate_example_image_fid")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_migrate_example_top_vintag {
  entity_type                            String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                                 String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                                Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                              Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id                            Int    @mkp_connect_drupal_db.UnsignedInt
  language                               String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                                  Int    @mkp_connect_drupal_db.UnsignedInt
  field_migrate_example_top_vintag_value Int?

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_migrate_example_wine_ratin {
  entity_type                            String   @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                                 String   @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                                Int      @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                              Int      @mkp_connect_drupal_db.UnsignedInt
  revision_id                            Int      @mkp_connect_drupal_db.UnsignedInt
  language                               String   @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                                  Int      @mkp_connect_drupal_db.UnsignedInt
  field_migrate_example_wine_ratin_value Decimal? @mkp_connect_drupal_db.Decimal(10, 2)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_mixed_gender {
  entity_type              String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                   String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                  Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id              Int    @mkp_connect_drupal_db.UnsignedInt
  language                 String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                    Int    @mkp_connect_drupal_db.UnsignedInt
  field_mixed_gender_value Int?

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_mixed_gender_value], map: "field_mixed_gender_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_mkp_area {
  entity_type           String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted               Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id             Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id           Int     @mkp_connect_drupal_db.UnsignedInt
  language              String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                 Int     @mkp_connect_drupal_db.UnsignedInt
  field_mkp_area_value  String? @mkp_connect_drupal_db.VarChar(60)
  field_mkp_area_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_mkp_area_format], map: "field_mkp_area_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_mkp_community {
  entity_type                String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                     String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                    Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                  Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                Int     @mkp_connect_drupal_db.UnsignedInt
  language                   String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                      Int     @mkp_connect_drupal_db.UnsignedInt
  field_mkp_community_value  String? @mkp_connect_drupal_db.VarChar(60)
  field_mkp_community_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_mkp_community_format], map: "field_mkp_community_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_mkp_region {
  entity_type             String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                  String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                 Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id               Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id             Int     @mkp_connect_drupal_db.UnsignedInt
  language                String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                   Int     @mkp_connect_drupal_db.UnsignedInt
  field_mkp_region_value  String? @mkp_connect_drupal_db.VarChar(60)
  field_mkp_region_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_mkp_region_format], map: "field_mkp_region_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_mkpi {
  entity_type      String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle           String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted          Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id        Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id      Int    @mkp_connect_drupal_db.UnsignedInt
  language         String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta            Int    @mkp_connect_drupal_db.UnsignedInt
  field_mkpi_value Int?

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_mkpi_value], map: "field_mkpi_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_mkpi_build {
  entity_type             String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                  String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                 Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id               Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id             Int     @mkp_connect_drupal_db.UnsignedInt
  language                String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                   Int     @mkp_connect_drupal_db.UnsignedInt
  field_mkpi_build_value  String? @mkp_connect_drupal_db.VarChar(2)
  field_mkpi_build_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_mkpi_build_format], map: "field_mkpi_build_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_moderation {
  entity_type            String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                 String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id              Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id            Int     @mkp_connect_drupal_db.UnsignedInt
  language               String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                  Int     @mkp_connect_drupal_db.UnsignedInt
  field_moderation_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_moderation_value], map: "field_moderation_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_mos_fee {
  entity_type         String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle              String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted             Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id           Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id         Int    @mkp_connect_drupal_db.UnsignedInt
  language            String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta               Int    @mkp_connect_drupal_db.UnsignedInt
  field_mos_fee_value Int?

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_multipay {
  entity_type           String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted               Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id             Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id           Int     @mkp_connect_drupal_db.UnsignedInt
  language              String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                 Int     @mkp_connect_drupal_db.UnsignedInt
  field_multipay_value  String? @mkp_connect_drupal_db.VarChar(2)
  field_multipay_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_multipay_format], map: "field_multipay_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_nb_civi_event {
  entity_type                String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                     String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                    Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                  Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                Int     @mkp_connect_drupal_db.UnsignedInt
  language                   String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                      Int     @mkp_connect_drupal_db.UnsignedInt
  field_nb_civi_event_value  String? @mkp_connect_drupal_db.VarChar(255)
  field_nb_civi_event_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_nb_civi_event_format], map: "field_nb_civi_event_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_nb_info_link {
  entity_type              String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                   String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                  Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id              Int    @mkp_connect_drupal_db.UnsignedInt
  language                 String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                    Int    @mkp_connect_drupal_db.UnsignedInt
  field_nb_info_link_value Int?

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_news_image {
  entity_type             String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                  String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                 Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id               Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id             Int     @mkp_connect_drupal_db.UnsignedInt
  language                String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                   Int     @mkp_connect_drupal_db.UnsignedInt
  field_news_image_fid    Int?    @mkp_connect_drupal_db.UnsignedInt
  field_news_image_alt    String? @mkp_connect_drupal_db.VarChar(512)
  field_news_image_title  String? @mkp_connect_drupal_db.VarChar(1024)
  field_news_image_width  Int?    @mkp_connect_drupal_db.UnsignedInt
  field_news_image_height Int?    @mkp_connect_drupal_db.UnsignedInt

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_news_image_fid], map: "field_news_image_fid")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_nudity {
  entity_type        String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle             String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted            Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id          Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id        Int     @mkp_connect_drupal_db.UnsignedInt
  language           String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta              Int     @mkp_connect_drupal_db.UnsignedInt
  field_nudity_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_nudity_value], map: "field_nudity_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_number_of_members {
  entity_type                   String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                        String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                       Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                     Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id                   Int    @mkp_connect_drupal_db.UnsignedInt
  language                      String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                         Int    @mkp_connect_drupal_db.UnsignedInt
  field_number_of_members_value Int?

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_number_of_users {
  entity_type                 String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                      String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                     Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                   Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id                 Int    @mkp_connect_drupal_db.UnsignedInt
  language                    String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                       Int    @mkp_connect_drupal_db.UnsignedInt
  field_number_of_users_value Int?

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_nwta_end_date {
  entity_type               String    @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                    String    @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                   Int       @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                 Int       @mkp_connect_drupal_db.UnsignedInt
  revision_id               Int       @mkp_connect_drupal_db.UnsignedInt
  language                  String    @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                     Int       @mkp_connect_drupal_db.UnsignedInt
  field_nwta_end_date_value DateTime? @mkp_connect_drupal_db.DateTime(0)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_nwta_end_date_value], map: "field_nwta_end_date_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_open_group {
  entity_type            String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                 String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id              Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id            Int     @mkp_connect_drupal_db.UnsignedInt
  language               String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                  Int     @mkp_connect_drupal_db.UnsignedInt
  field_open_group_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_open_group_value], map: "field_open_group_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_order {
  entity_type       String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle            String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted           Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id         Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id       Int    @mkp_connect_drupal_db.UnsignedInt
  language          String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta             Int    @mkp_connect_drupal_db.UnsignedInt
  field_order_value Int?

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_owner {
  entity_type       String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle            String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted           Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id         Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id       Int     @mkp_connect_drupal_db.UnsignedInt
  language          String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta             Int     @mkp_connect_drupal_db.UnsignedInt
  field_owner_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_owner_value], map: "field_owner_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_owner_1 {
  entity_type         String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle              String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted             Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id           Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id         Int     @mkp_connect_drupal_db.UnsignedInt
  language            String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta               Int     @mkp_connect_drupal_db.UnsignedInt
  field_owner_1_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_owner_1_value], map: "field_owner_1_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_owner_nid {
  entity_type           String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted               Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id             Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id           Int    @mkp_connect_drupal_db.UnsignedInt
  language              String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                 Int    @mkp_connect_drupal_db.UnsignedInt
  field_owner_nid_value Int?

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_part_fee {
  entity_type          String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle               String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted              Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id            Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id          Int    @mkp_connect_drupal_db.UnsignedInt
  language             String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                Int    @mkp_connect_drupal_db.UnsignedInt
  field_part_fee_value Int?

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_part_reg_end_date {
  entity_type                   String    @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                        String    @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                       Int       @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                     Int       @mkp_connect_drupal_db.UnsignedInt
  revision_id                   Int       @mkp_connect_drupal_db.UnsignedInt
  language                      String    @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                         Int       @mkp_connect_drupal_db.UnsignedInt
  field_part_reg_end_date_value DateTime? @mkp_connect_drupal_db.DateTime(0)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_part_reg_end_date_value], map: "field_part_reg_end_date_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_part_reg_start_date {
  entity_type                     String    @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                          String    @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                         Int       @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                       Int       @mkp_connect_drupal_db.UnsignedInt
  revision_id                     Int       @mkp_connect_drupal_db.UnsignedInt
  language                        String    @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                           Int       @mkp_connect_drupal_db.UnsignedInt
  field_part_reg_start_date_value DateTime? @mkp_connect_drupal_db.DateTime(0)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_part_reg_start_date_value], map: "field_part_reg_start_date_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_part_registration_link {
  entity_type                         String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                              String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                             Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                           Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                         Int     @mkp_connect_drupal_db.UnsignedInt
  language                            String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                               Int     @mkp_connect_drupal_db.UnsignedInt
  field_part_registration_link_value  String? @mkp_connect_drupal_db.VarChar(120)
  field_part_registration_link_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_part_registration_link_format], map: "field_part_registration_link_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_part_start_date {
  entity_type                 String    @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                      String    @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                     Int       @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                   Int       @mkp_connect_drupal_db.UnsignedInt
  revision_id                 Int       @mkp_connect_drupal_db.UnsignedInt
  language                    String    @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                       Int       @mkp_connect_drupal_db.UnsignedInt
  field_part_start_date_value DateTime? @mkp_connect_drupal_db.DateTime(0)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_part_start_date_value], map: "field_part_start_date_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_participant_registrar {
  entity_type                        String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                             String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                            Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                          Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                        Int     @mkp_connect_drupal_db.UnsignedInt
  language                           String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                              Int     @mkp_connect_drupal_db.UnsignedInt
  field_participant_registrar_value  String? @mkp_connect_drupal_db.VarChar(60)
  field_participant_registrar_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_participant_registrar_format], map: "field_participant_registrar_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_participant_registrar_emai {
  entity_type                             String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                                  String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                                 Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                               Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                             Int     @mkp_connect_drupal_db.UnsignedInt
  language                                String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                                   Int     @mkp_connect_drupal_db.UnsignedInt
  field_participant_registrar_emai_value  String? @mkp_connect_drupal_db.VarChar(60)
  field_participant_registrar_emai_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_participant_registrar_emai_format], map: "field_participant_registrar_emai_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_participant_registrar_phon {
  entity_type                             String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                                  String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                                 Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                               Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                             Int     @mkp_connect_drupal_db.UnsignedInt
  language                                String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                                   Int     @mkp_connect_drupal_db.UnsignedInt
  field_participant_registrar_phon_value  String? @mkp_connect_drupal_db.VarChar(40)
  field_participant_registrar_phon_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_participant_registrar_phon_format], map: "field_participant_registrar_phon_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_percentage_of_members {
  entity_type                       String   @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                            String   @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                           Int      @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                         Int      @mkp_connect_drupal_db.UnsignedInt
  revision_id                       Int      @mkp_connect_drupal_db.UnsignedInt
  language                          String   @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                             Int      @mkp_connect_drupal_db.UnsignedInt
  field_percentage_of_members_value Decimal? @mkp_connect_drupal_db.Decimal(10, 2)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_postal_code {
  entity_type              String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                   String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                  Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id              Int     @mkp_connect_drupal_db.UnsignedInt
  language                 String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                    Int     @mkp_connect_drupal_db.UnsignedInt
  field_postal_code_value  String? @mkp_connect_drupal_db.VarChar(20)
  field_postal_code_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_postal_code_format], map: "field_postal_code_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_prerequisite {
  entity_type              String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                   String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                  Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id              Int     @mkp_connect_drupal_db.UnsignedInt
  language                 String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                    Int     @mkp_connect_drupal_db.UnsignedInt
  field_prerequisite_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_prerequisite_value], map: "field_prerequisite_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_price {
  entity_type       String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle            String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted           Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id         Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id       Int    @mkp_connect_drupal_db.UnsignedInt
  language          String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta             Int    @mkp_connect_drupal_db.UnsignedInt
  field_price_value Int?

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_private {
  entity_type         String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle              String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted             Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id           Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id         Int    @mkp_connect_drupal_db.UnsignedInt
  language            String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta               Int    @mkp_connect_drupal_db.UnsignedInt
  field_private_value Int?

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_private_value], map: "field_private_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_private_group {
  entity_type               String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                    String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                   Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                 Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id               Int    @mkp_connect_drupal_db.UnsignedInt
  language                  String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                     Int    @mkp_connect_drupal_db.UnsignedInt
  field_private_group_value Int?

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_private_group_value], map: "field_private_group_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_process_type {
  entity_type              String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                   String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                  Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id              Int     @mkp_connect_drupal_db.UnsignedInt
  language                 String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                    Int     @mkp_connect_drupal_db.UnsignedInt
  field_process_type_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_process_type_value], map: "field_process_type_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_public_contact {
  entity_type                String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                     String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                    Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                  Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id                Int    @mkp_connect_drupal_db.UnsignedInt
  language                   String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                      Int    @mkp_connect_drupal_db.UnsignedInt
  field_public_contact_value Int?

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_public_contact_value], map: "field_public_contact_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_public_display {
  entity_type                String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                     String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                    Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                  Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id                Int    @mkp_connect_drupal_db.UnsignedInt
  language                   String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                      Int    @mkp_connect_drupal_db.UnsignedInt
  field_public_display_value Int?

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_public_display_value], map: "field_public_display_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_readiness {
  entity_type            String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                 String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id              Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id            Int     @mkp_connect_drupal_db.UnsignedInt
  language               String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                  Int     @mkp_connect_drupal_db.UnsignedInt
  field_readiness_value  String? @mkp_connect_drupal_db.VarChar(2)
  field_readiness_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_readiness_format], map: "field_readiness_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_reg_login {
  entity_type            String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                 String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id              Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id            Int     @mkp_connect_drupal_db.UnsignedInt
  language               String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                  Int     @mkp_connect_drupal_db.UnsignedInt
  field_reg_login_value  String? @mkp_connect_drupal_db.VarChar(2)
  field_reg_login_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_reg_login_format], map: "field_reg_login_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_region {
  entity_type        String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle             String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted            Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id          Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id        Int     @mkp_connect_drupal_db.UnsignedInt
  language           String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta              Int     @mkp_connect_drupal_db.UnsignedInt
  field_region_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_region_value], map: "field_region_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_region_link {
  entity_type              String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                   String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                  Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id              Int     @mkp_connect_drupal_db.UnsignedInt
  language                 String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                    Int     @mkp_connect_drupal_db.UnsignedInt
  field_region_link_value  String? @mkp_connect_drupal_db.VarChar(60)
  field_region_link_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_region_link_format], map: "field_region_link_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_region_nid {
  entity_type            String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                 String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id              Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id            Int    @mkp_connect_drupal_db.UnsignedInt
  language               String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                  Int    @mkp_connect_drupal_db.UnsignedInt
  field_region_nid_value Int?

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_registration_end_date {
  entity_type                       String    @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                            String    @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                           Int       @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                         Int       @mkp_connect_drupal_db.UnsignedInt
  revision_id                       Int       @mkp_connect_drupal_db.UnsignedInt
  language                          String    @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                             Int       @mkp_connect_drupal_db.UnsignedInt
  field_registration_end_date_value DateTime? @mkp_connect_drupal_db.DateTime(0)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_registration_end_date_value], map: "field_registration_end_date_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_registration_link {
  entity_type                    String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                         String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                        Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                      Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                    Int     @mkp_connect_drupal_db.UnsignedInt
  language                       String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                          Int     @mkp_connect_drupal_db.UnsignedInt
  field_registration_link_value  String? @mkp_connect_drupal_db.VarChar(120)
  field_registration_link_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_registration_link_format], map: "field_registration_link_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_registration_start_date {
  entity_type                         String    @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                              String    @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                             Int       @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                           Int       @mkp_connect_drupal_db.UnsignedInt
  revision_id                         Int       @mkp_connect_drupal_db.UnsignedInt
  language                            String    @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                               Int       @mkp_connect_drupal_db.UnsignedInt
  field_registration_start_date_value DateTime? @mkp_connect_drupal_db.DateTime(0)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_registration_start_date_value], map: "field_registration_start_date_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_related_event_1 {
  entity_type                 String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                      String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                     Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                   Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id                 Int    @mkp_connect_drupal_db.UnsignedInt
  language                    String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                       Int    @mkp_connect_drupal_db.UnsignedInt
  field_related_event_1_value Int?

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_related_event_2 {
  entity_type                 String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                      String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                     Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                   Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id                 Int    @mkp_connect_drupal_db.UnsignedInt
  language                    String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                       Int    @mkp_connect_drupal_db.UnsignedInt
  field_related_event_2_value Int?

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_reply_to {
  entity_type          String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle               String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted              Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id            Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id          Int     @mkp_connect_drupal_db.UnsignedInt
  language             String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                Int     @mkp_connect_drupal_db.UnsignedInt
  field_reply_to_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_reply_to_value], map: "field_reply_to_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_responsible {
  entity_type              String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                   String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                  Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id              Int     @mkp_connect_drupal_db.UnsignedInt
  language                 String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                    Int     @mkp_connect_drupal_db.UnsignedInt
  field_responsible_value  String? @mkp_connect_drupal_db.VarChar(120)
  field_responsible_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_responsible_format], map: "field_responsible_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_role {
  entity_type       String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle            String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted           Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id         Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id       Int     @mkp_connect_drupal_db.UnsignedInt
  language          String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta             Int     @mkp_connect_drupal_db.UnsignedInt
  field_role_value  String? @mkp_connect_drupal_db.VarChar(60)
  field_role_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_role_format], map: "field_role_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_specialized {
  entity_type              String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                   String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                  Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id              Int     @mkp_connect_drupal_db.UnsignedInt
  language                 String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                    Int     @mkp_connect_drupal_db.UnsignedInt
  field_specialized_value  String? @mkp_connect_drupal_db.VarChar(60)
  field_specialized_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_specialized_format], map: "field_specialized_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_sponsored {
  entity_type           String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted               Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id             Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id           Int     @mkp_connect_drupal_db.UnsignedInt
  language              String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                 Int     @mkp_connect_drupal_db.UnsignedInt
  field_sponsored_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_sponsored_value], map: "field_sponsored_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_sponsored_entity {
  entity_type                  String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                       String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                      Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                    Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                  Int     @mkp_connect_drupal_db.UnsignedInt
  language                     String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                        Int     @mkp_connect_drupal_db.UnsignedInt
  field_sponsored_entity_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_sponsored_entity_value], map: "field_sponsored_entity_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_sstype {
  entity_type        String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle             String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted            Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id          Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id        Int     @mkp_connect_drupal_db.UnsignedInt
  language           String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta              Int     @mkp_connect_drupal_db.UnsignedInt
  field_sstype_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_sstype_value], map: "field_sstype_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_staff_coordinator {
  entity_type                    String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                         String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                        Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                      Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                    Int     @mkp_connect_drupal_db.UnsignedInt
  language                       String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                          Int     @mkp_connect_drupal_db.UnsignedInt
  field_staff_coordinator_value  String? @mkp_connect_drupal_db.VarChar(60)
  field_staff_coordinator_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_staff_coordinator_format], map: "field_staff_coordinator_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_staff_coordinator_email {
  entity_type                          String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                               String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                              Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                            Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                          Int     @mkp_connect_drupal_db.UnsignedInt
  language                             String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                                Int     @mkp_connect_drupal_db.UnsignedInt
  field_staff_coordinator_email_value  String? @mkp_connect_drupal_db.VarChar(60)
  field_staff_coordinator_email_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_staff_coordinator_email_format], map: "field_staff_coordinator_email_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_staff_coordinator_phone {
  entity_type                          String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                               String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                              Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                            Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                          Int     @mkp_connect_drupal_db.UnsignedInt
  language                             String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                                Int     @mkp_connect_drupal_db.UnsignedInt
  field_staff_coordinator_phone_value  String? @mkp_connect_drupal_db.VarChar(40)
  field_staff_coordinator_phone_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_staff_coordinator_phone_format], map: "field_staff_coordinator_phone_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_staff_fee {
  entity_type           String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted               Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id             Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id           Int    @mkp_connect_drupal_db.UnsignedInt
  language              String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                 Int    @mkp_connect_drupal_db.UnsignedInt
  field_staff_fee_value Int?

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_staff_group_nid {
  entity_type                 String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                      String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                     Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                   Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id                 Int    @mkp_connect_drupal_db.UnsignedInt
  language                    String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                       Int    @mkp_connect_drupal_db.UnsignedInt
  field_staff_group_nid_value Int?

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_staff_reg_end_date {
  entity_type                    String    @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                         String    @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                        Int       @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                      Int       @mkp_connect_drupal_db.UnsignedInt
  revision_id                    Int       @mkp_connect_drupal_db.UnsignedInt
  language                       String    @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                          Int       @mkp_connect_drupal_db.UnsignedInt
  field_staff_reg_end_date_value DateTime? @mkp_connect_drupal_db.DateTime(0)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_staff_reg_end_date_value], map: "field_staff_reg_end_date_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_staff_reg_start_date {
  entity_type                      String    @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                           String    @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                          Int       @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                        Int       @mkp_connect_drupal_db.UnsignedInt
  revision_id                      Int       @mkp_connect_drupal_db.UnsignedInt
  language                         String    @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                            Int       @mkp_connect_drupal_db.UnsignedInt
  field_staff_reg_start_date_value DateTime? @mkp_connect_drupal_db.DateTime(0)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_staff_reg_start_date_value], map: "field_staff_reg_start_date_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_staff_registration_link {
  entity_type                          String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                               String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                              Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                            Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                          Int     @mkp_connect_drupal_db.UnsignedInt
  language                             String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                                Int     @mkp_connect_drupal_db.UnsignedInt
  field_staff_registration_link_value  String? @mkp_connect_drupal_db.VarChar(255)
  field_staff_registration_link_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_staff_registration_link_format], map: "field_staff_registration_link_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_staff_start_date {
  entity_type                  String    @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                       String    @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                      Int       @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                    Int       @mkp_connect_drupal_db.UnsignedInt
  revision_id                  Int       @mkp_connect_drupal_db.UnsignedInt
  language                     String    @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                        Int       @mkp_connect_drupal_db.UnsignedInt
  field_staff_start_date_value DateTime? @mkp_connect_drupal_db.DateTime(0)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_staff_start_date_value], map: "field_staff_start_date_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_start_date {
  entity_type            String    @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                 String    @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                Int       @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id              Int       @mkp_connect_drupal_db.UnsignedInt
  revision_id            Int       @mkp_connect_drupal_db.UnsignedInt
  language               String    @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                  Int       @mkp_connect_drupal_db.UnsignedInt
  field_start_date_value DateTime? @mkp_connect_drupal_db.DateTime(0)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_start_date_value], map: "field_start_date_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_state_province {
  entity_type                 String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                      String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                     Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                   Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                 Int     @mkp_connect_drupal_db.UnsignedInt
  language                    String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                       Int     @mkp_connect_drupal_db.UnsignedInt
  field_state_province_value  String? @mkp_connect_drupal_db.VarChar(80)
  field_state_province_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_state_province_format], map: "field_state_province_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_tags {
  entity_type    String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle         String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted        Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id      Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id    Int    @mkp_connect_drupal_db.UnsignedInt
  language       String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta          Int    @mkp_connect_drupal_db.UnsignedInt
  field_tags_tid Int?   @mkp_connect_drupal_db.UnsignedInt

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_tags_tid], map: "field_tags_tid")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_talk_group {
  entity_type            String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                 String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id              Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id            Int    @mkp_connect_drupal_db.UnsignedInt
  language               String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                  Int    @mkp_connect_drupal_db.UnsignedInt
  field_talk_group_value Int?

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_talk_group_old {
  entity_type            String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                 String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id              Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id            Int    @mkp_connect_drupal_db.UnsignedInt
  language               String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                  Int    @mkp_connect_drupal_db.UnsignedInt
  field_talk_group_value Int?

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_term_end_date {
  entity_type               String    @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                    String    @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                   Int       @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                 Int       @mkp_connect_drupal_db.UnsignedInt
  revision_id               Int       @mkp_connect_drupal_db.UnsignedInt
  language                  String    @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                     Int       @mkp_connect_drupal_db.UnsignedInt
  field_term_end_date_value DateTime? @mkp_connect_drupal_db.DateTime(0)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_term_end_date_value], map: "field_term_end_date_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_term_start_date {
  entity_type                 String    @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                      String    @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                     Int       @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                   Int       @mkp_connect_drupal_db.UnsignedInt
  revision_id                 Int       @mkp_connect_drupal_db.UnsignedInt
  language                    String    @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                       Int       @mkp_connect_drupal_db.UnsignedInt
  field_term_start_date_value DateTime? @mkp_connect_drupal_db.DateTime(0)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_term_start_date_value], map: "field_term_start_date_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_tinyurl {
  entity_type          String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle               String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted              Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id            Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id          Int     @mkp_connect_drupal_db.UnsignedInt
  language             String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                Int     @mkp_connect_drupal_db.UnsignedInt
  field_tinyurl_value  String? @mkp_connect_drupal_db.VarChar(100)
  field_tinyurl_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_tinyurl_format], map: "field_tinyurl_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_topic {
  entity_type       String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle            String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted           Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id         Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id       Int     @mkp_connect_drupal_db.UnsignedInt
  language          String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta             Int     @mkp_connect_drupal_db.UnsignedInt
  field_topic_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_topic_value], map: "field_topic_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_training_type {
  entity_type                String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                     String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                    Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                  Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                Int     @mkp_connect_drupal_db.UnsignedInt
  language                   String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                      Int     @mkp_connect_drupal_db.UnsignedInt
  field_training_type_value  String? @mkp_connect_drupal_db.VarChar(40)
  field_training_type_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_training_type_format], map: "field_training_type_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_type {
  entity_type      String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle           String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted          Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id        Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id      Int     @mkp_connect_drupal_db.UnsignedInt
  language         String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta            Int     @mkp_connect_drupal_db.UnsignedInt
  field_type_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_type_value], map: "field_type_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_upload_csv {
  entity_type                  String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                       String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                      Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                    Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                  Int     @mkp_connect_drupal_db.UnsignedInt
  language                     String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                        Int     @mkp_connect_drupal_db.UnsignedInt
  field_upload_csv_fid         Int?    @mkp_connect_drupal_db.UnsignedInt
  field_upload_csv_display     Int     @default(1) @mkp_connect_drupal_db.UnsignedTinyInt
  field_upload_csv_description String? @mkp_connect_drupal_db.Text

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_upload_csv_fid], map: "field_upload_csv_fid")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_vacc_rule {
  entity_type            String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                 String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id              Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id            Int     @mkp_connect_drupal_db.UnsignedInt
  language               String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                  Int     @mkp_connect_drupal_db.UnsignedInt
  field_vacc_rule_value  String? @mkp_connect_drupal_db.VarChar(10)
  field_vacc_rule_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_vacc_rule_format], map: "field_vacc_rule_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_venue {
  entity_type       String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle            String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted           Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id         Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id       Int     @mkp_connect_drupal_db.UnsignedInt
  language          String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta             Int     @mkp_connect_drupal_db.UnsignedInt
  field_venue_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_venue_value], map: "field_venue_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_vgroup_abbreviation {
  entity_type                      String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                           String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                          Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                        Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                      Int     @mkp_connect_drupal_db.UnsignedInt
  language                         String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                            Int     @mkp_connect_drupal_db.UnsignedInt
  field_vgroup_abbreviation_value  String? @mkp_connect_drupal_db.VarChar(20)
  field_vgroup_abbreviation_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_vgroup_abbreviation_format], map: "field_vgroup_abbreviation_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_wait_list {
  entity_type            String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                 String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id              Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id            Int     @mkp_connect_drupal_db.UnsignedInt
  language               String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                  Int     @mkp_connect_drupal_db.UnsignedInt
  field_wait_list_value  String? @mkp_connect_drupal_db.VarChar(2)
  field_wait_list_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_wait_list_format], map: "field_wait_list_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_waitlist {
  entity_type          String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle               String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted              Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id            Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id          Int    @mkp_connect_drupal_db.UnsignedInt
  language             String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                Int    @mkp_connect_drupal_db.UnsignedInt
  field_waitlist_value Int?

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_waitlist_value], map: "field_waitlist_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_what {
  entity_type      String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle           String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted          Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id        Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id      Int     @mkp_connect_drupal_db.UnsignedInt
  language         String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta            Int     @mkp_connect_drupal_db.UnsignedInt
  field_what_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_what_value], map: "field_what_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_who {
  entity_type     String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle          String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted         Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id       Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id     Int     @mkp_connect_drupal_db.UnsignedInt
  language        String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta           Int     @mkp_connect_drupal_db.UnsignedInt
  field_who_value String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_who_value], map: "field_who_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_wp_link {
  entity_type          String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle               String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted              Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id            Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id          Int     @mkp_connect_drupal_db.UnsignedInt
  language             String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                Int     @mkp_connect_drupal_db.UnsignedInt
  field_wp_link_value  String? @mkp_connect_drupal_db.VarChar(80)
  field_wp_link_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([field_wp_link_format], map: "field_wp_link_format")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_field_zoom {
  entity_type      String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle           String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted          Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id        Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id      Int    @mkp_connect_drupal_db.UnsignedInt
  language         String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta            Int    @mkp_connect_drupal_db.UnsignedInt
  field_zoom_value Int?

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_group_access {
  entity_type        String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle             String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted            Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id          Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id        Int    @mkp_connect_drupal_db.UnsignedInt
  language           String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta              Int    @mkp_connect_drupal_db.UnsignedInt
  group_access_value Int?

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([group_access_value], map: "group_access_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_group_group {
  entity_type       String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle            String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted           Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id         Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id       Int    @mkp_connect_drupal_db.UnsignedInt
  language          String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta             Int    @mkp_connect_drupal_db.UnsignedInt
  group_group_value Int?

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([group_group_value], map: "group_group_value")
  @@index([language], map: "language")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_migrate_example_beer_styles {
  entity_type                     String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                          String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                         Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                       Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id                     Int    @mkp_connect_drupal_db.UnsignedInt
  language                        String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                           Int    @mkp_connect_drupal_db.UnsignedInt
  migrate_example_beer_styles_tid Int?   @mkp_connect_drupal_db.UnsignedInt

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([migrate_example_beer_styles_tid], map: "migrate_example_beer_styles_tid")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_migrate_example_wine_best_with {
  entity_type                        String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                             String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                            Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                          Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id                        Int    @mkp_connect_drupal_db.UnsignedInt
  language                           String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                              Int    @mkp_connect_drupal_db.UnsignedInt
  migrate_example_wine_best_with_tid Int?   @mkp_connect_drupal_db.UnsignedInt

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([migrate_example_wine_best_with_tid], map: "migrate_example_wine_best_with_tid")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_migrate_example_wine_regions {
  entity_type                      String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                           String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                          Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                        Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id                      Int    @mkp_connect_drupal_db.UnsignedInt
  language                         String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                            Int    @mkp_connect_drupal_db.UnsignedInt
  migrate_example_wine_regions_tid Int?   @mkp_connect_drupal_db.UnsignedInt

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([migrate_example_wine_regions_tid], map: "migrate_example_wine_regions_tid")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_migrate_example_wine_varieties {
  entity_type                        String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                             String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                            Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                          Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id                        Int    @mkp_connect_drupal_db.UnsignedInt
  language                           String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                              Int    @mkp_connect_drupal_db.UnsignedInt
  migrate_example_wine_varieties_tid Int?   @mkp_connect_drupal_db.UnsignedInt

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([migrate_example_wine_varieties_tid], map: "migrate_example_wine_varieties_tid")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_og_membership_request {
  entity_type                  String  @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                       String  @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                      Int     @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                    Int     @mkp_connect_drupal_db.UnsignedInt
  revision_id                  Int     @mkp_connect_drupal_db.UnsignedInt
  language                     String  @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                        Int     @mkp_connect_drupal_db.UnsignedInt
  og_membership_request_value  String? @mkp_connect_drupal_db.LongText
  og_membership_request_format String? @mkp_connect_drupal_db.VarChar(255)

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([og_membership_request_format], map: "og_membership_request_format")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_og_roles_permissions {
  entity_type                String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                     String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                    Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id                  Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id                Int    @mkp_connect_drupal_db.UnsignedInt
  language                   String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                      Int    @mkp_connect_drupal_db.UnsignedInt
  og_roles_permissions_value Int?

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([og_roles_permissions_value], map: "og_roles_permissions_value")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model field_revision_og_user_node {
  entity_type            String @default("") @mkp_connect_drupal_db.VarChar(128)
  bundle                 String @default("") @mkp_connect_drupal_db.VarChar(128)
  deleted                Int    @default(0) @mkp_connect_drupal_db.TinyInt
  entity_id              Int    @mkp_connect_drupal_db.UnsignedInt
  revision_id            Int    @mkp_connect_drupal_db.UnsignedInt
  language               String @default("") @mkp_connect_drupal_db.VarChar(32)
  delta                  Int    @mkp_connect_drupal_db.UnsignedInt
  og_user_node_target_id Int    @mkp_connect_drupal_db.UnsignedInt

  @@id([entity_type, entity_id, revision_id, deleted, delta, language])
  @@index([bundle], map: "bundle")
  @@index([deleted], map: "deleted")
  @@index([entity_id], map: "entity_id")
  @@index([entity_type], map: "entity_type")
  @@index([language], map: "language")
  @@index([og_user_node_target_id], map: "og_user_node_target_id")
  @@index([revision_id], map: "revision_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model file_managed {
  fid       Int    @id @default(autoincrement()) @mkp_connect_drupal_db.UnsignedInt
  uid       Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  filename  String @default("") @mkp_connect_drupal_db.VarChar(255)
  uri       String @unique(map: "uri") @default("") @mkp_connect_drupal_db.VarChar(255)
  filemime  String @default("") @mkp_connect_drupal_db.VarChar(255)
  filesize  BigInt @default(0) @mkp_connect_drupal_db.UnsignedBigInt
  status    Int    @default(0) @mkp_connect_drupal_db.TinyInt
  timestamp Int    @default(0) @mkp_connect_drupal_db.UnsignedInt

  @@index([status], map: "status")
  @@index([timestamp], map: "timestamp")
  @@index([uid], map: "uid")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model file_managed_copy1 {
  fid       Int    @id @default(autoincrement()) @mkp_connect_drupal_db.UnsignedInt
  uid       Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  filename  String @default("") @mkp_connect_drupal_db.VarChar(255)
  uri       String @unique(map: "uri") @default("") @mkp_connect_drupal_db.VarChar(255)
  filemime  String @default("") @mkp_connect_drupal_db.VarChar(255)
  filesize  BigInt @default(0) @mkp_connect_drupal_db.UnsignedBigInt
  status    Int    @default(0) @mkp_connect_drupal_db.TinyInt
  timestamp Int    @default(0) @mkp_connect_drupal_db.UnsignedInt

  @@index([status], map: "status")
  @@index([timestamp], map: "timestamp")
  @@index([uid], map: "uid")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model file_usage {
  fid    Int    @mkp_connect_drupal_db.UnsignedInt
  module String @default("") @mkp_connect_drupal_db.VarChar(255)
  type   String @default("") @mkp_connect_drupal_db.VarChar(64)
  id     Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  count  Int    @default(0) @mkp_connect_drupal_db.UnsignedInt

  @@id([fid, type, id, module])
  @@index([fid, count], map: "fid_count")
  @@index([fid, module], map: "fid_module")
  @@index([type, id], map: "type_id")
}

model fillpdf_fields {
  fid          Int     @mkp_connect_drupal_db.UnsignedInt
  pdf_key      String  @mkp_connect_drupal_db.VarChar(255)
  label        String  @mkp_connect_drupal_db.VarChar(255)
  prefix       String? @mkp_connect_drupal_db.VarChar(4096)
  value        String  @mkp_connect_drupal_db.MediumText
  suffix       String? @mkp_connect_drupal_db.VarChar(4096)
  replacements String? @mkp_connect_drupal_db.Text

  @@id([fid, pdf_key])
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model fillpdf_file_context {
  fcid    Int     @id @default(autoincrement()) @mkp_connect_drupal_db.UnsignedInt
  context String? @mkp_connect_drupal_db.Text
  fid     Int     @mkp_connect_drupal_db.UnsignedInt
}

model fillpdf_forms {
  fid                  Int     @id @mkp_connect_drupal_db.UnsignedInt
  title                String  @mkp_connect_drupal_db.VarChar(512)
  default_nid          String? @mkp_connect_drupal_db.VarChar(255)
  url                  String  @mkp_connect_drupal_db.VarChar(255)
  destination_path     String? @mkp_connect_drupal_db.VarChar(255)
  replacements         String? @mkp_connect_drupal_db.Text
  destination_redirect Int?    @mkp_connect_drupal_db.UnsignedInt
  admin_title          String? @mkp_connect_drupal_db.VarChar(512)
  scheme               String? @mkp_connect_drupal_db.VarChar(64)
  default_entity_type  String? @mkp_connect_drupal_db.VarChar(255)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model filter {
  format   String @mkp_connect_drupal_db.VarChar(255)
  module   String @default("") @mkp_connect_drupal_db.VarChar(64)
  name     String @default("") @mkp_connect_drupal_db.VarChar(32)
  weight   Int    @default(0)
  status   Int    @default(0)
  settings Bytes?

  @@id([format, name])
  @@index([weight, module, name], map: "list")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model filter_format {
  format String @id @mkp_connect_drupal_db.VarChar(255)
  name   String @unique(map: "name") @default("") @mkp_connect_drupal_db.VarChar(255)
  cache  Int    @default(0) @mkp_connect_drupal_db.TinyInt
  status Int    @default(1) @mkp_connect_drupal_db.UnsignedTinyInt
  weight Int    @default(0)

  @@index([status, weight], map: "status_weight")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model flood {
  fid        Int    @id @default(autoincrement())
  event      String @default("") @mkp_connect_drupal_db.VarChar(64)
  identifier String @default("") @mkp_connect_drupal_db.VarChar(128)
  timestamp  Int    @default(0)
  expiration Int    @default(0)

  @@index([event, identifier, timestamp], map: "allow")
  @@index([expiration], map: "purge")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model front_page {
  rid           Int    @id @mkp_connect_drupal_db.UnsignedInt
  mode          String @default("") @mkp_connect_drupal_db.VarChar(10)
  data          String @mkp_connect_drupal_db.LongText
  filter_format String @default("") @mkp_connect_drupal_db.VarChar(255)
  weight        Int?   @default(0)

  @@index([weight], map: "weight")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model geofield_update {
  id        Int    @id @default(autoincrement()) @mkp_connect_drupal_db.UnsignedInt
  bundle    String @mkp_connect_drupal_db.VarChar(32)
  entity_id Int    @mkp_connect_drupal_db.UnsignedInt
  updated   Int    @default(0) @mkp_connect_drupal_db.TinyInt
  created   Int    @mkp_connect_drupal_db.UnsignedInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model grc_leaders {
  id                             Int       @id @default(0) @mkp_connect_drupal_db.UnsignedInt
  uf_id                          Int?
  entity_id                      Int       @mkp_connect_drupal_db.UnsignedInt
  grc_lead_man_status_789        String?   @mkp_connect_drupal_db.VarChar(255)
  rec_date_790                   DateTime? @mkp_connect_drupal_db.DateTime(0)
  sort_name                      String?   @mkp_connect_drupal_db.VarChar(128)
  current_center_1               String?   @mkp_connect_drupal_db.VarChar(30)
  email                          String?   @mkp_connect_drupal_db.VarChar(254)
  nwta_leader_team_agreement_773 DateTime? @mkp_connect_drupal_db.DateTime(0)
  phone                          String?   @mkp_connect_drupal_db.VarChar(32)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model guts_instructors {
  id                                 Int     @id @default(autoincrement())
  entity_id                          Int     @mkp_connect_drupal_db.UnsignedInt
  uf_id                              Int     @mkp_connect_drupal_db.UnsignedInt
  sort_name                          String? @mkp_connect_drupal_db.VarChar(128)
  guts_instructor_classification_617 String? @mkp_connect_drupal_db.VarChar(255)
  guts_instructor_status_618         String? @mkp_connect_drupal_db.VarChar(255)
  current_center_1                   String? @mkp_connect_drupal_db.VarChar(30)
  email                              String? @mkp_connect_drupal_db.VarChar(254)
  phone                              String? @mkp_connect_drupal_db.VarChar(32)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model history {
  uid       Int @default(0)
  nid       Int @default(0) @mkp_connect_drupal_db.UnsignedInt
  timestamp Int @default(0)

  @@id([uid, nid])
  @@index([nid], map: "nid")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model igft_instructors {
  id                            Int       @id @default(autoincrement())
  uf_id                         Int       @mkp_connect_drupal_db.UnsignedInt
  entity_id                     Int       @mkp_connect_drupal_db.UnsignedInt
  sort_name                     String?   @mkp_connect_drupal_db.VarChar(128)
  instructor_status_548         String?   @mkp_connect_drupal_db.VarChar(255)
  instructor_classification_549 String?   @mkp_connect_drupal_db.VarChar(255)
  certification_date_550        DateTime? @mkp_connect_drupal_db.DateTime(0)
  email                         String?   @mkp_connect_drupal_db.VarChar(254)
  phone                         String?   @mkp_connect_drupal_db.VarChar(32)
  current_center_1              String?   @mkp_connect_drupal_db.VarChar(60)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model image_effects {
  ieid   Int    @id @default(autoincrement()) @mkp_connect_drupal_db.UnsignedInt
  isid   Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  weight Int    @default(0)
  name   String @mkp_connect_drupal_db.VarChar(255)
  data   Bytes

  @@index([isid], map: "isid")
  @@index([weight], map: "weight")
}

model image_location {
  id       Int     @id @default(autoincrement())
  code     String? @mkp_connect_drupal_db.VarChar(10)
  location String? @mkp_connect_drupal_db.VarChar(100)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model image_styles {
  isid  Int    @id @default(autoincrement()) @mkp_connect_drupal_db.UnsignedInt
  name  String @unique(map: "name") @mkp_connect_drupal_db.VarChar(255)
  label String @default("") @mkp_connect_drupal_db.VarChar(255)
}

model initiate_payments_summary {
  civi_event Int      @default(0)
  contact_id Int      @default(0)
  payments   Decimal? @mkp_connect_drupal_db.Decimal(32, 0)

  @@id([civi_event, contact_id])
}

model initiate_payments_summary_copy1 {
  civi_event Int      @default(0)
  contact_id Int      @default(0)
  payments   Decimal? @mkp_connect_drupal_db.Decimal(32, 0)

  @@id([civi_event, contact_id])
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model invite {
  iid      Int    @id @default(autoincrement()) @mkp_connect_drupal_db.UnsignedInt
  reg_code String @default("") @mkp_connect_drupal_db.VarChar(10)
  type     String @default("") @mkp_connect_drupal_db.VarChar(255)
  uid      Int
  invitee  Int
  status   Int    @default(1)
  created  Int    @default(0)
  expiry   Int    @default(0)
  joined   Int    @default(0)
  canceled Int    @default(0)
  data     String @mkp_connect_drupal_db.Text

  @@index([invitee], map: "invitee")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model invite_og {
  email    String?   @mkp_connect_drupal_db.VarChar(80)
  group_id Int?
  date     DateTime? @mkp_connect_drupal_db.DateTime(0)

  @@ignore
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model invite_sending_controller {
  type   String @mkp_connect_drupal_db.VarChar(32)
  module String @default("") @mkp_connect_drupal_db.VarChar(255)
  name   String @mkp_connect_drupal_db.VarChar(32)

  @@unique([type, name], map: "invite_type_controller")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model invite_type {
  itid        Int     @id @default(autoincrement())
  type        String  @unique(map: "type") @mkp_connect_drupal_db.VarChar(32)
  label       String  @default("") @mkp_connect_drupal_db.VarChar(255)
  description String  @mkp_connect_drupal_db.MediumText
  data        String  @mkp_connect_drupal_db.Text
  status      Int     @default(1) @mkp_connect_drupal_db.TinyInt
  module      String? @mkp_connect_drupal_db.VarChar(255)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model job_schedule {
  item_id   Int    @id @default(autoincrement()) @mkp_connect_drupal_db.UnsignedInt
  name      String @default("") @mkp_connect_drupal_db.VarChar(128)
  type      String @default("") @mkp_connect_drupal_db.VarChar(128)
  id        Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  period    Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  crontab   String @default("") @mkp_connect_drupal_db.VarChar(255)
  data      Bytes?
  expire    Int    @default(0)
  last      Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  periodic  Int    @default(0) @mkp_connect_drupal_db.UnsignedSmallInt
  next      Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  scheduled Int    @default(0) @mkp_connect_drupal_db.UnsignedInt

  @@index([name, type], map: "name_type")
  @@index([name, type, id], map: "name_type_id")
  @@index([next], map: "next")
  @@index([scheduled], map: "scheduled")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model js_injector_rule {
  name                  String? @unique(map: "name") @mkp_connect_drupal_db.VarChar(255)
  crid                  Int     @id @default(autoincrement()) @mkp_connect_drupal_db.UnsignedInt
  admin_description     String? @mkp_connect_drupal_db.VarChar(255)
  js                    String? @mkp_connect_drupal_db.LongText
  position              String? @mkp_connect_drupal_db.VarChar(255)
  preprocess            Int     @default(0)
  inline                Int     @default(0)
  page_visibility       Int     @default(0)
  page_visibility_pages String? @mkp_connect_drupal_db.LongText
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model leader_trainer {
  id                            Int       @id @default(0) @mkp_connect_drupal_db.UnsignedInt
  entity_id                     Int       @mkp_connect_drupal_db.UnsignedInt
  uid                           Int?
  sort_name                     String?   @mkp_connect_drupal_db.VarChar(128)
  area                          String?   @mkp_connect_drupal_db.VarChar(30)
  email                         String?   @mkp_connect_drupal_db.VarChar(254)
  city                          String?   @mkp_connect_drupal_db.VarChar(64)
  state                         String?   @mkp_connect_drupal_db.VarChar(64)
  country                       String?   @mkp_connect_drupal_db.VarChar(64)
  leader_trainer_status_409     String?   @mkp_connect_drupal_db.VarChar(20)
  ilc_regional_leader_body_410  String?   @mkp_connect_drupal_db.VarChar(40)
  data_updated_411              DateTime? @mkp_connect_drupal_db.DateTime(0)
  trainer_dues_paid_through_412 DateTime? @mkp_connect_drupal_db.DateTime(0)
  last_cert_date_414            DateTime? @mkp_connect_drupal_db.DateTime(0)
  recert_due_415                DateTime? @mkp_connect_drupal_db.DateTime(0)
  languages_spoken_419          String?   @mkp_connect_drupal_db.VarChar(255)
  leader_trainer_mentor_420     String?   @mkp_connect_drupal_db.VarChar(80)
  leader_trainings_approved_431 String?   @mkp_connect_drupal_db.VarChar(255)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model legal_accepted {
  legal_id Int    @id @default(autoincrement())
  version  Int    @default(0)
  revision Int    @default(0)
  language String @default("") @mkp_connect_drupal_db.VarChar(12)
  uid      Int    @default(0)
  accepted Int    @default(0)
  tc_id    Int    @default(0)

  @@index([uid], map: "uid")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model legal_conditions {
  tc_id      Int     @id @default(autoincrement())
  version    Int     @default(0)
  revision   Int     @default(0)
  language   String  @default("") @mkp_connect_drupal_db.VarChar(12)
  conditions String  @mkp_connect_drupal_db.LongText
  date       Int     @default(0)
  extras     String? @mkp_connect_drupal_db.Text
  changes    String? @mkp_connect_drupal_db.Text
  format     String? @mkp_connect_drupal_db.VarChar(255)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model location {
  lid         Int     @id @default(autoincrement()) @mkp_connect_drupal_db.UnsignedInt
  name        String  @default("") @mkp_connect_drupal_db.VarChar(255)
  street      String  @default("") @mkp_connect_drupal_db.VarChar(255)
  additional  String  @default("") @mkp_connect_drupal_db.VarChar(255)
  city        String  @default("") @mkp_connect_drupal_db.VarChar(255)
  province    String  @default("") @mkp_connect_drupal_db.VarChar(16)
  postal_code String  @default("") @mkp_connect_drupal_db.VarChar(16)
  country     String  @default("") @mkp_connect_drupal_db.Char(2)
  latitude    Decimal @default(0.000000) @mkp_connect_drupal_db.Decimal(10, 6)
  longitude   Decimal @default(0.000000) @mkp_connect_drupal_db.Decimal(10, 6)
  source      Int     @default(0) @mkp_connect_drupal_db.TinyInt
  is_primary  Int     @default(0) @mkp_connect_drupal_db.TinyInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model location_country {
  code String @id @mkp_connect_drupal_db.Char(2)
  name String @default("") @mkp_connect_drupal_db.VarChar(255)
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model location_instance {
  nid   Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  vid   Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  uid   Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  genid String @default("") @mkp_connect_drupal_db.VarChar(255)
  lid   Int    @default(0) @mkp_connect_drupal_db.UnsignedInt

  @@index([genid], map: "genid")
  @@index([lid], map: "lid")
  @@index([nid], map: "nid")
  @@index([uid], map: "uid")
  @@index([vid], map: "vid")
  @@ignore
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model maillog {
  header_message_id String @default("") @mkp_connect_drupal_db.VarChar(255)
  header_from       String @mkp_connect_drupal_db.Text
  header_to         String @mkp_connect_drupal_db.Text
  header_reply_to   String @mkp_connect_drupal_db.Text
  header_all        String @mkp_connect_drupal_db.Text
  subject           String @default("") @mkp_connect_drupal_db.VarChar(255)
  body              String @mkp_connect_drupal_db.LongText
  sent_date         Int    @default(0)
  id                Int    @id @default(autoincrement()) @mkp_connect_drupal_db.UnsignedInt
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model masquerade {
  sid      String @default("") @mkp_connect_drupal_db.VarChar(64)
  uid_from Int    @default(0)
  uid_as   Int    @default(0)

  @@index([sid, uid_from], map: "sid")
  @@index([sid, uid_as], map: "sid_2")
  @@ignore
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model masquerade_users {
  uid_from Int @default(0)
  uid_to   Int @default(0)

  @@id([uid_from, uid_to])
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model menu_custom {
  menu_name   String  @id @default("") @mkp_connect_drupal_db.VarChar(32)
  title       String  @default("") @mkp_connect_drupal_db.VarChar(255)
  description String? @mkp_connect_drupal_db.Text
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model menu_links {
  menu_name    String @default("") @mkp_connect_drupal_db.VarChar(32)
  mlid         Int    @id @default(autoincrement()) @mkp_connect_drupal_db.UnsignedInt
  plid         Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  link_path    String @default("") @mkp_connect_drupal_db.VarChar(255)
  router_path  String @default("") @mkp_connect_drupal_db.VarChar(255)
  link_title   String @default("") @mkp_connect_drupal_db.VarChar(255)
  options      Bytes? @mkp_connect_drupal_db.Blob
  module       String @default("system") @mkp_connect_drupal_db.VarChar(255)
  hidden       Int    @default(0) @mkp_connect_drupal_db.SmallInt
  external     Int    @default(0) @mkp_connect_drupal_db.SmallInt
  has_children Int    @default(0) @mkp_connect_drupal_db.SmallInt
  expanded     Int    @default(0) @mkp_connect_drupal_db.SmallInt
  weight       Int    @default(0)
  depth        Int    @default(0) @mkp_connect_drupal_db.SmallInt
  customized   Int    @default(0) @mkp_connect_drupal_db.SmallInt
  p1           Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  p2           Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  p3           Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  p4           Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  p5           Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  p6           Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  p7           Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  p8           Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  p9           Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  updated      Int    @default(0) @mkp_connect_drupal_db.SmallInt

  @@index([menu_name, p1, p2, p3, p4, p5, p6, p7, p8, p9], map: "menu_parents")
  @@index([menu_name, plid, expanded, has_children], map: "menu_plid_expand_child")
  @@index([link_path(length: 128), menu_name], map: "path_menu")
  @@index([router_path(length: 128)], map: "router_path")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model menu_links_visibility_role {
  mlid Int @mkp_connect_drupal_db.UnsignedInt
  rid  Int @mkp_connect_drupal_db.UnsignedInt

  @@id([mlid, rid])
  @@index([rid], map: "rid")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model menu_router {
  path              String  @id @default("") @mkp_connect_drupal_db.VarChar(255)
  load_functions    Bytes   @mkp_connect_drupal_db.Blob
  to_arg_functions  Bytes   @mkp_connect_drupal_db.Blob
  access_callback   String  @default("") @mkp_connect_drupal_db.VarChar(255)
  access_arguments  Bytes?  @mkp_connect_drupal_db.Blob
  page_callback     String  @default("") @mkp_connect_drupal_db.VarChar(255)
  page_arguments    Bytes?  @mkp_connect_drupal_db.Blob
  delivery_callback String  @default("") @mkp_connect_drupal_db.VarChar(255)
  fit               Int     @default(0)
  number_parts      Int     @default(0) @mkp_connect_drupal_db.SmallInt
  context           Int     @default(0)
  tab_parent        String  @default("") @mkp_connect_drupal_db.VarChar(255)
  tab_root          String  @default("") @mkp_connect_drupal_db.VarChar(255)
  title             String  @default("") @mkp_connect_drupal_db.VarChar(255)
  title_callback    String  @default("") @mkp_connect_drupal_db.VarChar(255)
  title_arguments   String  @default("") @mkp_connect_drupal_db.VarChar(255)
  theme_callback    String  @default("") @mkp_connect_drupal_db.VarChar(255)
  theme_arguments   String  @default("") @mkp_connect_drupal_db.VarChar(255)
  type              Int     @default(0)
  description       String  @mkp_connect_drupal_db.Text
  position          String  @default("") @mkp_connect_drupal_db.VarChar(255)
  weight            Int     @default(0)
  include_file      String? @mkp_connect_drupal_db.MediumText

  @@index([fit], map: "fit")
  @@index([tab_parent(length: 64), weight, title], map: "tab_parent")
  @@index([tab_root(length: 64), weight, title], map: "tab_root_weight_title")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model messaging_destination {
  mdid    Int    @id @default(autoincrement()) @mkp_connect_drupal_db.UnsignedInt
  uid     Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  type    String @default("") @mkp_connect_drupal_db.VarChar(60)
  address String @default("") @mkp_connect_drupal_db.VarChar(255)
  status  Int    @default(1) @mkp_connect_drupal_db.TinyInt
  sent    Int    @default(0)

  @@index([address], map: "address")
  @@index([uid], map: "uid")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model messaging_message {
  msid     Int    @id @default(autoincrement()) @mkp_connect_drupal_db.UnsignedInt
  method   String @default("") @mkp_connect_drupal_db.VarChar(255)
  store    String @default("") @mkp_connect_drupal_db.VarChar(255)
  mqid     Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  language String @default("") @mkp_connect_drupal_db.VarChar(12)
  status   Int    @default(1) @mkp_connect_drupal_db.TinyInt
  created  Int    @default(0)
  updated  Int    @default(0)
  sent     Int    @default(0)
  error    Int    @default(0) @mkp_connect_drupal_db.TinyInt

  @@index([method], map: "method")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model node {
  nid       Int    @id @default(autoincrement()) @mkp_connect_drupal_db.UnsignedInt
  vid       Int?   @unique(map: "vid") @mkp_connect_drupal_db.UnsignedInt
  type      String @default("") @mkp_connect_drupal_db.VarChar(32)
  language  String @default("") @mkp_connect_drupal_db.VarChar(12)
  title     String @default("") @mkp_connect_drupal_db.VarChar(255)
  uid       Int    @default(0)
  status    Int    @default(1)
  created   Int    @default(0)
  changed   Int    @default(0)
  comment   Int    @default(0)
  promote   Int    @default(0)
  sticky    Int    @default(0)
  tnid      Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  translate Int    @default(0)

  @@index([language], map: "language")
  @@index([changed], map: "node_changed")
  @@index([created], map: "node_created")
  @@index([promote, status, sticky, created], map: "node_frontpage")
  @@index([status, type, nid], map: "node_status_type")
  @@index([title, type(length: 4)], map: "node_title_type")
  @@index([type(length: 4)], map: "node_type")
  @@index([tnid], map: "tnid")
  @@index([translate], map: "translate")
  @@index([uid], map: "uid")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model node_access {
  nid          Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  gid          Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  realm        String @default("") @mkp_connect_drupal_db.VarChar(255)
  grant_view   Int    @default(0) @mkp_connect_drupal_db.UnsignedTinyInt
  grant_update Int    @default(0) @mkp_connect_drupal_db.UnsignedTinyInt
  grant_delete Int    @default(0) @mkp_connect_drupal_db.UnsignedTinyInt

  @@id([nid, gid, realm])
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model node_convert_templates {
  nctid            Int     @id @default(autoincrement()) @mkp_connect_drupal_db.UnsignedInt
  machine_name     String  @default("") @mkp_connect_drupal_db.VarChar(255)
  name             String? @mkp_connect_drupal_db.TinyText
  source_type      String? @mkp_connect_drupal_db.TinyText
  destination_type String  @mkp_connect_drupal_db.TinyText
  data             String? @mkp_connect_drupal_db.MediumText
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model node_copy {
  nid       Int    @id @mkp_connect_drupal_db.UnsignedInt
  vid       Int    @unique(map: "vid") @default(autoincrement()) @mkp_connect_drupal_db.UnsignedInt
  type      String @default("") @mkp_connect_drupal_db.VarChar(32)
  language  String @default("") @mkp_connect_drupal_db.VarChar(12)
  title     String @default("") @mkp_connect_drupal_db.VarChar(255)
  uid       Int    @default(0)
  status    Int    @default(1)
  created   Int    @default(0)
  changed   Int    @default(0)
  comment   Int    @default(0)
  promote   Int    @default(0)
  sticky    Int    @default(0)
  tnid      Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  translate Int    @default(0)
  uuid      String @default("") @mkp_connect_drupal_db.Char(36)

  @@index([language], map: "language")
  @@index([changed], map: "node_changed")
  @@index([created], map: "node_created")
  @@index([promote, status, sticky, created], map: "node_frontpage")
  @@index([status, type, nid], map: "node_status_type")
  @@index([title, type(length: 4)], map: "node_title_type")
  @@index([type(length: 4)], map: "node_type")
  @@index([tnid], map: "tnid")
  @@index([translate], map: "translate")
  @@index([uid], map: "uid")
  @@index([uuid], map: "uuid")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model node_copy1 {
  nid       Int    @id @default(autoincrement()) @mkp_connect_drupal_db.UnsignedInt
  vid       Int?   @unique(map: "vid") @mkp_connect_drupal_db.UnsignedInt
  type      String @default("") @mkp_connect_drupal_db.VarChar(32)
  language  String @default("") @mkp_connect_drupal_db.VarChar(12)
  title     String @default("") @mkp_connect_drupal_db.VarChar(255)
  uid       Int    @default(0)
  status    Int    @default(1)
  created   Int    @default(0)
  changed   Int    @default(0)
  comment   Int    @default(0)
  promote   Int    @default(0)
  sticky    Int    @default(0)
  tnid      Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  translate Int    @default(0)

  @@index([language], map: "language")
  @@index([changed], map: "node_changed")
  @@index([created], map: "node_created")
  @@index([promote, status, sticky, created], map: "node_frontpage")
  @@index([status, type, nid], map: "node_status_type")
  @@index([title, type(length: 4)], map: "node_title_type")
  @@index([type(length: 4)], map: "node_type")
  @@index([tnid], map: "tnid")
  @@index([translate], map: "translate")
  @@index([uid], map: "uid")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model node_counter {
  nid        Int    @id @default(0)
  totalcount BigInt @default(0) @mkp_connect_drupal_db.UnsignedBigInt
  daycount   Int    @default(0) @mkp_connect_drupal_db.UnsignedMediumInt
  timestamp  Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model node_resolution {
  serid     Int    @id @unique(map: "serid") @default(autoincrement()) @mkp_connect_drupal_db.UnsignedInt
  sni_nid   Int    @mkp_connect_drupal_db.UnsignedInt
  data      Bytes?
  reference String @default("") @mkp_connect_drupal_db.VarChar(128)
  status    String @default("0") @mkp_connect_drupal_db.VarChar(50)
  created   Int    @default(0)
  changed   Int    @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model node_revision {
  nid       Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  vid       Int    @id @default(autoincrement()) @mkp_connect_drupal_db.UnsignedInt
  uid       Int    @default(0)
  title     String @default("") @mkp_connect_drupal_db.VarChar(255)
  log       String @mkp_connect_drupal_db.LongText
  timestamp Int    @default(0)
  status    Int    @default(1)
  comment   Int    @default(0)
  promote   Int    @default(0)
  sticky    Int    @default(0)

  @@index([nid], map: "nid")
  @@index([uid], map: "uid")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model node_type {
  type        String @id @mkp_connect_drupal_db.VarChar(32)
  name        String @default("") @mkp_connect_drupal_db.VarChar(255)
  base        String @mkp_connect_drupal_db.VarChar(255)
  module      String @mkp_connect_drupal_db.VarChar(255)
  description String @mkp_connect_drupal_db.MediumText
  help        String @mkp_connect_drupal_db.MediumText
  has_title   Int    @mkp_connect_drupal_db.UnsignedTinyInt
  title_label String @default("") @mkp_connect_drupal_db.VarChar(255)
  custom      Int    @default(0) @mkp_connect_drupal_db.TinyInt
  modified    Int    @default(0) @mkp_connect_drupal_db.TinyInt
  locked      Int    @default(0) @mkp_connect_drupal_db.TinyInt
  disabled    Int    @default(0) @mkp_connect_drupal_db.TinyInt
  orig_type   String @default("") @mkp_connect_drupal_db.VarChar(255)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model notifications_event {
  eid           Int    @id @default(autoincrement()) @mkp_connect_drupal_db.UnsignedInt
  type          String @default("") @mkp_connect_drupal_db.VarChar(255)
  action        String @default("") @mkp_connect_drupal_db.VarChar(255)
  created       Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  updated       Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  counter       Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  send_start    Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  send_end      Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  send_time     Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  notif_count   Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  notif_errors  Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  notif_sent    Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  notif_success Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  log           Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  data          Bytes?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model notifications_subscription {
  sid           Int     @id @default(autoincrement()) @mkp_connect_drupal_db.UnsignedInt
  uid           Int
  mdid          Int     @mkp_connect_drupal_db.UnsignedInt
  type          String  @default("") @mkp_connect_drupal_db.VarChar(255)
  event_type    String  @default("") @mkp_connect_drupal_db.VarChar(255)
  conditions    Int     @mkp_connect_drupal_db.UnsignedInt
  send_interval Int     @default(0)
  language      String  @default("") @mkp_connect_drupal_db.VarChar(255)
  send_method   String  @default("") @mkp_connect_drupal_db.VarChar(255)
  cron          Int     @default(0) @mkp_connect_drupal_db.UnsignedTinyInt
  module        String? @mkp_connect_drupal_db.VarChar(255)
  status        Int     @default(1)
  destination   String  @default("") @mkp_connect_drupal_db.VarChar(255)
  created       Int     @default(0) @mkp_connect_drupal_db.UnsignedInt
  updated       Int     @default(0) @mkp_connect_drupal_db.UnsignedInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model notifications_subscription_fields {
  sid      Int    @mkp_connect_drupal_db.UnsignedInt
  type     String @mkp_connect_drupal_db.VarChar(255)
  position Int    @default(0)
  value    String @mkp_connect_drupal_db.VarChar(255)
  intval   Int    @default(0)

  @@id([sid, type, position])
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model nwta_leaders {
  id                                 Int       @id @default(0) @mkp_connect_drupal_db.UnsignedInt
  entity_id                          Int       @mkp_connect_drupal_db.UnsignedInt
  current_status_566                 String?   @mkp_connect_drupal_db.VarChar(40)
  ilc_regional_leader_body_567       String?   @mkp_connect_drupal_db.VarChar(40)
  original_cert_decl_date_570        DateTime? @mkp_connect_drupal_db.DateTime(0)
  last_cert_hot_seat_date_571        DateTime? @mkp_connect_drupal_db.DateTime(0)
  cert_hot_seat_due_572              DateTime? @mkp_connect_drupal_db.DateTime(0)
  languages_spoken_573               String?   @mkp_connect_drupal_db.VarChar(255)
  mentor_name_575                    String?   @mkp_connect_drupal_db.VarChar(80)
  last_staffing_578                  DateTime? @mkp_connect_drupal_db.DateTime(0)
  nwta_staffings_579                 Int?
  st1_580                            String?   @mkp_connect_drupal_db.VarChar(80)
  st2_581                            String?   @mkp_connect_drupal_db.VarChar(80)
  lt1_582                            String?   @mkp_connect_drupal_db.VarChar(80)
  lt2_583                            String?   @mkp_connect_drupal_db.VarChar(80)
  lt3_584                            String?   @mkp_connect_drupal_db.VarChar(10)
  current_level_585                  String?   @mkp_connect_drupal_db.VarChar(255)
  data_updated_586                   DateTime? @mkp_connect_drupal_db.DateTime(0)
  dues_paid_through_589              DateTime? @mkp_connect_drupal_db.DateTime(0)
  note_590                           String?   @mkp_connect_drupal_db.VarChar(255)
  rapid_response_team__591           Int?      @mkp_connect_drupal_db.TinyInt
  leader_covenant_current__592       String?   @mkp_connect_drupal_db.VarChar(8)
  sign_language__593                 String?   @mkp_connect_drupal_db.VarChar(8)
  multicultural_training__594        String?   @mkp_connect_drupal_db.VarChar(8)
  i_group__596                       String?   @mkp_connect_drupal_db.VarChar(8)
  uid                                Int       @mkp_connect_drupal_db.UnsignedInt
  sort_name                          String?   @mkp_connect_drupal_db.VarChar(128)
  current_center_1                   String?   @mkp_connect_drupal_db.VarChar(30)
  email                              String?   @mkp_connect_drupal_db.VarChar(254)
  link_name                          String    @mkp_connect_drupal_db.VarChar(20)
  nwta_leader_team_agreement_773     DateTime? @mkp_connect_drupal_db.Date
  conflict_of_interest_statement_774 DateTime? @mkp_connect_drupal_db.Date
}

model og_ancestry {
  nid       Int
  group_nid Int

  @@id([nid, group_nid])
  @@index([group_nid], map: "group_nid")
}

model og_audit_status {
  id         Int     @id @default(autoincrement()) @mkp_connect_drupal_db.UnsignedInt
  Phase      String? @mkp_connect_drupal_db.VarChar(10)
  CurrentRow Int?
  TotalRows  Int?
  Nid        Int?
  Uid        Int?
  Comments   String? @mkp_connect_drupal_db.VarChar(255)
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model og_delete_these {
  id          Int     @default(0)
  groupId     Int
  memberKey   String? @mkp_connect_drupal_db.VarChar(30)
  memberEmail String? @mkp_connect_drupal_db.VarChar(255)
  nid         Int?
  uid         Int?
  role        String? @mkp_connect_drupal_db.VarChar(20)
  googleKey   String? @mkp_connect_drupal_db.VarChar(30)
  groupEmail  String? @mkp_connect_drupal_db.VarChar(255)

  @@ignore
}

model og_google_groups {
  nid              Int       @id
  email            String    @unique(map: "email") @mkp_connect_drupal_db.VarChar(255)
  groupKey         String    @mkp_connect_drupal_db.VarChar(255)
  type             String?   @mkp_connect_drupal_db.VarChar(20)
  dateCreated      DateTime? @default(now()) @mkp_connect_drupal_db.Timestamp(0)
  dateSynchronized DateTime? @mkp_connect_drupal_db.Timestamp(0)
  og_members       Int?
  gg_members       Int?
  oguid_members    Int?
}

model og_google_groups_copy1 {
  nid              Int       @id
  email            String    @unique(map: "email") @mkp_connect_drupal_db.VarChar(255)
  groupKey         String    @mkp_connect_drupal_db.VarChar(255)
  type             String?   @mkp_connect_drupal_db.VarChar(20)
  dateCreated      DateTime? @default(now()) @mkp_connect_drupal_db.Timestamp(0)
  dateSynchronized DateTime? @mkp_connect_drupal_db.Timestamp(0)
  og_members       Int?
  gg_members       Int?
  oguid_members    Int?
}

model og_google_groups_users {
  memberkey   String    @mkp_connect_drupal_db.VarChar(255)
  nid         Int
  uid         Int
  email       String?   @mkp_connect_drupal_db.VarChar(80)
  role        String?   @mkp_connect_drupal_db.VarChar(10)
  dateCreated DateTime? @default(now()) @mkp_connect_drupal_db.Timestamp(0)

  @@id([uid, nid])
  @@index([email], map: "memberemail")
  @@index([memberkey], map: "memberkey")
  @@index([nid], map: "nid")
  @@index([nid, uid], map: "nid_uid")
}

model og_google_groups_users_copy1 {
  memberkey   String    @mkp_connect_drupal_db.VarChar(255)
  nid         Int
  uid         Int
  email       String?   @mkp_connect_drupal_db.VarChar(80)
  role        String?   @mkp_connect_drupal_db.VarChar(10)
  dateCreated DateTime? @default(now()) @mkp_connect_drupal_db.Timestamp(0)

  @@id([uid, nid])
  @@index([email], map: "memberemail")
  @@index([memberkey], map: "memberkey")
  @@index([nid], map: "nid")
  @@index([nid, uid], map: "nid_uid")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model og_manual_add {
  nid   Int?
  uid   Int?
  email String? @mkp_connect_drupal_db.VarChar(80)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model og_manual_delete {
  nid       Int?
  uid       Int?
  email     String? @mkp_connect_drupal_db.VarChar(80)
  memberKey String? @mkp_connect_drupal_db.VarChar(40)
  groupKey  String? @mkp_connect_drupal_db.VarChar(40)

  @@ignore
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model og_membership {
  id          Int     @id @default(autoincrement()) @mkp_connect_drupal_db.UnsignedInt
  type        String  @default("") @mkp_connect_drupal_db.VarChar(255)
  etid        Int     @default(0) @mkp_connect_drupal_db.UnsignedInt
  entity_type String  @default("") @mkp_connect_drupal_db.VarChar(32)
  gid         Int
  group_type  String  @default("") @mkp_connect_drupal_db.VarChar(32)
  state       String? @default("") @mkp_connect_drupal_db.VarChar(255)
  created     Int     @default(0)
  field_name  String  @default("") @mkp_connect_drupal_db.VarChar(255)
  language    String  @default("") @mkp_connect_drupal_db.VarChar(12)

  @@index([etid, entity_type], map: "entity")
  @@index([gid, group_type], map: "group")
  @@index([group_type], map: "group_type")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model og_membership_admin {
  id   Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  role String @default("") @mkp_connect_drupal_db.VarChar(5)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model og_membership_admin_copy1 {
  id   Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  role String @default("") @mkp_connect_drupal_db.VarChar(5)

  @@ignore
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model og_membership_copy1 {
  id          Int     @id @default(autoincrement()) @mkp_connect_drupal_db.UnsignedInt
  type        String  @default("") @mkp_connect_drupal_db.VarChar(255)
  etid        Int     @default(0) @mkp_connect_drupal_db.UnsignedInt
  entity_type String  @default("") @mkp_connect_drupal_db.VarChar(32)
  gid         Int
  group_type  String  @default("") @mkp_connect_drupal_db.VarChar(32)
  state       String? @default("") @mkp_connect_drupal_db.VarChar(255)
  created     Int     @default(0)
  field_name  String  @default("") @mkp_connect_drupal_db.VarChar(255)
  language    String  @default("") @mkp_connect_drupal_db.VarChar(12)

  @@index([etid, entity_type], map: "entity")
  @@index([gid, group_type], map: "group")
  @@index([group_type], map: "group_type")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model og_membership_type {
  id          Int     @id @default(autoincrement())
  name        String  @unique(map: "name") @default("") @mkp_connect_drupal_db.VarChar(255)
  description String  @default("") @mkp_connect_drupal_db.VarChar(255)
  status      Int     @default(1) @mkp_connect_drupal_db.TinyInt
  module      String? @mkp_connect_drupal_db.VarChar(255)
  language    String  @default("") @mkp_connect_drupal_db.VarChar(12)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model og_role {
  rid          Int    @id @default(autoincrement()) @mkp_connect_drupal_db.UnsignedInt
  gid          Int
  group_type   String @default("") @mkp_connect_drupal_db.VarChar(255)
  group_bundle String @default("") @mkp_connect_drupal_db.VarChar(255)
  name         String @default("") @mkp_connect_drupal_db.VarChar(64)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model og_role_permission {
  rid        Int    @mkp_connect_drupal_db.UnsignedInt
  permission String @default("") @mkp_connect_drupal_db.VarChar(128)
  module     String @default("") @mkp_connect_drupal_db.VarChar(255)

  @@id([rid, permission])
  @@index([permission], map: "permission")
}

model og_synch_group {
  id               Int               @id @default(autoincrement())
  googleKey        String?           @mkp_connect_drupal_db.VarChar(30)
  nid              Int?
  groupEmail       String?           @mkp_connect_drupal_db.VarChar(255)
  dateSynchronized DateTime?         @mkp_connect_drupal_db.Timestamp(0)
  Count            String?           @mkp_connect_drupal_db.VarChar(255)
  og_synch_member  og_synch_member[]
}

model og_synch_group_copy1 {
  id               Int       @id @default(autoincrement())
  googleKey        String?   @mkp_connect_drupal_db.VarChar(30)
  nid              Int?
  groupEmail       String?   @mkp_connect_drupal_db.VarChar(255)
  dateSynchronized DateTime? @mkp_connect_drupal_db.Timestamp(0)
  Count            String?   @mkp_connect_drupal_db.VarChar(255)
}

model og_synch_member {
  id             Int            @id @default(autoincrement())
  groupId        Int
  memberKey      String?        @mkp_connect_drupal_db.VarChar(30)
  memberEmail    String?        @mkp_connect_drupal_db.VarChar(255)
  nid            Int?
  uid            Int?
  role           String?        @mkp_connect_drupal_db.VarChar(20)
  og_synch_group og_synch_group @relation(fields: [groupId], references: [id], onDelete: Cascade, onUpdate: Restrict, map: "og_synch_member_ibfk_1")

  @@index([groupId], map: "SynchGroup")
  @@index([memberKey], map: "memberkey")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model og_uid_temp {
  gid     Int
  members BigInt

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model og_uid_temp_1 {
  gid                   Int
  members               BigInt
  member_status_text_47 String? @mkp_connect_drupal_db.VarChar(5)

  @@ignore
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model og_users_roles {
  uid        Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  rid        Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  gid        Int
  group_type String @default("") @mkp_connect_drupal_db.VarChar(255)

  @@id([uid, rid, gid])
  @@index([rid], map: "rid")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model og_users_roles_copy1 {
  uid        Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  rid        Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  gid        Int
  group_type String @default("") @mkp_connect_drupal_db.VarChar(255)

  @@id([uid, rid, gid])
  @@index([rid], map: "rid")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model online_facilitators {
  id                            Int       @id @default(autoincrement())
  uf_id                         Int       @mkp_connect_drupal_db.UnsignedInt
  entity_id                     Int       @mkp_connect_drupal_db.UnsignedInt
  sort_name                     String?   @mkp_connect_drupal_db.VarChar(255)
  data_updated_647              DateTime? @mkp_connect_drupal_db.DateTime(0)
  recert_due_649                DateTime? @mkp_connect_drupal_db.DateTime(0)
  notes_655                     String?   @mkp_connect_drupal_db.VarChar(255)
  training_role_status_code_662 String?   @mkp_connect_drupal_db.VarChar(255)
  email                         String?   @mkp_connect_drupal_db.VarChar(254)
  phone                         String?   @mkp_connect_drupal_db.VarChar(32)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model page_theme {
  ptid   Int    @id @default(autoincrement()) @mkp_connect_drupal_db.UnsignedInt
  theme  String @unique(map: "theme") @default("") @mkp_connect_drupal_db.VarChar(255)
  pages  String @mkp_connect_drupal_db.Text
  status Int    @default(0) @mkp_connect_drupal_db.TinyInt
  weight Int    @default(0)

  @@index([theme, weight], map: "list")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pathauto_state {
  entity_type String @mkp_connect_drupal_db.VarChar(32)
  entity_id   Int    @mkp_connect_drupal_db.UnsignedInt
  pathauto    Int    @default(0) @mkp_connect_drupal_db.TinyInt

  @@id([entity_type, entity_id])
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model pit_instructors {
  id                                Int       @id @default(autoincrement())
  uf_id                             Int       @mkp_connect_drupal_db.UnsignedInt
  entity_id                         Int       @mkp_connect_drupal_db.UnsignedInt
  sort_name                         String?   @mkp_connect_drupal_db.VarChar(128)
  pit_instructor_classification_619 String?   @mkp_connect_drupal_db.VarChar(255)
  pit_instructor_status_620         String?   @mkp_connect_drupal_db.VarChar(255)
  pit_certification_date_621        DateTime? @mkp_connect_drupal_db.DateTime(0)
  current_center_1                  String?   @mkp_connect_drupal_db.VarChar(30)
  email                             String?   @mkp_connect_drupal_db.VarChar(254)
  phone                             String?   @mkp_connect_drupal_db.VarChar(32)
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model policy_body {
  nid  Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  body String @mkp_connect_drupal_db.LongText

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model process_body {
  nid    Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  body   String @mkp_connect_drupal_db.LongText
  teaser String @mkp_connect_drupal_db.LongText

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model public_contact {
  Region        String? @mkp_connect_drupal_db.VarChar(80)
  Area          String? @mkp_connect_drupal_db.VarChar(80)
  Community     String? @mkp_connect_drupal_db.VarChar(80)
  Role          String? @mkp_connect_drupal_db.LongText
  UID           Int?    @mkp_connect_drupal_db.UnsignedInt
  Name          String? @mkp_connect_drupal_db.VarChar(128)
  Email         String? @mkp_connect_drupal_db.VarChar(254)
  Phone         String? @mkp_connect_drupal_db.VarChar(32)
  Contact_Order Int?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model public_contact_dump {
  Region    String? @mkp_connect_drupal_db.VarChar(255)
  Area      String? @mkp_connect_drupal_db.VarChar(255)
  Community String? @mkp_connect_drupal_db.VarChar(255)
  Role      String? @mkp_connect_drupal_db.LongText
  UID       Int?
  Name      String? @mkp_connect_drupal_db.VarChar(128)
  Email     String? @mkp_connect_drupal_db.VarChar(100)
  Phone     String? @mkp_connect_drupal_db.VarChar(40)
  Order     Int?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model public_events_dump {
  title                 String?   @mkp_connect_drupal_db.VarChar(255)
  start_date            DateTime? @mkp_connect_drupal_db.DateTime(0)
  end_date              DateTime? @mkp_connect_drupal_db.DateTime(0)
  center_area_266       String?   @mkp_connect_drupal_db.VarChar(255)
  registration_link_270 String?   @mkp_connect_drupal_db.VarChar(255)
  region_454            String?   @mkp_connect_drupal_db.VarChar(255)
  gateway_429           String?   @mkp_connect_drupal_db.VarChar(255)
  link                  String?   @mkp_connect_drupal_db.VarChar(255)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model public_igroup {
  nid                        Int      @default(0) @mkp_connect_drupal_db.UnsignedInt
  title                      String   @default("") @mkp_connect_drupal_db.VarChar(255)
  og_description             String?  @mkp_connect_drupal_db.VarChar(255)
  field_ig_region_value      String?  @mkp_connect_drupal_db.LongText
  field_center_value         String?  @mkp_connect_drupal_db.LongText
  field_community_value      String?  @mkp_connect_drupal_db.LongText
  field_ig_owner_value       String?  @mkp_connect_drupal_db.LongText
  field_night_value          String?  @mkp_connect_drupal_db.LongText
  field_frequency_value      String?  @mkp_connect_drupal_db.LongText
  field_meeting_time_value   String?  @mkp_connect_drupal_db.LongText
  field_public_display_value String?  @mkp_connect_drupal_db.LongText
  street                     String?  @default("") @mkp_connect_drupal_db.VarChar(255)
  city                       String?  @default("") @mkp_connect_drupal_db.VarChar(255)
  province                   String?  @default("") @mkp_connect_drupal_db.VarChar(16)
  postal_code                String?  @default("") @mkp_connect_drupal_db.VarChar(16)
  sort_name                  String?  @mkp_connect_drupal_db.VarChar(128)
  search_email_238           String?  @mkp_connect_drupal_db.VarChar(100)
  search_phone_239           String?  @mkp_connect_drupal_db.VarChar(40)
  OpenMembers                String?  @mkp_connect_drupal_db.LongText
  OpenVisitors               String?  @mkp_connect_drupal_db.LongText
  OpenGuests                 String?  @mkp_connect_drupal_db.LongText
  UID                        Int?     @mkp_connect_drupal_db.UnsignedInt
  latitude                   Decimal? @mkp_connect_drupal_db.Decimal(14, 8)
  longitude                  Decimal? @mkp_connect_drupal_db.Decimal(14, 8)
  zoom                       Int?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model public_igroup_dump {
  nid                        Int     @default(0) @mkp_connect_drupal_db.UnsignedInt
  title                      String  @default("") @mkp_connect_drupal_db.VarChar(255)
  og_description             String? @mkp_connect_drupal_db.VarChar(255)
  field_ig_region_value      String? @mkp_connect_drupal_db.VarChar(100)
  field_center_value         String? @mkp_connect_drupal_db.VarChar(100)
  field_ig_owner_value       String? @mkp_connect_drupal_db.VarChar(25)
  field_night_value          String? @mkp_connect_drupal_db.VarChar(40)
  field_frequency_value      String? @mkp_connect_drupal_db.VarChar(20)
  field_meeting_time_value   String? @mkp_connect_drupal_db.VarChar(20)
  field_public_display_value String? @mkp_connect_drupal_db.VarChar(10)
  street                     String? @default("") @mkp_connect_drupal_db.VarChar(255)
  city                       String? @default("") @mkp_connect_drupal_db.VarChar(255)
  province                   String? @default("") @mkp_connect_drupal_db.VarChar(16)
  postal_code                String? @default("") @mkp_connect_drupal_db.VarChar(16)
  sort_name                  String? @mkp_connect_drupal_db.VarChar(128)
  search_email_238           String? @mkp_connect_drupal_db.VarChar(100)
  search_phone_239           String? @mkp_connect_drupal_db.VarChar(40)
  OpenMembers                String? @mkp_connect_drupal_db.VarChar(20)
  OpenVisitors               String? @mkp_connect_drupal_db.VarChar(20)
  OpenGuests                 String? @mkp_connect_drupal_db.VarChar(20)
  UID                        Int?
  latitude                   String? @mkp_connect_drupal_db.VarChar(16)
  longitude                  String? @mkp_connect_drupal_db.VarChar(16)
  zoom                       Int?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model public_name_dump {
  nid         Int      @default(0) @mkp_connect_drupal_db.UnsignedInt
  region      String?  @mkp_connect_drupal_db.VarChar(40)
  area        String?  @mkp_connect_drupal_db.VarChar(40)
  community   String?  @mkp_connect_drupal_db.VarChar(40)
  calendar_id String?  @default("") @mkp_connect_drupal_db.VarChar(40)
  email       String   @default("") @mkp_connect_drupal_db.VarChar(40)
  chartered   String   @mkp_connect_drupal_db.Char(1)
  latitude    Decimal? @mkp_connect_drupal_db.Decimal(10, 3)
  longitude   Decimal? @mkp_connect_drupal_db.Decimal(10, 3)
  zoom        Int?
  timezone    String?  @mkp_connect_drupal_db.VarChar(50)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model public_reg_dump {
  email      String @default("") @mkp_connect_drupal_db.VarChar(40)
  registered String @mkp_connect_drupal_db.Char(3)

  @@ignore
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model queue {
  item_id Int    @id @default(autoincrement()) @mkp_connect_drupal_db.UnsignedInt
  name    String @default("") @mkp_connect_drupal_db.VarChar(255)
  data    Bytes?
  expire  Int    @default(0)
  created Int    @default(0)

  @@index([expire], map: "expire")
  @@index([name, created], map: "name_created")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model quicktabs {
  machine_name    String  @id @mkp_connect_drupal_db.VarChar(255)
  ajax            Int     @default(0) @mkp_connect_drupal_db.UnsignedInt
  hide_empty_tabs Int     @default(0) @mkp_connect_drupal_db.UnsignedTinyInt
  default_tab     Int     @default(0) @mkp_connect_drupal_db.UnsignedInt
  title           String  @mkp_connect_drupal_db.VarChar(255)
  tabs            String  @mkp_connect_drupal_db.MediumText
  renderer        String  @mkp_connect_drupal_db.VarChar(255)
  style           String  @mkp_connect_drupal_db.VarChar(255)
  options         String? @mkp_connect_drupal_db.MediumText
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rdf_mapping {
  type    String @mkp_connect_drupal_db.VarChar(128)
  bundle  String @mkp_connect_drupal_db.VarChar(128)
  mapping Bytes?

  @@id([type, bundle])
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model readiness_data {
  uf_id                                Int       @id @mkp_connect_drupal_db.UnsignedInt
  entity_id                            Int       @mkp_connect_drupal_db.UnsignedInt
  display_name                         String?   @mkp_connect_drupal_db.VarChar(128)
  significant_medical_465              String?   @mkp_connect_drupal_db.VarChar(4)
  significant_medical_detail_466       String?   @mkp_connect_drupal_db.VarChar(255)
  significant_physical_467             String?   @mkp_connect_drupal_db.VarChar(4)
  significant_physical_detail_468      String?   @mkp_connect_drupal_db.VarChar(255)
  significant_ilnessess_469            String?   @mkp_connect_drupal_db.VarChar(4)
  significant_illness_detail_470       String?   @mkp_connect_drupal_db.VarChar(255)
  prescription_medications_devices_471 String?   @mkp_connect_drupal_db.VarChar(4)
  prescription_device_detail_472       String?   @mkp_connect_drupal_db.VarChar(255)
  food_drug_allergies_473              String?   @mkp_connect_drupal_db.VarChar(4)
  food_drug_allergy_detail_474         String?   @mkp_connect_drupal_db.VarChar(255)
  prescription_device_detail_2_475     String?   @mkp_connect_drupal_db.VarChar(255)
  prescription_device_detail_3_476     String?   @mkp_connect_drupal_db.VarChar(255)
  prescription_device_detail_4_477     String?   @mkp_connect_drupal_db.VarChar(255)
  prescription_device_detail_5_478     String?   @mkp_connect_drupal_db.VarChar(255)
  prescription_device_detail_addit_479 String?   @mkp_connect_drupal_db.VarChar(255)
  anxiety_panic_depression_grief_480   String?   @mkp_connect_drupal_db.VarChar(4)
  anxiety_panic_depression_grief_d_481 String?   @mkp_connect_drupal_db.VarChar(255)
  difficulty_controlling_behavior_482  String?   @mkp_connect_drupal_db.VarChar(4)
  difficulty_controlling_behavior__483 String?   @mkp_connect_drupal_db.VarChar(255)
  job_school_relationship_family_484   String?   @mkp_connect_drupal_db.VarChar(4)
  job_school_relationship_family_d_485 String?   @mkp_connect_drupal_db.VarChar(255)
  thoughts_harm_self_others_486        String?   @mkp_connect_drupal_db.VarChar(4)
  thoughts_harm_self_others_detail_487 String?   @mkp_connect_drupal_db.VarChar(255)
  armed_services_veteran_488           String?   @mkp_connect_drupal_db.VarChar(4)
  armed_services_veteran_detail_489    String?   @mkp_connect_drupal_db.VarChar(255)
  see_hear_experience_things_490       String?   @mkp_connect_drupal_db.VarChar(4)
  see_hear_experience_things_detai_491 String?   @mkp_connect_drupal_db.VarChar(255)
  traumatic_experience_492             String?   @mkp_connect_drupal_db.VarChar(4)
  traumatic_experience_detail_493      String?   @mkp_connect_drupal_db.VarChar(255)
  active_addiction_detail_495          String?   @mkp_connect_drupal_db.VarChar(255)
  recovery_process_496                 String?   @mkp_connect_drupal_db.VarChar(4)
  recovery_process_detail_497          String?   @mkp_connect_drupal_db.VarChar(255)
  alcohol_use_498                      String?   @mkp_connect_drupal_db.VarChar(4)
  recreational_drug_500                String?   @mkp_connect_drupal_db.VarChar(4)
  recreational_drug_detail_501         String?   @mkp_connect_drupal_db.VarChar(255)
  anything_else_502                    String?   @mkp_connect_drupal_db.VarChar(4)
  anything_else_detail_503             String?   @mkp_connect_drupal_db.VarChar(255)
  submission_date_504                  DateTime? @mkp_connect_drupal_db.DateTime(0)
  last_reviewed_date_505               DateTime? @mkp_connect_drupal_db.DateTime(0)
  reviewed_status_506                  String?   @mkp_connect_drupal_db.VarChar(255)
  active_addiction_508                 String?   @mkp_connect_drupal_db.VarChar(4)
  counseling_509                       String?   @mkp_connect_drupal_db.VarChar(4)
  counseling_topics_511                String?   @mkp_connect_drupal_db.VarChar(255)
  anxiety_panic_depression_grief_t_514 String?   @mkp_connect_drupal_db.VarChar(255)
  mood_conflict_515                    String?   @mkp_connect_drupal_db.VarChar(255)
  mood_conflict_timeframe_516          String?   @mkp_connect_drupal_db.VarChar(255)
  mood_conflict_detail_517             String?   @mkp_connect_drupal_db.VarChar(255)
  sleep_eating_518                     String?   @mkp_connect_drupal_db.VarChar(255)
  sleep_eating_timeframe_519           String?   @mkp_connect_drupal_db.VarChar(255)
  sleep_eating_detail_520              String?   @mkp_connect_drupal_db.VarChar(255)
  energy_fatigue_521                   String?   @mkp_connect_drupal_db.VarChar(255)
  energy_fatigue_timeframe_522         String?   @mkp_connect_drupal_db.VarChar(255)
  energy_fatigue_detail_523            String?   @mkp_connect_drupal_db.VarChar(255)
  risks_524                            String?   @mkp_connect_drupal_db.VarChar(255)
  risks_timeframe_525                  String?   @mkp_connect_drupal_db.VarChar(255)
  risks_detail_526                     String?   @mkp_connect_drupal_db.VarChar(255)
  difficulty_controlling_behavior__527 String?   @mkp_connect_drupal_db.VarChar(255)
  job_school_relationship_family_t_528 String?   @mkp_connect_drupal_db.VarChar(255)
  thoughts_harm_self_others_timefr_529 String?   @mkp_connect_drupal_db.VarChar(255)
  traumatic_experience_timeframe_530   String?   @mkp_connect_drupal_db.VarChar(255)
  drug_abstinence_531                  String?   @mkp_connect_drupal_db.VarChar(255)
  drug_abstinence_detail_532           String?   @mkp_connect_drupal_db.VarChar(255)
  height_533                           String?   @mkp_connect_drupal_db.VarChar(255)
  weight_534                           String?   @mkp_connect_drupal_db.VarChar(255)
  expiration_date_536                  DateTime? @mkp_connect_drupal_db.DateTime(0)
  alcohol_use_detail_537               String?   @mkp_connect_drupal_db.VarChar(255)
  reviewer_comments_541                String?   @mkp_connect_drupal_db.Text
  see_hear_timeframe_545               String?   @mkp_connect_drupal_db.VarChar(255)
  registered_sex_offender_553          String?   @mkp_connect_drupal_db.VarChar(4)
  rso_detail_554                       String?   @mkp_connect_drupal_db.VarChar(255)
  reviewed_onsite_555                  String?   @mkp_connect_drupal_db.VarChar(255)
  administrative_flag_562              Int?      @mkp_connect_drupal_db.TinyInt
  arrival_date_next_nwta_600           DateTime? @mkp_connect_drupal_db.DateTime(0)
  felony_611                           String?   @mkp_connect_drupal_db.VarChar(4)
  felony_detail_612                    String?   @mkp_connect_drupal_db.VarChar(255)
  fully_vaccinated_751                 String?   @mkp_connect_drupal_db.VarChar(4)
  online_flag_757                      Int?      @mkp_connect_drupal_db.TinyInt
  on_site_summary_788                  String?   @mkp_connect_drupal_db.Text
  event_title                          String?   @mkp_connect_drupal_db.VarChar(80)
  event_date                           String?   @mkp_connect_drupal_db.VarChar(10)
  nwta_graduate__10                    Int?      @mkp_connect_drupal_db.TinyInt
  birth_date_432                       DateTime? @mkp_connect_drupal_db.DateTime(0)
  emergency_contact_name_656           String?   @mkp_connect_drupal_db.VarChar(120)
  emergency_contact_phone_657          String?   @mkp_connect_drupal_db.VarChar(100)
  phone                                String?   @mkp_connect_drupal_db.VarChar(120)
  email                                String?   @mkp_connect_drupal_db.VarChar(120)
  reviewer_792                         String?   @mkp_connect_drupal_db.VarChar(20)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model readiness_data_1 {
  uf_id                      Int       @id @mkp_connect_drupal_db.UnsignedInt
  submission_date_504        DateTime? @mkp_connect_drupal_db.DateTime(0)
  last_reviewed_date_505     DateTime? @mkp_connect_drupal_db.DateTime(0)
  reviewed_status_506        String?   @mkp_connect_drupal_db.VarChar(255)
  expiration_date_536        DateTime? @mkp_connect_drupal_db.DateTime(0)
  arrival_date_next_nwta_600 DateTime? @mkp_connect_drupal_db.DateTime(0)
  arrival_message            String?   @mkp_connect_drupal_db.VarChar(100)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model readiness_data_past {
  uf_id                                Int       @id @mkp_connect_drupal_db.UnsignedInt
  entity_id                            Int       @mkp_connect_drupal_db.UnsignedInt
  display_name                         String?   @mkp_connect_drupal_db.VarChar(128)
  significant_medical_465              String?   @mkp_connect_drupal_db.VarChar(4)
  significant_medical_detail_466       String?   @mkp_connect_drupal_db.VarChar(255)
  significant_physical_467             String?   @mkp_connect_drupal_db.VarChar(4)
  significant_physical_detail_468      String?   @mkp_connect_drupal_db.VarChar(255)
  significant_ilnessess_469            String?   @mkp_connect_drupal_db.VarChar(4)
  significant_illness_detail_470       String?   @mkp_connect_drupal_db.VarChar(255)
  prescription_medications_devices_471 String?   @mkp_connect_drupal_db.VarChar(4)
  prescription_device_detail_472       String?   @mkp_connect_drupal_db.VarChar(255)
  food_drug_allergies_473              String?   @mkp_connect_drupal_db.VarChar(4)
  food_drug_allergy_detail_474         String?   @mkp_connect_drupal_db.VarChar(255)
  prescription_device_detail_2_475     String?   @mkp_connect_drupal_db.VarChar(255)
  prescription_device_detail_3_476     String?   @mkp_connect_drupal_db.VarChar(255)
  prescription_device_detail_4_477     String?   @mkp_connect_drupal_db.VarChar(255)
  prescription_device_detail_5_478     String?   @mkp_connect_drupal_db.VarChar(255)
  prescription_device_detail_addit_479 String?   @mkp_connect_drupal_db.VarChar(255)
  anxiety_panic_depression_grief_480   String?   @mkp_connect_drupal_db.VarChar(4)
  anxiety_panic_depression_grief_d_481 String?   @mkp_connect_drupal_db.VarChar(255)
  difficulty_controlling_behavior_482  String?   @mkp_connect_drupal_db.VarChar(4)
  difficulty_controlling_behavior__483 String?   @mkp_connect_drupal_db.VarChar(255)
  job_school_relationship_family_484   String?   @mkp_connect_drupal_db.VarChar(4)
  job_school_relationship_family_d_485 String?   @mkp_connect_drupal_db.VarChar(255)
  thoughts_harm_self_others_486        String?   @mkp_connect_drupal_db.VarChar(4)
  thoughts_harm_self_others_detail_487 String?   @mkp_connect_drupal_db.VarChar(255)
  armed_services_veteran_488           String?   @mkp_connect_drupal_db.VarChar(4)
  armed_services_veteran_detail_489    String?   @mkp_connect_drupal_db.VarChar(255)
  see_hear_experience_things_490       String?   @mkp_connect_drupal_db.VarChar(4)
  see_hear_experience_things_detai_491 String?   @mkp_connect_drupal_db.VarChar(255)
  traumatic_experience_492             String?   @mkp_connect_drupal_db.VarChar(4)
  traumatic_experience_detail_493      String?   @mkp_connect_drupal_db.VarChar(255)
  active_addiction_detail_495          String?   @mkp_connect_drupal_db.VarChar(255)
  recovery_process_496                 String?   @mkp_connect_drupal_db.VarChar(4)
  recovery_process_detail_497          String?   @mkp_connect_drupal_db.VarChar(255)
  alcohol_use_498                      String?   @mkp_connect_drupal_db.VarChar(4)
  recreational_drug_500                String?   @mkp_connect_drupal_db.VarChar(4)
  recreational_drug_detail_501         String?   @mkp_connect_drupal_db.VarChar(255)
  anything_else_502                    String?   @mkp_connect_drupal_db.VarChar(4)
  anything_else_detail_503             String?   @mkp_connect_drupal_db.VarChar(255)
  submission_date_504                  DateTime? @mkp_connect_drupal_db.DateTime(0)
  last_reviewed_date_505               DateTime? @mkp_connect_drupal_db.DateTime(0)
  reviewed_status_506                  String?   @mkp_connect_drupal_db.VarChar(255)
  active_addiction_508                 String?   @mkp_connect_drupal_db.VarChar(4)
  counseling_509                       String?   @mkp_connect_drupal_db.VarChar(4)
  counseling_topics_511                String?   @mkp_connect_drupal_db.VarChar(255)
  anxiety_panic_depression_grief_t_514 String?   @mkp_connect_drupal_db.VarChar(255)
  mood_conflict_515                    String?   @mkp_connect_drupal_db.VarChar(255)
  mood_conflict_timeframe_516          String?   @mkp_connect_drupal_db.VarChar(255)
  mood_conflict_detail_517             String?   @mkp_connect_drupal_db.VarChar(255)
  sleep_eating_518                     String?   @mkp_connect_drupal_db.VarChar(255)
  sleep_eating_timeframe_519           String?   @mkp_connect_drupal_db.VarChar(255)
  sleep_eating_detail_520              String?   @mkp_connect_drupal_db.VarChar(255)
  energy_fatigue_521                   String?   @mkp_connect_drupal_db.VarChar(255)
  energy_fatigue_timeframe_522         String?   @mkp_connect_drupal_db.VarChar(255)
  energy_fatigue_detail_523            String?   @mkp_connect_drupal_db.VarChar(255)
  risks_524                            String?   @mkp_connect_drupal_db.VarChar(255)
  risks_timeframe_525                  String?   @mkp_connect_drupal_db.VarChar(255)
  risks_detail_526                     String?   @mkp_connect_drupal_db.VarChar(255)
  difficulty_controlling_behavior__527 String?   @mkp_connect_drupal_db.VarChar(255)
  job_school_relationship_family_t_528 String?   @mkp_connect_drupal_db.VarChar(255)
  thoughts_harm_self_others_timefr_529 String?   @mkp_connect_drupal_db.VarChar(255)
  traumatic_experience_timeframe_530   String?   @mkp_connect_drupal_db.VarChar(255)
  drug_abstinence_531                  String?   @mkp_connect_drupal_db.VarChar(255)
  drug_abstinence_detail_532           String?   @mkp_connect_drupal_db.VarChar(255)
  height_533                           String?   @mkp_connect_drupal_db.VarChar(255)
  weight_534                           String?   @mkp_connect_drupal_db.VarChar(255)
  expiration_date_536                  DateTime? @mkp_connect_drupal_db.DateTime(0)
  alcohol_use_detail_537               String?   @mkp_connect_drupal_db.VarChar(255)
  reviewer_comments_541                String?   @mkp_connect_drupal_db.Text
  see_hear_timeframe_545               String?   @mkp_connect_drupal_db.VarChar(255)
  registered_sex_offender_553          String?   @mkp_connect_drupal_db.VarChar(4)
  rso_detail_554                       String?   @mkp_connect_drupal_db.VarChar(255)
  reviewed_onsite_555                  String?   @mkp_connect_drupal_db.VarChar(255)
  administrative_flag_562              Int?      @mkp_connect_drupal_db.TinyInt
  arrival_date_next_nwta_600           DateTime? @mkp_connect_drupal_db.DateTime(0)
  felony_611                           String?   @mkp_connect_drupal_db.VarChar(4)
  felony_detail_612                    String?   @mkp_connect_drupal_db.VarChar(255)
  fully_vaccinated_751                 String?   @mkp_connect_drupal_db.VarChar(4)
  online_flag_757                      Int?      @mkp_connect_drupal_db.TinyInt
  on_site_summary_788                  String?   @mkp_connect_drupal_db.Text
  event_title                          String?   @mkp_connect_drupal_db.VarChar(80)
  event_date                           String?   @mkp_connect_drupal_db.VarChar(10)
  nwta_graduate__10                    Int?      @mkp_connect_drupal_db.TinyInt
  birth_date_432                       DateTime? @mkp_connect_drupal_db.DateTime(0)
  emergency_contact_name_656           String?   @mkp_connect_drupal_db.VarChar(120)
  emergency_contact_phone_657          String?   @mkp_connect_drupal_db.VarChar(100)
  phone                                String?   @mkp_connect_drupal_db.VarChar(120)
  email                                String?   @mkp_connect_drupal_db.VarChar(120)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model readiness_next_nwta {
  id                  Int     @id @default(autoincrement())
  contact_id          Int     @mkp_connect_drupal_db.UnsignedInt
  date_event          String  @default("") @mkp_connect_drupal_db.VarChar(30)
  event_id            String? @mkp_connect_drupal_db.VarChar(10)
  event_title         String? @mkp_connect_drupal_db.VarChar(80)
  event_date          String? @mkp_connect_drupal_db.VarChar(10)
  contact_name        String? @mkp_connect_drupal_db.VarChar(120)
  contact_city        String? @mkp_connect_drupal_db.VarChar(80)
  contact_state_id    Int?
  contact_state       String? @mkp_connect_drupal_db.VarChar(80)
  contact_email       String? @mkp_connect_drupal_db.VarChar(120)
  contact_center      String? @mkp_connect_drupal_db.VarChar(80)
  contact_region      String? @mkp_connect_drupal_db.VarChar(80)
  contact_online_help Int?
  role_id             Int?
  uid                 Int?
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model redirect {
  rid              Int    @id @default(autoincrement())
  hash             String @unique(map: "hash") @mkp_connect_drupal_db.VarChar(64)
  type             String @default("") @mkp_connect_drupal_db.VarChar(64)
  uid              Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  source           String @mkp_connect_drupal_db.VarChar(900)
  source_options   Bytes  @mkp_connect_drupal_db.Blob
  redirect         String @mkp_connect_drupal_db.VarChar(900)
  redirect_options Bytes  @mkp_connect_drupal_db.Blob
  language         String @default("und") @mkp_connect_drupal_db.VarChar(12)
  status_code      Int    @mkp_connect_drupal_db.SmallInt
  count            Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  access           Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  status           Int    @default(1) @mkp_connect_drupal_db.SmallInt

  @@index([type, access], map: "expires")
  @@index([redirect(length: 255)], map: "redirect")
  @@index([status, source(length: 255), language], map: "status_source_language")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model registry {
  name     String @default("") @mkp_connect_drupal_db.VarChar(255)
  type     String @default("") @mkp_connect_drupal_db.VarChar(9)
  filename String @mkp_connect_drupal_db.VarChar(255)
  module   String @default("") @mkp_connect_drupal_db.VarChar(255)
  weight   Int    @default(0)

  @@id([name, type])
  @@index([type, weight, module], map: "hook")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model registry_file {
  filename String @id @mkp_connect_drupal_db.VarChar(255)
  hash     String @mkp_connect_drupal_db.VarChar(64)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model ritual_elders {
  id                                 Int       @id @default(0) @mkp_connect_drupal_db.UnsignedInt
  uf_id                              Int?
  entity_id                          Int       @mkp_connect_drupal_db.UnsignedInt
  level_421                          String?   @mkp_connect_drupal_db.VarChar(255)
  status_422                         String?   @mkp_connect_drupal_db.VarChar(255)
  last_certification_date_423        DateTime? @mkp_connect_drupal_db.DateTime(0)
  certification_due_424              DateTime? @mkp_connect_drupal_db.DateTime(0)
  last_staffing_as_re_425            DateTime? @mkp_connect_drupal_db.DateTime(0)
  re_mentor_426                      String?   @mkp_connect_drupal_db.VarChar(255)
  et1_626                            String?   @mkp_connect_drupal_db.VarChar(255)
  et2_627                            String?   @mkp_connect_drupal_db.VarChar(255)
  et3_628                            String?   @mkp_connect_drupal_db.VarChar(255)
  folder_629                         String?   @mkp_connect_drupal_db.VarChar(255)
  note_630                           String?   @mkp_connect_drupal_db.VarChar(255)
  original_re_certification_date_632 String?   @mkp_connect_drupal_db.VarChar(255)
  re_covenant_763                    DateTime? @mkp_connect_drupal_db.DateTime(0)
  sort_name                          String?   @mkp_connect_drupal_db.VarChar(128)
  current_center_1                   String?   @mkp_connect_drupal_db.VarChar(30)
  nwta_leader_team_agreement_773     DateTime? @mkp_connect_drupal_db.DateTime(0)
  email                              String?   @mkp_connect_drupal_db.VarChar(254)
  phone                              String?   @mkp_connect_drupal_db.VarChar(32)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model role {
  rid    Int    @id @default(autoincrement()) @mkp_connect_drupal_db.UnsignedInt
  name   String @unique(map: "name") @default("") @mkp_connect_drupal_db.VarChar(64)
  weight Int    @default(0)

  @@index([name, weight], map: "name_weight")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model role_permission {
  rid        Int    @mkp_connect_drupal_db.UnsignedInt
  permission String @default("") @mkp_connect_drupal_db.VarChar(128)
  module     String @default("") @mkp_connect_drupal_db.VarChar(255)

  @@id([rid, permission])
  @@index([permission], map: "permission")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rules_config {
  id             Int     @id @default(autoincrement())
  name           String  @unique(map: "name") @mkp_connect_drupal_db.VarChar(64)
  label          String  @default("unlabeled") @mkp_connect_drupal_db.VarChar(255)
  plugin         String  @mkp_connect_drupal_db.VarChar(127)
  active         Int     @default(1)
  weight         Int     @default(0) @mkp_connect_drupal_db.TinyInt
  status         Int     @default(1) @mkp_connect_drupal_db.TinyInt
  dirty          Int     @default(0) @mkp_connect_drupal_db.TinyInt
  module         String? @mkp_connect_drupal_db.VarChar(255)
  owner          String  @default("rules") @mkp_connect_drupal_db.VarChar(255)
  access_exposed Int     @default(0) @mkp_connect_drupal_db.TinyInt
  data           Bytes?

  @@index([plugin, active], map: "plugin")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rules_dependencies {
  id     Int    @mkp_connect_drupal_db.UnsignedInt
  module String @mkp_connect_drupal_db.VarChar(255)

  @@id([id, module])
  @@index([module], map: "module")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rules_tags {
  id  Int    @mkp_connect_drupal_db.UnsignedInt
  tag String @mkp_connect_drupal_db.VarChar(255)

  @@id([id, tag])
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model rules_trigger {
  id    Int    @mkp_connect_drupal_db.UnsignedInt
  event String @default("") @mkp_connect_drupal_db.VarChar(127)

  @@id([id, event])
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model search_dataset {
  sid     Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  type    String @mkp_connect_drupal_db.VarChar(16)
  data    String @mkp_connect_drupal_db.LongText
  reindex Int    @default(0) @mkp_connect_drupal_db.UnsignedInt

  @@id([sid, type])
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model search_index {
  word  String @default("") @mkp_connect_drupal_db.VarChar(50)
  sid   Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  type  String @mkp_connect_drupal_db.VarChar(16)
  score Float? @mkp_connect_drupal_db.Float

  @@id([word, sid, type])
  @@index([sid, type], map: "sid_type")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model search_node_links {
  sid     Int     @default(0) @mkp_connect_drupal_db.UnsignedInt
  type    String  @default("") @mkp_connect_drupal_db.VarChar(16)
  nid     Int     @default(0) @mkp_connect_drupal_db.UnsignedInt
  caption String? @mkp_connect_drupal_db.LongText

  @@id([sid, type, nid])
  @@index([nid], map: "nid")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model search_total {
  word  String @id @default("") @mkp_connect_drupal_db.VarChar(50)
  count Float? @mkp_connect_drupal_db.Float
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model semaphore {
  name   String @id @default("") @mkp_connect_drupal_db.VarChar(255)
  value  String @default("") @mkp_connect_drupal_db.VarChar(255)
  expire Float

  @@index([expire], map: "expire")
  @@index([value], map: "value")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model sequences {
  value Int @id @default(autoincrement()) @mkp_connect_drupal_db.UnsignedInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model services_endpoint {
  eid             Int    @id @default(autoincrement()) @mkp_connect_drupal_db.UnsignedInt
  name            String @unique(map: "name") @mkp_connect_drupal_db.VarChar(255)
  server          String @mkp_connect_drupal_db.VarChar(32)
  path            String @mkp_connect_drupal_db.VarChar(255)
  authentication  String @mkp_connect_drupal_db.LongText
  server_settings Bytes
  resources       String @mkp_connect_drupal_db.LongText
  debug           Int    @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model services_user {
  uid     Int @id @mkp_connect_drupal_db.UnsignedInt
  created Int @default(0)
  changed Int @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model sessions {
  uid       Int    @mkp_connect_drupal_db.UnsignedInt
  sid       String @mkp_connect_drupal_db.VarChar(128)
  ssid      String @default("") @mkp_connect_drupal_db.VarChar(128)
  hostname  String @default("") @mkp_connect_drupal_db.VarChar(128)
  timestamp Int    @default(0)
  cache     Int    @default(0)
  session   Bytes?

  @@id([sid, ssid])
  @@index([ssid], map: "ssid")
  @@index([timestamp], map: "timestamp")
  @@index([uid], map: "uid")
}

model staffing_initiate_payments {
  id              Int       @id @default(autoincrement())
  drupal_group    Int?
  civi_event      Int?
  contact_id      Int?
  payment_date    DateTime? @mkp_connect_drupal_db.Date
  payment         Decimal?  @mkp_connect_drupal_db.Decimal(10, 0)
  notes           String?   @mkp_connect_drupal_db.Text
  contribution_id Int?

  @@index([contact_id], map: "contact_id")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model staffing_latest {
  uid        Int       @mkp_connect_drupal_db.UnsignedInt
  contact_id Int       @mkp_connect_drupal_db.UnsignedInt
  event      String?   @mkp_connect_drupal_db.VarChar(255)
  role_id    String?   @mkp_connect_drupal_db.VarChar(128)
  start_date DateTime? @mkp_connect_drupal_db.DateTime(0)
  role_label String?   @mkp_connect_drupal_db.VarChar(80)

  @@ignore
}

model staffing_requests {
  id              Int     @id @default(autoincrement())
  civi_event_id   Int?
  contact_id      Int?
  uid             Int?
  sort_name       String? @mkp_connect_drupal_db.VarChar(100)
  request         Int?
  nid             Int?
  times_staffed   Int?
  leadership_role String? @mkp_connect_drupal_db.VarChar(40)
  elder_role      String? @mkp_connect_drupal_db.VarChar(40)
  request_name    String? @mkp_connect_drupal_db.VarChar(40)

  @@index([request], map: "request")
  @@index([uid], map: "uid")
}

model staffing_roles {
  role_id       Int     @id @default(autoincrement())
  role_protocol String? @mkp_connect_drupal_db.VarChar(4)
  role_title    String? @mkp_connect_drupal_db.VarChar(80)
  role_category String? @mkp_connect_drupal_db.VarChar(20)
  role_order    Int?
}

model staffing_roles_times {
  id         Int  @id @default(autoincrement())
  contact_id Int?
  uid        Int?
  role_id    Int?
  event_id   Int?
}

model staffing_roles_times_summary {
  id              Int     @id @default(autoincrement())
  uid             Int?
  event_id        Int?
  role_id         Int?
  times_staffed   Int     @default(0)
  leadership_role String? @mkp_connect_drupal_db.VarChar(40)

  @@index([uid], map: "UID")
  @@index([role_id], map: "role_id")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model styleguide_palette_swatch {
  id          Int    @id @default(autoincrement()) @mkp_connect_drupal_db.UnsignedInt
  name        String @default("") @mkp_connect_drupal_db.VarChar(255)
  description String @default("") @mkp_connect_drupal_db.VarChar(255)
  hex         String @default("0") @mkp_connect_drupal_db.VarChar(7)
  theme       String @default("") @mkp_connect_drupal_db.VarChar(64)
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model sysdoc_body {
  nid  Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  body String @mkp_connect_drupal_db.LongText

  @@ignore
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model system {
  filename       String @id @default("") @mkp_connect_drupal_db.VarChar(255)
  name           String @default("") @mkp_connect_drupal_db.VarChar(255)
  type           String @default("") @mkp_connect_drupal_db.VarChar(12)
  owner          String @default("") @mkp_connect_drupal_db.VarChar(255)
  status         Int    @default(0)
  bootstrap      Int    @default(0)
  schema_version Int    @default(-1) @mkp_connect_drupal_db.SmallInt
  weight         Int    @default(0)
  info           Bytes? @mkp_connect_drupal_db.Blob

  @@index([status, bootstrap, type, weight, name], map: "system_list")
  @@index([type, name], map: "type_name")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model system_copy1 {
  filename       String @id @default("") @mkp_connect_drupal_db.VarChar(255)
  name           String @default("") @mkp_connect_drupal_db.VarChar(255)
  type           String @default("") @mkp_connect_drupal_db.VarChar(12)
  owner          String @default("") @mkp_connect_drupal_db.VarChar(255)
  status         Int    @default(0)
  bootstrap      Int    @default(0)
  schema_version Int    @default(-1) @mkp_connect_drupal_db.SmallInt
  weight         Int    @default(0)
  info           Bytes? @mkp_connect_drupal_db.Blob

  @@index([status, bootstrap, type, weight, name], map: "system_list")
  @@index([type, name], map: "type_name")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model taxonomy_index {
  nid     Int  @default(0) @mkp_connect_drupal_db.UnsignedInt
  tid     Int  @default(0) @mkp_connect_drupal_db.UnsignedInt
  sticky  Int? @default(0) @mkp_connect_drupal_db.TinyInt
  created Int  @default(0)

  @@index([nid], map: "nid")
  @@index([tid, sticky, created], map: "term_node")
  @@ignore
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model taxonomy_term_data {
  tid         Int     @id @default(autoincrement()) @mkp_connect_drupal_db.UnsignedInt
  vid         Int     @default(0) @mkp_connect_drupal_db.UnsignedInt
  name        String  @default("") @mkp_connect_drupal_db.VarChar(255)
  description String? @mkp_connect_drupal_db.LongText
  format      String? @mkp_connect_drupal_db.VarChar(255)
  weight      Int     @default(0)

  @@index([name], map: "name")
  @@index([vid, weight, name], map: "taxonomy_tree")
  @@index([vid, name], map: "vid_name")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model taxonomy_term_hierarchy {
  tid    Int @default(0) @mkp_connect_drupal_db.UnsignedInt
  parent Int @default(0) @mkp_connect_drupal_db.UnsignedInt

  @@id([tid, parent])
  @@index([parent], map: "parent")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model taxonomy_vocabulary {
  vid          Int     @id @default(autoincrement()) @mkp_connect_drupal_db.UnsignedInt
  name         String  @default("") @mkp_connect_drupal_db.VarChar(255)
  machine_name String  @unique(map: "machine_name") @default("") @mkp_connect_drupal_db.VarChar(255)
  description  String? @mkp_connect_drupal_db.LongText
  hierarchy    Int     @default(0) @mkp_connect_drupal_db.UnsignedTinyInt
  module       String  @default("") @mkp_connect_drupal_db.VarChar(255)
  weight       Int     @default(0)

  @@index([weight, name], map: "list")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model temp_auth {
  username String? @mkp_connect_drupal_db.VarChar(100)
  pw       String? @mkp_connect_drupal_db.VarChar(40)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model temp_body_export {
  nid    Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  body   String @mkp_connect_drupal_db.LongText
  teaser String @mkp_connect_drupal_db.LongText

  @@ignore
}

model temp_content_type_email_archives {
  title                  String?   @mkp_connect_drupal_db.VarChar(512)
  field_email_from_value String?   @mkp_connect_drupal_db.LongText
  field_email_date_value DateTime? @mkp_connect_drupal_db.DateTime(0)
  field_group_id_value   Int?
  field_datestamp_value  String?   @mkp_connect_drupal_db.LongText
  field_message_id_value String    @id @default("") @mkp_connect_drupal_db.VarChar(128)
  body                   String?   @mkp_connect_drupal_db.LongText
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model temp_event_dates {
  entity_id                 Int       @mkp_connect_drupal_db.UnsignedInt
  title                     String    @default("") @mkp_connect_drupal_db.VarChar(255)
  field_civicrm_event_value Int?
  start_date                DateTime? @mkp_connect_drupal_db.DateTime(0)
  end_date                  DateTime? @mkp_connect_drupal_db.DateTime(0)
  registration_start_date   DateTime? @mkp_connect_drupal_db.DateTime(0)
  registration_end_date     DateTime? @mkp_connect_drupal_db.DateTime(0)

  @@ignore
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model temp_facilitators {
  id         Int    @id @default(autoincrement())
  nid        Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  title      String @default("") @mkp_connect_drupal_db.VarChar(255)
  uid        Int
  contact_id Int?   @mkp_connect_drupal_db.UnsignedInt
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model temp_info_body {
  nid    Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  body   String @mkp_connect_drupal_db.LongText
  teaser String @mkp_connect_drupal_db.LongText

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model temp_init_payments {
  civi_event      Int       @default(0) @mkp_connect_drupal_db.UnsignedInt
  contact_id      Int       @mkp_connect_drupal_db.UnsignedInt
  payment_date    DateTime? @mkp_connect_drupal_db.DateTime(0)
  payment         Decimal   @mkp_connect_drupal_db.Decimal(20, 2)
  notes           String?   @mkp_connect_drupal_db.Text
  contribution_id Int       @default(0) @mkp_connect_drupal_db.UnsignedInt

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model temp_main_contact {
  field_entity_value       Int?
  field_contact_uid        Int? @mkp_connect_drupal_db.UnsignedInt
  field_main_contact_value Int?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model temp_main_contact_uid {
  nid               Int?
  title             String? @mkp_connect_drupal_db.VarChar(80)
  field_contact_uid Int?    @mkp_connect_drupal_db.UnsignedInt

  @@ignore
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model temp_member_listing {
  uid              Int     @id @mkp_connect_drupal_db.UnsignedInt
  current_center_1 String? @mkp_connect_drupal_db.VarChar(30)
  member_type_45   String? @mkp_connect_drupal_db.VarChar(40)
  display_name     String? @mkp_connect_drupal_db.VarChar(128)
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model temp_membership {
  id                       Int       @default(0) @mkp_connect_drupal_db.UnsignedInt
  contact_id               Int       @mkp_connect_drupal_db.UnsignedInt
  membership_type_id       Int       @mkp_connect_drupal_db.UnsignedInt
  join_date                DateTime? @mkp_connect_drupal_db.Date
  start_date               DateTime? @mkp_connect_drupal_db.Date
  end_date                 DateTime? @mkp_connect_drupal_db.Date
  source                   String?   @mkp_connect_drupal_db.VarChar(128)
  status_id                Int       @mkp_connect_drupal_db.UnsignedInt
  is_override              Int       @default(0) @mkp_connect_drupal_db.TinyInt
  owner_membership_id      Int?      @mkp_connect_drupal_db.UnsignedInt
  max_related              Int?
  is_test                  Int       @default(0) @mkp_connect_drupal_db.TinyInt
  is_pay_later             Int       @default(0) @mkp_connect_drupal_db.TinyInt
  contribution_recur_id    Int?      @mkp_connect_drupal_db.UnsignedInt
  campaign_id              Int?      @mkp_connect_drupal_db.UnsignedInt
  status_override_end_date DateTime? @mkp_connect_drupal_db.Date

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model temp_membership_1 {
  contact_id Int  @mkp_connect_drupal_db.UnsignedInt
  status_id  Int  @mkp_connect_drupal_db.UnsignedInt
  id         Int? @mkp_connect_drupal_db.UnsignedInt

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model temp_membership_expiration {
  contact_id                       Int       @mkp_connect_drupal_db.UnsignedInt
  Max_civicrm_membership_end_date_ DateTime? @map("Max(civicrm_membership.end_date)") @mkp_connect_drupal_db.Date

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model temp_nalb {
  entity_id Int @mkp_connect_drupal_db.UnsignedInt
  uf_id     Int @mkp_connect_drupal_db.UnsignedInt

  @@ignore
}

model temp_nwta_build_import {
  id                      Int       @id @default(autoincrement())
  title                   String?   @mkp_connect_drupal_db.VarChar(120)
  center_area             String?   @mkp_connect_drupal_db.VarChar(80)
  region                  String?   @mkp_connect_drupal_db.VarChar(40)
  owner_nid               Int?
  mkpi                    String?   @mkp_connect_drupal_db.Char(4)
  email_prefix            String?   @mkp_connect_drupal_db.VarChar(40)
  staff_coordinator       String?   @mkp_connect_drupal_db.VarChar(80)
  staff_coordinator_email String?   @mkp_connect_drupal_db.VarChar(80)
  staff_coordinator_phone String?   @mkp_connect_drupal_db.VarChar(40)
  part_registrar          String?   @mkp_connect_drupal_db.VarChar(80)
  part_registrar_email    String?   @mkp_connect_drupal_db.VarChar(80)
  part_registrar_phone    String?   @mkp_connect_drupal_db.VarChar(40)
  admin                   String?   @mkp_connect_drupal_db.VarChar(80)
  admin_email             String?   @mkp_connect_drupal_db.VarChar(80)
  admin_phone             String?   @mkp_connect_drupal_db.VarChar(40)
  registration_link       String?   @mkp_connect_drupal_db.VarChar(120)
  staff_registration_link String?   @mkp_connect_drupal_db.VarChar(120)
  staff_start_date        DateTime? @mkp_connect_drupal_db.Date
  staff_start_time        DateTime? @mkp_connect_drupal_db.Time(0)
  nb_start_date           DateTime? @mkp_connect_drupal_db.Date
  nb_start_time           DateTime? @mkp_connect_drupal_db.Time(0)
  nwta_end_date           DateTime? @mkp_connect_drupal_db.Date
  nwta_end_time           DateTime? @mkp_connect_drupal_db.Time(0)
  staff_reg_start         DateTime? @mkp_connect_drupal_db.Date
  staff_reg_start_time    DateTime? @mkp_connect_drupal_db.Time(0)
  staff_reg_end           DateTime? @mkp_connect_drupal_db.Date
  staff_reg_end_time      DateTime? @mkp_connect_drupal_db.Time(0)
  part_reg_start          DateTime? @mkp_connect_drupal_db.Date
  part_reg_start_time     DateTime? @mkp_connect_drupal_db.Time(0)
  part_reg_end            DateTime? @mkp_connect_drupal_db.Date
  part_reg_end_time       DateTime? @mkp_connect_drupal_db.Time(0)
  part_fee                Int?
  staff_fee               Int?
  mos_fee                 Int?
  accounting_code         String?   @mkp_connect_drupal_db.VarChar(40)
  class                   String?   @mkp_connect_drupal_db.VarChar(120)
  readiness               String?   @mkp_connect_drupal_db.Char(6)
  multipay                String?   @mkp_connect_drupal_db.Char(6)
  max_part                Int?
  gateway                 String?   @mkp_connect_drupal_db.Char(20)
  reg_login               String?   @mkp_connect_drupal_db.Char(6)
  waitlist                String?   @mkp_connect_drupal_db.Char(6)
  track_vaccine           String?   @mkp_connect_drupal_db.Char(6)
  vacc_rule               String?   @mkp_connect_drupal_db.VarChar(4)
  location                String?   @mkp_connect_drupal_db.VarChar(120)
  loc_block_id            String?   @mkp_connect_drupal_db.VarChar(10)
  city                    String?   @mkp_connect_drupal_db.VarChar(80)
  state                   String?   @mkp_connect_drupal_db.VarChar(40)
  zip                     String?   @mkp_connect_drupal_db.VarChar(20)
  country                 String?   @mkp_connect_drupal_db.VarChar(40)
  vgroup_abbreviation     String?   @mkp_connect_drupal_db.VarChar(20)
}

model temp_nwta_build_import_australia {
  id                      Int       @id @default(autoincrement())
  title                   String?   @mkp_connect_drupal_db.VarChar(120)
  center_area             String?   @mkp_connect_drupal_db.VarChar(80)
  region                  String?   @mkp_connect_drupal_db.VarChar(40)
  owner_nid               Int?
  mkpi                    String?   @mkp_connect_drupal_db.Char(4)
  email_prefix            String?   @mkp_connect_drupal_db.VarChar(40)
  staff_coordinator       String?   @mkp_connect_drupal_db.VarChar(80)
  staff_coordinator_email String?   @mkp_connect_drupal_db.VarChar(80)
  staff_coordinator_phone String?   @mkp_connect_drupal_db.VarChar(40)
  part_registrar          String?   @mkp_connect_drupal_db.VarChar(80)
  part_registrar_email    String?   @mkp_connect_drupal_db.VarChar(80)
  part_registrar_phone    String?   @mkp_connect_drupal_db.VarChar(40)
  admin                   String?   @mkp_connect_drupal_db.VarChar(80)
  admin_email             String?   @mkp_connect_drupal_db.VarChar(80)
  admin_phone             String?   @mkp_connect_drupal_db.VarChar(40)
  registration_link       String?   @mkp_connect_drupal_db.VarChar(120)
  staff_registration_link String?   @mkp_connect_drupal_db.VarChar(120)
  staff_start_date        DateTime? @mkp_connect_drupal_db.Date
  staff_start_time        DateTime? @mkp_connect_drupal_db.Time(0)
  nb_start_date           DateTime? @mkp_connect_drupal_db.Date
  nb_start_time           DateTime? @mkp_connect_drupal_db.Time(0)
  nwta_end_date           DateTime? @mkp_connect_drupal_db.Date
  nwta_end_time           DateTime? @mkp_connect_drupal_db.Time(0)
  staff_reg_start         DateTime? @mkp_connect_drupal_db.Date
  staff_reg_start_time    DateTime? @mkp_connect_drupal_db.Time(0)
  staff_reg_end           DateTime? @mkp_connect_drupal_db.Date
  staff_reg_end_time      DateTime? @mkp_connect_drupal_db.Time(0)
  part_reg_start          DateTime? @mkp_connect_drupal_db.Date
  part_reg_start_time     DateTime? @mkp_connect_drupal_db.Time(0)
  part_reg_end            DateTime? @mkp_connect_drupal_db.Date
  part_reg_end_time       DateTime? @mkp_connect_drupal_db.Time(0)
  part_fee                Int?
  staff_fee               Int?
  mos_fee                 Int?
  max_part                Int?
  gateway                 String?   @mkp_connect_drupal_db.Char(6)
  location                String?   @mkp_connect_drupal_db.VarChar(120)
  loc_block_id            String?   @mkp_connect_drupal_db.VarChar(10)
  city                    String?   @mkp_connect_drupal_db.VarChar(80)
  state                   String?   @mkp_connect_drupal_db.VarChar(40)
  zip                     String?   @mkp_connect_drupal_db.VarChar(20)
  country                 String?   @mkp_connect_drupal_db.VarChar(40)
  vgroup_abbreviation     String?   @mkp_connect_drupal_db.VarChar(20)
}

model temp_nwta_build_import_calendar {
  id                   Int       @id @default(autoincrement())
  title                String?   @mkp_connect_drupal_db.VarChar(120)
  center_area          String?   @mkp_connect_drupal_db.VarChar(80)
  region               String?   @mkp_connect_drupal_db.VarChar(40)
  mkpi                 String?   @mkp_connect_drupal_db.Char(4)
  email_prefix         String?   @mkp_connect_drupal_db.VarChar(40)
  part_registrar       String?   @mkp_connect_drupal_db.VarChar(80)
  part_registrar_email String?   @mkp_connect_drupal_db.VarChar(80)
  part_registrar_phone String?   @mkp_connect_drupal_db.VarChar(40)
  admin                String?   @mkp_connect_drupal_db.VarChar(80)
  admin_email          String?   @mkp_connect_drupal_db.VarChar(80)
  admin_phone          String?   @mkp_connect_drupal_db.VarChar(40)
  registration_link    String?   @mkp_connect_drupal_db.VarChar(120)
  nb_start_date        DateTime? @mkp_connect_drupal_db.Date
  nb_start_time        DateTime? @mkp_connect_drupal_db.Time(0)
  nwta_end_date        DateTime? @mkp_connect_drupal_db.Date
  nwta_end_time        DateTime? @mkp_connect_drupal_db.Time(0)
  location             String?   @mkp_connect_drupal_db.VarChar(120)
  city                 String?   @mkp_connect_drupal_db.VarChar(80)
  state                String?   @mkp_connect_drupal_db.VarChar(40)
  zip                  String?   @mkp_connect_drupal_db.VarChar(20)
  country              String?   @mkp_connect_drupal_db.VarChar(40)
  civi_part_event      Int?
}

model temp_nwta_build_import_copy1 {
  id                      Int       @id @default(autoincrement())
  title                   String?   @mkp_connect_drupal_db.VarChar(120)
  center_area             String?   @mkp_connect_drupal_db.VarChar(80)
  region                  String?   @mkp_connect_drupal_db.VarChar(40)
  owner_nid               Int?
  mkpi                    String?   @mkp_connect_drupal_db.Char(4)
  email_prefix            String?   @mkp_connect_drupal_db.VarChar(40)
  staff_coordinator       String?   @mkp_connect_drupal_db.VarChar(80)
  staff_coordinator_email String?   @mkp_connect_drupal_db.VarChar(80)
  staff_coordinator_phone String?   @mkp_connect_drupal_db.VarChar(40)
  part_registrar          String?   @mkp_connect_drupal_db.VarChar(80)
  part_registrar_email    String?   @mkp_connect_drupal_db.VarChar(80)
  part_registrar_phone    String?   @mkp_connect_drupal_db.VarChar(40)
  admin                   String?   @mkp_connect_drupal_db.VarChar(80)
  admin_email             String?   @mkp_connect_drupal_db.VarChar(80)
  admin_phone             String?   @mkp_connect_drupal_db.VarChar(40)
  registration_link       String?   @mkp_connect_drupal_db.VarChar(120)
  staff_registration_link String?   @mkp_connect_drupal_db.VarChar(120)
  staff_start_date        DateTime? @mkp_connect_drupal_db.Date
  staff_start_time        DateTime? @mkp_connect_drupal_db.Time(0)
  nb_start_date           DateTime? @mkp_connect_drupal_db.Date
  nb_start_time           DateTime? @mkp_connect_drupal_db.Time(0)
  nwta_end_date           DateTime? @mkp_connect_drupal_db.Date
  nwta_end_time           DateTime? @mkp_connect_drupal_db.Time(0)
  staff_reg_start         DateTime? @mkp_connect_drupal_db.Date
  staff_reg_start_time    DateTime? @mkp_connect_drupal_db.Time(0)
  staff_reg_end           DateTime? @mkp_connect_drupal_db.Date
  staff_reg_end_time      DateTime? @mkp_connect_drupal_db.Time(0)
  part_reg_start          DateTime? @mkp_connect_drupal_db.Date
  part_reg_start_time     DateTime? @mkp_connect_drupal_db.Time(0)
  part_reg_end            DateTime? @mkp_connect_drupal_db.Date
  part_reg_end_time       DateTime? @mkp_connect_drupal_db.Time(0)
  part_fee                Int?
  staff_fee               Int?
  mos_fee                 Int?
  accounting_code         String?   @mkp_connect_drupal_db.VarChar(40)
  class                   String?   @mkp_connect_drupal_db.VarChar(120)
  readiness               String?   @mkp_connect_drupal_db.Char(6)
  multipay                String?   @mkp_connect_drupal_db.Char(6)
  max_part                Int?
  gateway                 String?   @mkp_connect_drupal_db.Char(6)
  reg_login               String?   @mkp_connect_drupal_db.Char(6)
  waitlist                String?   @mkp_connect_drupal_db.Char(6)
  track_vaccine           String?   @mkp_connect_drupal_db.Char(6)
  vacc_rule               String?   @mkp_connect_drupal_db.VarChar(4)
  location                String?   @mkp_connect_drupal_db.VarChar(120)
  loc_block_id            String?   @mkp_connect_drupal_db.VarChar(10)
  city                    String?   @mkp_connect_drupal_db.VarChar(80)
  state                   String?   @mkp_connect_drupal_db.VarChar(40)
  zip                     String?   @mkp_connect_drupal_db.VarChar(20)
  country                 String?   @mkp_connect_drupal_db.VarChar(40)
  vgroup_abbreviation     String?   @mkp_connect_drupal_db.VarChar(20)
}

model temp_nwta_build_import_ous {
  id                      Int       @id @default(autoincrement())
  title                   String?   @mkp_connect_drupal_db.VarChar(120)
  center_area             String?   @mkp_connect_drupal_db.VarChar(80)
  region                  String?   @mkp_connect_drupal_db.VarChar(40)
  owner_nid               Int?
  mkpi                    String?   @mkp_connect_drupal_db.Char(4)
  email_prefix            String?   @mkp_connect_drupal_db.VarChar(40)
  staff_coordinator       String?   @mkp_connect_drupal_db.VarChar(80)
  staff_coordinator_email String?   @mkp_connect_drupal_db.VarChar(80)
  staff_coordinator_phone String?   @mkp_connect_drupal_db.VarChar(40)
  part_registrar          String?   @mkp_connect_drupal_db.VarChar(80)
  part_registrar_email    String?   @mkp_connect_drupal_db.VarChar(80)
  part_registrar_phone    String?   @mkp_connect_drupal_db.VarChar(40)
  admin                   String?   @mkp_connect_drupal_db.VarChar(80)
  admin_email             String?   @mkp_connect_drupal_db.VarChar(80)
  admin_phone             String?   @mkp_connect_drupal_db.VarChar(40)
  registration_link       String?   @mkp_connect_drupal_db.VarChar(120)
  staff_registration_link String?   @mkp_connect_drupal_db.VarChar(120)
  staff_start_date        DateTime? @mkp_connect_drupal_db.Date
  staff_start_time        DateTime? @mkp_connect_drupal_db.Time(0)
  nb_start_date           DateTime? @mkp_connect_drupal_db.Date
  nb_start_time           DateTime? @mkp_connect_drupal_db.Time(0)
  nwta_end_date           DateTime? @mkp_connect_drupal_db.Date
  nwta_end_time           DateTime? @mkp_connect_drupal_db.Time(0)
  staff_reg_start         DateTime? @mkp_connect_drupal_db.Date
  staff_reg_start_time    DateTime? @mkp_connect_drupal_db.Time(0)
  staff_reg_end           DateTime? @mkp_connect_drupal_db.Date
  staff_reg_end_time      DateTime? @mkp_connect_drupal_db.Time(0)
  part_reg_start          DateTime? @mkp_connect_drupal_db.Date
  part_reg_start_time     DateTime? @mkp_connect_drupal_db.Time(0)
  part_reg_end            DateTime? @mkp_connect_drupal_db.Date
  part_reg_end_time       DateTime? @mkp_connect_drupal_db.Time(0)
  part_fee                Int?
  staff_fee               Int?
  mos_fee                 Int?
  accounting_code         String?   @mkp_connect_drupal_db.VarChar(40)
  class                   String?   @mkp_connect_drupal_db.VarChar(120)
  readiness               String?   @mkp_connect_drupal_db.Char(6)
  multipay                String?   @mkp_connect_drupal_db.Char(6)
  max_part                Int?
  gateway                 String?   @mkp_connect_drupal_db.Char(6)
  reg_login               String?   @mkp_connect_drupal_db.Char(6)
  waitlist                String?   @mkp_connect_drupal_db.Char(6)
  track_vaccine           String?   @mkp_connect_drupal_db.Char(6)
  vacc_rule               String?   @mkp_connect_drupal_db.VarChar(4)
  location                String?   @mkp_connect_drupal_db.VarChar(120)
  loc_block_id            String?   @mkp_connect_drupal_db.VarChar(10)
  city                    String?   @mkp_connect_drupal_db.VarChar(80)
  state                   String?   @mkp_connect_drupal_db.VarChar(40)
  zip                     String?   @mkp_connect_drupal_db.VarChar(20)
  country                 String?   @mkp_connect_drupal_db.VarChar(40)
  vgroup_abbreviation     String?   @mkp_connect_drupal_db.VarChar(20)
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model temp_nwta_location {
  Title                   String?   @mkp_connect_drupal_db.VarChar(255)
  City                    String?   @mkp_connect_drupal_db.VarChar(40)
  State                   String?   @mkp_connect_drupal_db.VarChar(15)
  Country                 String?   @mkp_connect_drupal_db.VarChar(40)
  start_date              DateTime? @mkp_connect_drupal_db.DateTime(0)
  end_date                DateTime? @mkp_connect_drupal_db.DateTime(0)
  Gateway                 String?   @mkp_connect_drupal_db.VarChar(255)
  Center_Area             String?   @mkp_connect_drupal_db.VarChar(255)
  Price                   Int?
  early_bird              String?   @mkp_connect_drupal_db.VarChar(29)
  early_bird_deadline_803 DateTime? @mkp_connect_drupal_db.DateTime(0)
  nudity_814              String?   @mkp_connect_drupal_db.VarChar(255)
  Registration            String?   @mkp_connect_drupal_db.VarChar(274)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model temp_participant {
  event_id         Int       @mkp_connect_drupal_db.UnsignedInt
  contact_id       Int       @mkp_connect_drupal_db.UnsignedInt
  sort_name        String?   @mkp_connect_drupal_db.VarChar(64)
  readiness_status String?   @mkp_connect_drupal_db.VarChar(11)
  email            String?   @mkp_connect_drupal_db.VarChar(120)
  phone            String?   @mkp_connect_drupal_db.VarChar(32)
  street_address   String?   @mkp_connect_drupal_db.VarChar(96)
  city             String?   @mkp_connect_drupal_db.VarChar(64)
  state            String?   @mkp_connect_drupal_db.VarChar(64)
  postal_code      String?   @mkp_connect_drupal_db.VarChar(64)
  country          String?   @mkp_connect_drupal_db.VarChar(64)
  first_name       String?   @mkp_connect_drupal_db.VarChar(64)
  last_name        String?   @mkp_connect_drupal_db.VarChar(64)
  animal_name_2    String?   @mkp_connect_drupal_db.VarChar(80)
  birth_date_432   DateTime? @mkp_connect_drupal_db.DateTime(0)
  nwtagrad         Int?      @mkp_connect_drupal_db.TinyInt
  email_private    Int?      @mkp_connect_drupal_db.TinyInt
  address_private  Int?      @mkp_connect_drupal_db.TinyInt
  phone_private    Int?      @mkp_connect_drupal_db.TinyInt

  @@ignore
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model temp_readiness {
  id                 Int?    @default(0) @mkp_connect_drupal_db.UnsignedInt
  id1                Int     @id @default(autoincrement())
  sort_name          String? @mkp_connect_drupal_db.VarChar(128)
  event_title        String? @mkp_connect_drupal_db.VarChar(80)
  event_date         String? @mkp_connect_drupal_db.VarChar(10)
  nwta_graduate      String? @mkp_connect_drupal_db.VarChar(10)
  reviewed_status    String? @mkp_connect_drupal_db.VarChar(40)
  submission_date    String? @mkp_connect_drupal_db.MediumText
  last_reviewed_date String? @mkp_connect_drupal_db.MediumText

  @@index([id1], map: "id1")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model temp_readiness_roster {
  contact_id                  Int     @mkp_connect_drupal_db.UnsignedInt
  sort_name                   String? @mkp_connect_drupal_db.VarChar(128)
  role_id                     String? @mkp_connect_drupal_db.VarChar(128)
  height_533                  String? @mkp_connect_drupal_db.VarChar(255)
  weight_534                  String? @mkp_connect_drupal_db.VarChar(255)
  reviewer_comments_541       String? @mkp_connect_drupal_db.Text
  emergency_contact_name_656  String? @mkp_connect_drupal_db.VarChar(120)
  emergency_contact_phone_657 String? @mkp_connect_drupal_db.VarChar(100)
  on_site_summary_788         String? @mkp_connect_drupal_db.Text
  age                         Int?
  fully_vaccinated_751        Int?
  uf_id                       Int?

  @@ignore
}

model temp_training_build_import {
  id                       Int       @id @default(autoincrement())
  title                    String?   @mkp_connect_drupal_db.VarChar(120)
  training_type            String?   @mkp_connect_drupal_db.VarChar(40)
  calendar_title           String?   @mkp_connect_drupal_db.VarChar(120)
  center_area              String?   @mkp_connect_drupal_db.VarChar(80)
  region                   String?   @mkp_connect_drupal_db.VarChar(40)
  owner_nid                Int?
  mkpi                     String?   @mkp_connect_drupal_db.Char(4)
  contact                  String?   @mkp_connect_drupal_db.VarChar(80)
  contact_email            String?   @mkp_connect_drupal_db.VarChar(80)
  contact_phone            String?   @mkp_connect_drupal_db.VarChar(40)
  email_prefix             String?   @mkp_connect_drupal_db.VarChar(40)
  registration_link        String?   @mkp_connect_drupal_db.VarChar(120)
  training_start_date      DateTime? @mkp_connect_drupal_db.Date
  training_start_date_time DateTime? @mkp_connect_drupal_db.Time(0)
  training_end_date        DateTime? @mkp_connect_drupal_db.Date
  training_end_date_time   DateTime? @mkp_connect_drupal_db.Time(0)
  training_reg_start       DateTime? @mkp_connect_drupal_db.Date
  training_reg_start_time  DateTime? @mkp_connect_drupal_db.Time(0)
  training_reg_end         DateTime? @mkp_connect_drupal_db.Date
  training_reg_end_time    DateTime? @mkp_connect_drupal_db.Time(0)
  location                 String?   @mkp_connect_drupal_db.VarChar(60)
  loc_block_id             Int?
  training_city            String?   @mkp_connect_drupal_db.VarChar(80)
  training_state           String?   @mkp_connect_drupal_db.VarChar(40)
  training_zip             String?   @mkp_connect_drupal_db.VarChar(20)
  training_country         String?   @mkp_connect_drupal_db.VarChar(40)
  tuition                  Int?
  center_nid               Int?
  acctg_class              String?   @mkp_connect_drupal_db.VarChar(40)
  max_registrants          Int?
  audience                 String?   @mkp_connect_drupal_db.VarChar(40)
  prerequisite             String?   @mkp_connect_drupal_db.VarChar(40)
  sponsored                String?   @mkp_connect_drupal_db.VarChar(40)
  sponsoring_entity        String?   @mkp_connect_drupal_db.VarChar(60)
  do_not_list              String?   @mkp_connect_drupal_db.VarChar(40)
  specialized              String?   @mkp_connect_drupal_db.VarChar(40)
  auto_regog               String?   @mkp_connect_drupal_db.VarChar(5)
  reg_login                String?   @mkp_connect_drupal_db.VarChar(5)
  vgroup_abbreviation      String?   @mkp_connect_drupal_db.VarChar(40)
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model temp_webform_component {
  nid      Int     @default(0) @mkp_connect_drupal_db.UnsignedInt
  cid      Int     @default(0) @mkp_connect_drupal_db.UnsignedSmallInt
  pid      Int     @default(0) @mkp_connect_drupal_db.UnsignedSmallInt
  form_key String? @mkp_connect_drupal_db.VarChar(128)
  name     String  @mkp_connect_drupal_db.Text
  type     String? @mkp_connect_drupal_db.VarChar(16)
  value    String  @mkp_connect_drupal_db.Text
  extra    String  @mkp_connect_drupal_db.Text
  required Int     @default(0) @mkp_connect_drupal_db.TinyInt
  weight   Int     @default(0) @mkp_connect_drupal_db.SmallInt

  @@ignore
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model themekey_properties {
  id        Int    @id @default(autoincrement()) @mkp_connect_drupal_db.UnsignedInt
  property  String @default("") @mkp_connect_drupal_db.VarChar(255)
  operator  String @default("=") @mkp_connect_drupal_db.VarChar(2)
  value     String @default("") @mkp_connect_drupal_db.VarChar(255)
  weight    Int    @default(0)
  theme     String @default("") @mkp_connect_drupal_db.VarChar(255)
  enabled   Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  wildcards String @mkp_connect_drupal_db.LongText
  parent    Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  module    String @default("") @mkp_connect_drupal_db.VarChar(255)

  @@index([enabled, parent, weight], map: "enabled_parent_weight")
  @@index([parent, weight], map: "parent_weight")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model trigger_assignments {
  hook   String @default("") @mkp_connect_drupal_db.VarChar(78)
  aid    String @default("") @mkp_connect_drupal_db.VarChar(255)
  weight Int    @default(0)

  @@id([hook, aid])
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model url_alias {
  pid      Int    @id @default(autoincrement()) @mkp_connect_drupal_db.UnsignedInt
  source   String @default("") @mkp_connect_drupal_db.VarChar(255)
  alias    String @default("") @mkp_connect_drupal_db.VarChar(255)
  language String @default("") @mkp_connect_drupal_db.VarChar(12)

  @@index([alias, language, pid], map: "alias_language_pid")
  @@index([source, language, pid], map: "source_language_pid")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model users {
  uid              Int     @id @default(0) @mkp_connect_drupal_db.UnsignedInt
  name             String  @unique(map: "name") @default("") @mkp_connect_drupal_db.VarChar(60)
  pass             String  @default("") @mkp_connect_drupal_db.VarChar(128)
  mail             String? @default("") @mkp_connect_drupal_db.VarChar(254)
  theme            String  @default("") @mkp_connect_drupal_db.VarChar(255)
  signature        String  @default("") @mkp_connect_drupal_db.VarChar(255)
  signature_format String? @mkp_connect_drupal_db.VarChar(255)
  created          Int     @default(0)
  access           Int     @default(0)
  login            Int     @default(0)
  status           Int     @default(0) @mkp_connect_drupal_db.TinyInt
  timezone         String? @mkp_connect_drupal_db.VarChar(32)
  language         String  @default("") @mkp_connect_drupal_db.VarChar(12)
  picture          Int     @default(0)
  init             String? @default("") @mkp_connect_drupal_db.VarChar(254)
  data             Bytes?
  changed          Int     @default(0)

  @@index([access], map: "access")
  @@index([changed], map: "changed")
  @@index([created], map: "created")
  @@index([mail], map: "mail")
  @@index([picture], map: "picture")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model users_copy1 {
  uid              Int     @id @default(0) @mkp_connect_drupal_db.UnsignedInt
  name             String  @unique(map: "name") @default("") @mkp_connect_drupal_db.VarChar(60)
  pass             String  @default("") @mkp_connect_drupal_db.VarChar(128)
  mail             String? @default("") @mkp_connect_drupal_db.VarChar(254)
  theme            String  @default("") @mkp_connect_drupal_db.VarChar(255)
  signature        String  @default("") @mkp_connect_drupal_db.VarChar(255)
  signature_format String? @mkp_connect_drupal_db.VarChar(255)
  created          Int     @default(0)
  access           Int     @default(0)
  login            Int     @default(0)
  status           Int     @default(0) @mkp_connect_drupal_db.TinyInt
  timezone         String? @mkp_connect_drupal_db.VarChar(32)
  language         String  @default("") @mkp_connect_drupal_db.VarChar(12)
  picture          Int     @default(0)
  init             String? @default("") @mkp_connect_drupal_db.VarChar(254)
  data             Bytes?
  changed          Int     @default(0)

  @@index([access], map: "access")
  @@index([changed], map: "changed")
  @@index([created], map: "created")
  @@index([mail], map: "mail")
  @@index([picture], map: "picture")
}

model users_drupal6 {
  uid              Int     @id @default(autoincrement()) @mkp_connect_drupal_db.UnsignedInt
  name             String  @unique(map: "name") @default("") @mkp_connect_drupal_db.VarChar(60)
  pass             String  @default("") @mkp_connect_drupal_db.VarChar(32)
  mail             String? @default("") @mkp_connect_drupal_db.VarChar(320)
  mode             Int     @default(0) @mkp_connect_drupal_db.TinyInt
  sort             Int?    @default(0) @mkp_connect_drupal_db.TinyInt
  threshold        Int?    @default(0) @mkp_connect_drupal_db.TinyInt
  theme            String  @default("") @mkp_connect_drupal_db.VarChar(255)
  signature        String  @default("") @mkp_connect_drupal_db.VarChar(255)
  signature_format Int     @default(0) @mkp_connect_drupal_db.SmallInt
  created          Int     @default(0)
  access           Int     @default(0)
  login            Int     @default(0)
  status           Int     @default(0) @mkp_connect_drupal_db.TinyInt
  timezone         String? @mkp_connect_drupal_db.VarChar(8)
  language         String  @default("") @mkp_connect_drupal_db.VarChar(12)
  picture          String  @default("") @mkp_connect_drupal_db.VarChar(255)
  init             String? @default("") @mkp_connect_drupal_db.VarChar(64)
  data             String? @mkp_connect_drupal_db.LongText
  timezone_name    String  @default("") @mkp_connect_drupal_db.VarChar(50)

  @@index([access], map: "access")
  @@index([created], map: "created")
  @@index([mail], map: "mail")
}

model users_roles {
  uid Int @default(0) @mkp_connect_drupal_db.UnsignedInt
  rid Int @default(0) @mkp_connect_drupal_db.UnsignedInt

  @@id([uid, rid])
  @@index([rid], map: "rid")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model users_roles_copy1 {
  uid Int @default(0) @mkp_connect_drupal_db.UnsignedInt
  rid Int @default(0) @mkp_connect_drupal_db.UnsignedInt

  @@id([uid, rid])
  @@index([rid], map: "rid")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model users_tba {
  uid              Int     @default(0) @mkp_connect_drupal_db.UnsignedInt
  name             String  @default("") @mkp_connect_drupal_db.VarChar(60)
  pass             String  @default("") @mkp_connect_drupal_db.VarChar(32)
  mail             String? @default("") @mkp_connect_drupal_db.VarChar(320)
  mode             Int     @default(0) @mkp_connect_drupal_db.TinyInt
  sort             Int?    @default(0) @mkp_connect_drupal_db.TinyInt
  threshold        Int?    @default(0) @mkp_connect_drupal_db.TinyInt
  theme            String  @default("") @mkp_connect_drupal_db.VarChar(255)
  signature        String  @default("") @mkp_connect_drupal_db.VarChar(255)
  signature_format Int     @default(0) @mkp_connect_drupal_db.SmallInt
  created          Int     @default(0)
  access           Int     @default(0)
  login            Int     @default(0)
  status           Int     @default(0) @mkp_connect_drupal_db.TinyInt
  timezone         String? @mkp_connect_drupal_db.VarChar(8)
  language         String  @default("") @mkp_connect_drupal_db.VarChar(12)
  picture          String  @default("") @mkp_connect_drupal_db.VarChar(255)
  init             String? @default("") @mkp_connect_drupal_db.VarChar(64)
  data             String? @mkp_connect_drupal_db.LongText
  timezone_name    String  @default("") @mkp_connect_drupal_db.VarChar(50)

  @@ignore
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model variable {
  name  String @id @default("") @mkp_connect_drupal_db.VarChar(128)
  value Bytes
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model variable_copy1 {
  name  String @id @default("") @mkp_connect_drupal_db.VarChar(128)
  value Bytes
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model vars {
  name  String @default("") @mkp_connect_drupal_db.VarChar(128)
  value String @mkp_connect_drupal_db.LongText

  @@ignore
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model views_display {
  vid             Int     @default(0) @mkp_connect_drupal_db.UnsignedInt
  id              String  @default("") @mkp_connect_drupal_db.VarChar(64)
  display_title   String  @default("") @mkp_connect_drupal_db.VarChar(64)
  display_plugin  String  @default("") @mkp_connect_drupal_db.VarChar(64)
  position        Int?    @default(0)
  display_options String? @mkp_connect_drupal_db.LongText

  @@id([vid, id])
  @@index([vid, position], map: "vid")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model views_display_copy1 {
  vid             Int     @default(0) @mkp_connect_drupal_db.UnsignedInt
  id              String  @default("") @mkp_connect_drupal_db.VarChar(64)
  display_title   String  @default("") @mkp_connect_drupal_db.VarChar(64)
  display_plugin  String  @default("") @mkp_connect_drupal_db.VarChar(64)
  position        Int?    @default(0)
  display_options String? @mkp_connect_drupal_db.LongText

  @@id([vid, id])
  @@index([vid, position], map: "vid")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model views_distinct {
  view_name  String @default("") @mkp_connect_drupal_db.VarChar(64)
  display_id String @default("") @mkp_connect_drupal_db.VarChar(64)
  field_id   String @default("") @mkp_connect_drupal_db.VarChar(128)
  settings   Bytes? @mkp_connect_drupal_db.Blob

  @@id([view_name, display_id, field_id])
  @@index([view_name, display_id, field_id], map: "field_setting")
  @@index([view_name], map: "view")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model views_distinct_copy1 {
  view_name  String @default("") @mkp_connect_drupal_db.VarChar(64)
  display_id String @default("") @mkp_connect_drupal_db.VarChar(64)
  field_id   String @default("") @mkp_connect_drupal_db.VarChar(128)
  settings   Bytes? @mkp_connect_drupal_db.Blob

  @@id([view_name, display_id, field_id])
  @@index([view_name, display_id, field_id], map: "field_setting")
  @@index([view_name], map: "view")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model views_view {
  vid         Int     @id @default(autoincrement()) @mkp_connect_drupal_db.UnsignedInt
  name        String  @unique(map: "name") @default("") @mkp_connect_drupal_db.VarChar(128)
  description String? @default("") @mkp_connect_drupal_db.VarChar(255)
  tag         String? @default("") @mkp_connect_drupal_db.VarChar(255)
  base_table  String  @default("") @mkp_connect_drupal_db.VarChar(64)
  human_name  String? @default("") @mkp_connect_drupal_db.VarChar(255)
  core        Int?    @default(0)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model views_view_copy1 {
  vid         Int     @id @default(autoincrement()) @mkp_connect_drupal_db.UnsignedInt
  name        String  @unique(map: "name") @default("") @mkp_connect_drupal_db.VarChar(128)
  description String? @default("") @mkp_connect_drupal_db.VarChar(255)
  tag         String? @default("") @mkp_connect_drupal_db.VarChar(255)
  base_table  String  @default("") @mkp_connect_drupal_db.VarChar(64)
  human_name  String? @default("") @mkp_connect_drupal_db.VarChar(255)
  core        Int?    @default(0)
}

model votingapi_cache {
  vote_cache_id Int    @id @default(autoincrement()) @mkp_connect_drupal_db.UnsignedInt
  entity_type   String @default("node") @mkp_connect_drupal_db.VarChar(64)
  entity_id     Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  value         Float  @default(0) @mkp_connect_drupal_db.Float
  value_type    String @default("percent") @mkp_connect_drupal_db.VarChar(64)
  tag           String @default("vote") @mkp_connect_drupal_db.VarChar(64)
  function      String @default("") @mkp_connect_drupal_db.VarChar(64)
  timestamp     Int    @default(0) @mkp_connect_drupal_db.UnsignedInt

  @@index([entity_type, entity_id], map: "content")
  @@index([entity_type, entity_id, function], map: "content_function")
  @@index([entity_type, entity_id, tag, function], map: "content_tag_func")
  @@index([entity_type, entity_id, value_type, tag], map: "content_vtype_tag")
  @@index([entity_type, entity_id, value_type, tag, function], map: "content_vtype_tag_func")
}

model votingapi_vote {
  vote_id     Int     @id @default(autoincrement()) @mkp_connect_drupal_db.UnsignedInt
  entity_type String  @default("node") @mkp_connect_drupal_db.VarChar(64)
  entity_id   Int     @default(0) @mkp_connect_drupal_db.UnsignedInt
  value       Float   @default(0) @mkp_connect_drupal_db.Float
  value_type  String  @default("percent") @mkp_connect_drupal_db.VarChar(64)
  tag         String  @default("vote") @mkp_connect_drupal_db.VarChar(64)
  uid         Int     @default(0) @mkp_connect_drupal_db.UnsignedInt
  timestamp   Int     @default(0) @mkp_connect_drupal_db.UnsignedInt
  vote_source String? @mkp_connect_drupal_db.VarChar(255)

  @@index([entity_type, entity_id, vote_source], map: "content_source")
  @@index([entity_type, entity_id, uid], map: "content_uid")
  @@index([entity_type, uid], map: "content_uid_2")
  @@index([entity_type, entity_id, value_type, tag], map: "content_value_tag")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model watchdog {
  wid       Int     @id @default(autoincrement())
  uid       Int     @default(0)
  type      String  @default("") @mkp_connect_drupal_db.VarChar(64)
  message   String  @mkp_connect_drupal_db.LongText
  variables Bytes
  severity  Int     @default(0) @mkp_connect_drupal_db.UnsignedTinyInt
  link      String? @default("") @mkp_connect_drupal_db.VarChar(255)
  location  String  @mkp_connect_drupal_db.Text
  referer   String? @mkp_connect_drupal_db.Text
  hostname  String  @default("") @mkp_connect_drupal_db.VarChar(128)
  timestamp Int     @default(0)

  @@index([severity], map: "severity")
  @@index([type], map: "type")
  @@index([uid], map: "uid")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model webform {
  nid                              Int     @id @mkp_connect_drupal_db.UnsignedInt
  next_serial                      Int     @default(1) @mkp_connect_drupal_db.UnsignedInt
  confirmation                     String  @mkp_connect_drupal_db.Text
  confirmation_format              String? @mkp_connect_drupal_db.VarChar(255)
  redirect_url                     String? @default("<confirmation>") @mkp_connect_drupal_db.VarChar(2048)
  status                           Int     @default(1) @mkp_connect_drupal_db.TinyInt
  block                            Int     @default(0) @mkp_connect_drupal_db.TinyInt
  allow_draft                      Int     @default(0) @mkp_connect_drupal_db.TinyInt
  auto_save                        Int     @default(0) @mkp_connect_drupal_db.TinyInt
  submit_notice                    Int     @default(1) @mkp_connect_drupal_db.TinyInt
  confidential                     Int     @default(0) @mkp_connect_drupal_db.TinyInt
  submit_text                      String? @mkp_connect_drupal_db.VarChar(255)
  submit_limit                     Int     @default(-1) @mkp_connect_drupal_db.TinyInt
  submit_interval                  Int     @default(-1)
  total_submit_limit               Int     @default(-1)
  total_submit_interval            Int     @default(-1)
  progressbar_bar                  Int     @default(0) @mkp_connect_drupal_db.TinyInt
  progressbar_page_number          Int     @default(0) @mkp_connect_drupal_db.TinyInt
  progressbar_percent              Int     @default(0) @mkp_connect_drupal_db.TinyInt
  progressbar_pagebreak_labels     Int     @default(0) @mkp_connect_drupal_db.TinyInt
  progressbar_include_confirmation Int     @default(0) @mkp_connect_drupal_db.TinyInt
  progressbar_label_first          String? @mkp_connect_drupal_db.VarChar(255)
  progressbar_label_confirmation   String? @mkp_connect_drupal_db.VarChar(255)
  preview                          Int     @default(0) @mkp_connect_drupal_db.TinyInt
  preview_next_button_label        String? @mkp_connect_drupal_db.VarChar(255)
  preview_prev_button_label        String? @mkp_connect_drupal_db.VarChar(255)
  preview_title                    String? @mkp_connect_drupal_db.VarChar(255)
  preview_message                  String  @mkp_connect_drupal_db.Text
  preview_message_format           String? @mkp_connect_drupal_db.VarChar(255)
  preview_excluded_components      String  @mkp_connect_drupal_db.Text
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model webform_civicrm_forms {
  nid                     Int     @id @default(0) @mkp_connect_drupal_db.UnsignedInt
  data                    String? @mkp_connect_drupal_db.Text
  prefix_known            String  @mkp_connect_drupal_db.Text
  prefix_unknown          String  @mkp_connect_drupal_db.Text
  message                 String  @default("") @mkp_connect_drupal_db.VarChar(255)
  confirm_subscription    Int     @default(0) @mkp_connect_drupal_db.TinyInt
  block_unknown_users     Int     @default(0) @mkp_connect_drupal_db.TinyInt
  create_fieldsets        Int     @default(0) @mkp_connect_drupal_db.TinyInt
  new_contact_source      String  @default("") @mkp_connect_drupal_db.VarChar(255)
  create_new_relationship Int     @default(0) @mkp_connect_drupal_db.TinyInt
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model webform_civicrm_submissions {
  sid          Int     @id @default(0) @mkp_connect_drupal_db.UnsignedInt
  contact_id   String  @default("") @mkp_connect_drupal_db.VarChar(2000)
  civicrm_data String? @mkp_connect_drupal_db.Text
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model webform_component {
  nid      Int     @default(0) @mkp_connect_drupal_db.UnsignedInt
  cid      Int     @default(0) @mkp_connect_drupal_db.UnsignedSmallInt
  pid      Int     @default(0) @mkp_connect_drupal_db.UnsignedSmallInt
  form_key String? @mkp_connect_drupal_db.VarChar(128)
  name     String  @mkp_connect_drupal_db.Text
  type     String? @mkp_connect_drupal_db.VarChar(16)
  value    String  @mkp_connect_drupal_db.Text
  extra    String  @mkp_connect_drupal_db.Text
  required Int     @default(0) @mkp_connect_drupal_db.TinyInt
  weight   Int     @default(0) @mkp_connect_drupal_db.SmallInt

  @@id([nid, cid])
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model webform_conditional {
  nid    Int     @default(0) @mkp_connect_drupal_db.UnsignedInt
  rgid   Int     @default(0) @mkp_connect_drupal_db.UnsignedSmallInt
  andor  String? @mkp_connect_drupal_db.VarChar(128)
  weight Int     @default(0) @mkp_connect_drupal_db.SmallInt

  @@id([nid, rgid])
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model webform_conditional_actions {
  nid         Int     @default(0) @mkp_connect_drupal_db.UnsignedInt
  rgid        Int     @default(0) @mkp_connect_drupal_db.UnsignedSmallInt
  aid         Int     @default(0) @mkp_connect_drupal_db.UnsignedSmallInt
  target_type String? @mkp_connect_drupal_db.VarChar(128)
  target      String? @mkp_connect_drupal_db.VarChar(128)
  invert      Int     @default(0) @mkp_connect_drupal_db.UnsignedSmallInt
  action      String? @mkp_connect_drupal_db.VarChar(128)
  argument    String? @mkp_connect_drupal_db.Text

  @@id([nid, rgid, aid])
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model webform_conditional_rules {
  nid         Int     @default(0) @mkp_connect_drupal_db.UnsignedInt
  rgid        Int     @default(0) @mkp_connect_drupal_db.UnsignedSmallInt
  rid         Int     @default(0) @mkp_connect_drupal_db.UnsignedSmallInt
  source_type String? @mkp_connect_drupal_db.VarChar(128)
  source      Int     @default(0) @mkp_connect_drupal_db.UnsignedSmallInt
  operator    String? @mkp_connect_drupal_db.VarChar(128)
  value       String? @mkp_connect_drupal_db.Text

  @@id([nid, rgid, rid])
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model webform_emails {
  nid                 Int     @default(0) @mkp_connect_drupal_db.UnsignedInt
  eid                 Int     @default(0) @mkp_connect_drupal_db.UnsignedSmallInt
  email               String? @mkp_connect_drupal_db.Text
  subject             String? @mkp_connect_drupal_db.Text
  from_name           String? @mkp_connect_drupal_db.Text
  from_address        String? @mkp_connect_drupal_db.Text
  template            String? @mkp_connect_drupal_db.Text
  excluded_components String  @mkp_connect_drupal_db.Text
  html                Int     @default(0) @mkp_connect_drupal_db.UnsignedTinyInt
  attachments         Int     @default(0) @mkp_connect_drupal_db.UnsignedTinyInt
  exclude_empty       Int     @default(0) @mkp_connect_drupal_db.UnsignedTinyInt
  extra               String  @mkp_connect_drupal_db.Text
  status              Int     @default(1) @mkp_connect_drupal_db.UnsignedTinyInt

  @@id([nid, eid])
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model webform_last_download {
  nid       Int @default(0) @mkp_connect_drupal_db.UnsignedInt
  uid       Int @default(0) @mkp_connect_drupal_db.UnsignedInt
  sid       Int @default(0) @mkp_connect_drupal_db.UnsignedInt
  requested Int @default(0) @mkp_connect_drupal_db.UnsignedInt

  @@id([nid, uid])
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model webform_roles {
  nid Int @default(0) @mkp_connect_drupal_db.UnsignedInt
  rid Int @default(0) @mkp_connect_drupal_db.UnsignedInt

  @@id([nid, rid])
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model webform_submissions {
  sid                Int     @id @default(autoincrement()) @mkp_connect_drupal_db.UnsignedInt
  nid                Int     @default(0) @mkp_connect_drupal_db.UnsignedInt
  serial             Int     @mkp_connect_drupal_db.UnsignedInt
  uid                Int     @default(0) @mkp_connect_drupal_db.UnsignedInt
  is_draft           Int     @default(0) @mkp_connect_drupal_db.TinyInt
  highest_valid_page Int     @default(0) @mkp_connect_drupal_db.SmallInt
  submitted          Int     @default(0)
  completed          Int     @default(0)
  modified           Int     @default(0)
  remote_addr        String? @mkp_connect_drupal_db.VarChar(128)

  @@unique([nid, serial], map: "nid_serial")
  @@unique([sid, nid], map: "sid_nid")
  @@index([nid, sid], map: "nid_sid")
  @@index([nid, uid, sid], map: "nid_uid_sid")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model webform_submitted_data {
  nid  Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  sid  Int    @default(0) @mkp_connect_drupal_db.UnsignedInt
  cid  Int    @default(0) @mkp_connect_drupal_db.UnsignedSmallInt
  no   String @default("0") @mkp_connect_drupal_db.VarChar(128)
  data String @mkp_connect_drupal_db.MediumText

  @@id([nid, sid, cid, no])
  @@index([data(length: 64)], map: "data")
  @@index([nid], map: "nid")
  @@index([sid, nid], map: "sid_nid")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model zipcodes {
  zip       String  @default("0") @mkp_connect_drupal_db.VarChar(16)
  city      String  @default("") @mkp_connect_drupal_db.VarChar(30)
  state     String  @default("") @mkp_connect_drupal_db.VarChar(30)
  latitude  Decimal @default(0.000000) @mkp_connect_drupal_db.Decimal(10, 6)
  longitude Decimal @default(0.000000) @mkp_connect_drupal_db.Decimal(10, 6)
  timezone  Int     @default(0) @mkp_connect_drupal_db.TinyInt
  dst       Int     @default(0) @mkp_connect_drupal_db.TinyInt
  country   String  @default("") @mkp_connect_drupal_db.Char(2)

  @@index([country], map: "country")
  @@index([latitude], map: "latitude")
  @@index([longitude], map: "longitude")
  @@index([country, zip], map: "pc")
  @@index([zip], map: "zip")
  @@ignore
}
